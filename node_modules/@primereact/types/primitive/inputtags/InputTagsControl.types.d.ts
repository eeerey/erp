/**
 *
 * InputTags.Control exposes the props needed to control a text-entry element
 * (a plain `<input>`, an `<AutoComplete.Input>`, etc.) plus tag-mutation helpers.
 *
 * @module InputTagsControl
 * @group components
 */
import type { ComponentInstance } from '@primereact/types/core';
import type { UseInputTagsControlProps } from '@primereact/types/headless/inputtags';
import type { BaseComponentProps, PassThroughType } from '../..';
import type { InputTagsRootInstance } from './InputTagsRoot.types';
/**
 * Defines passthrough(pt) options type in InputTagsControl component.
 */
export type InputTagsControlPassThroughType<E> = PassThroughType<InputTagsControlInstance, E>;
/**
 * Defines passthrough(pt) options of InputTagsControl component.
 */
export interface InputTagsControlPassThrough {
}
/**
 * Arguments handed to the `InputTags.Control` render prop.
 */
export interface InputTagsControlRenderArgs {
    /**
     * Props for the text-entry element. Spread onto a plain `<input>` for a basic
     * tag-input, or onto `<AutoComplete.Input>` for typeahead-driven tag entry.
     */
    controlProps: UseInputTagsControlProps;
    /**
     * Adds one or more tags to the value (each subject to `max`, `allowDuplicate`, trimming).
     * Accepts a single string or an array of strings.
     */
    add: (tags: string | string[]) => string[];
    /**
     * Removes the tag at the given index.
     */
    remove: (index: number) => void;
    /**
     * Removes the last tag in the value.
     */
    removeLast: () => void;
    /**
     * Removes every tag.
     */
    removeAll: () => void;
    /**
     * The current tag list.
     */
    value: string[];
    /**
     * The current text-entry value.
     */
    inputValue: string;
    /**
     * Whether the text-entry element currently has focus.
     */
    focused: boolean;
}
/**
 * Defines valid properties in InputTagsControl component. `children` is a function
 * invoked with the bridging API.
 */
export interface InputTagsControlProps extends BaseComponentProps<InputTagsControlInstance, unknown, InputTagsControlPassThrough> {
    children: (args: InputTagsControlRenderArgs) => React.ReactNode;
}
/**
 * Defines valid state in InputTagsControl component.
 */
export interface InputTagsControlState {
}
/**
 * Defines the methods and properties exposed by InputTagsControl component.
 */
export interface InputTagsControlExposes {
    /**
     * The InputTags component instance.
     */
    inputtags: InputTagsRootInstance | undefined | null;
}
/**
 * Instance of InputTagsControl component.
 */
export type InputTagsControlInstance = ComponentInstance<InputTagsControlProps, InputTagsControlState, InputTagsControlExposes>;
