/**
 *
 * DataTableFrozenTBody renders a tbody that stays pinned to the top of the scroll viewport.
 * It does not fetch any data on its own — children are authored by the consumer.
 *
 * [Live Demo](https://www.primereact.org/datatable/)
 *
 * @module DataTableFrozenTBody
 * @group components
 *
 */
import type { ComponentInstance } from '@primereact/types/core';
import * as React from 'react';
import type { BaseComponentProps, PassThroughType } from '../..';
import type { DataTableRootInstance } from './DataTableRoot.types';
/**
 * Defines passthrough(pt) options type in DataTableFrozenTBody component.
 */
export type DataTableFrozenTBodyPassThroughType<E> = PassThroughType<DataTableFrozenTBodyInstance, E>;
/**
 * Defines passthrough(pt) options of DataTableFrozenTBody component.
 */
export interface DataTableFrozenTBodyPassThrough {
    /**
     * Used to pass attributes to the root's DOM element.
     */
    root?: DataTableFrozenTBodyPassThroughType<React.HTMLAttributes<HTMLTableSectionElement>>;
}
/**
 * Defines valid properties in DataTableFrozenTBody component.
 */
export interface DataTableFrozenTBodyProps extends BaseComponentProps<DataTableFrozenTBodyInstance, unknown, DataTableFrozenTBodyPassThrough, 'tbody'> {
    /**
     * Frozen rows that the consumer wants pinned at the top of the scroll viewport.
     */
    children?: React.ReactNode;
}
/**
 * Defines valid state in DataTableFrozenTBody component.
 */
export interface DataTableFrozenTBodyState {
}
/**
 * Defines the methods and properties exposed by DataTableFrozenTBody component.
 */
export interface DataTableFrozenTBodyExposes {
    /**
     * The DataTable component instance.
     */
    datatable: DataTableRootInstance | undefined | null;
}
/**
 * Instance of DataTableFrozenTBody component.
 */
export type DataTableFrozenTBodyInstance = ComponentInstance<DataTableFrozenTBodyProps, DataTableFrozenTBodyState, DataTableFrozenTBodyExposes>;
