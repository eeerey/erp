/**
 * @module useSidebarLayout
 * @group headless
 */
import type { HeadlessInstance } from '@primereact/types/core';
import type React from 'react';
export interface SidebarRegistryEntry {
    toggle: (event?: React.SyntheticEvent) => void;
    collapse: (event?: React.SyntheticEvent) => void;
    open: boolean;
    state: 'expanded' | 'collapsed';
    overlay: boolean;
    hideOnOutsideClick: boolean;
}
export interface UseSidebarLayoutProps {
}
export interface UseSidebarLayoutState {
}
export interface UseSidebarLayoutExposes {
    /**
     * Publish a sidebar entry to the layout. Pass `null` to remove the sidebar
     * (called from the cleanup of the publishing effect on unmount).
     */
    setSidebar: (id: string, entry: SidebarRegistryEntry | null) => void;
    toggle: (target?: string, event?: React.SyntheticEvent) => void;
    collapse: (target?: string, event?: React.SyntheticEvent) => void;
    backdropProps: {
        'data-scope': 'sidebar';
        'data-part': 'backdrop';
        onClick: () => void;
        'data-state': 'open' | 'closed';
    };
    mainProps: {
        onClick?: (event: React.MouseEvent) => void;
        /**
         * `true` while a modal (overlay) sidebar is open, removing the main content from the
         * tab order and hiding it from assistive technology.
         */
        inert?: boolean;
        /**
         * `true` while a modal (overlay) sidebar is open so AT skips the background content.
         */
        'aria-hidden'?: boolean;
    };
    backdropMotionProps: {
        visible: boolean;
    };
}
export type UseSidebarLayoutInstance = HeadlessInstance<UseSidebarLayoutProps, UseSidebarLayoutState, UseSidebarLayoutExposes>;
