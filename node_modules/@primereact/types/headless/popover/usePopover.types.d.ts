/**
 * The usePopover manages the state and functionality of a popover component.
 *
 * [Live Demo](https://www.primereact.org/popover/)
 *
 * @module usePopover
 * @group headless
 *
 */
import type { HeadlessInstance } from '@primereact/types/core';
import type { UseFocusTrapExposes } from '@primereact/types/headless/focustrap';
import * as React from 'react';
/**
 * Event fired when the popover's open state changes.
 */
export interface UsePopoverOpenChangeEvent {
    /**
     * The original browser event that triggered the change.
     */
    originalEvent?: Event;
    /**
     * The open value of the popover.
     */
    value: boolean | undefined;
}
/**
 * Defines valid properties in usePopover.
 */
export interface UsePopoverProps {
    /**
     * External anchor element for positioning the popover programmatically.
     * When provided, it is used instead of the trigger element.
     * @default undefined
     */
    anchor?: HTMLElement | null | undefined;
    /**
     * Whether the popover is open by default.
     * @default undefined
     */
    defaultOpen?: boolean | undefined;
    /**
     * Whether the popover is open.
     * @default undefined
     */
    open?: boolean | undefined;
    /**
     * When enabled, focus is trapped within the popover (modal behavior).
     * When disabled, focus leaving the popover closes it (non-modal behavior).
     * @default false
     */
    trapped?: boolean | undefined;
    /**
     * Whether to focus the first focusable element when the popover is opened.
     * @default true
     */
    autoFocus?: boolean | undefined;
    /**
     * Specifies if pressing escape key should hide the dialog.
     * @default true
     */
    closeOnEscape?: boolean | undefined;
    /**
     * Callback to invoke when the open state changes.
     * @default undefined
     */
    onOpenChange?: (event: UsePopoverOpenChangeEvent) => void;
    /**
     * Callback fired after the leave (exit) transition completes.
     * @default undefined
     */
    onExitComplete?: () => void;
    /**
     * DOM element or CSS selector to append the overlay to.
     * @default 'body'
     */
    appendTo?: 'body' | 'self' | HTMLElement | undefined;
}
/**
 * Defines valid state in usePopover.
 */
export interface UsePopoverState {
    /**
     * Whether the popover is open.
     */
    open: boolean | undefined;
    /**
     * Whether the popover positioner is rendered (stays true during exit animation).
     */
    rendered: boolean;
    /**
     * Whether focus is trapped within the popover.
     */
    trapped: boolean;
    /**
     * The anchor element.
     */
    anchorElement: HTMLElement | null;
    /**
     * The positioner element.
     */
    positionerElement: HTMLDivElement | null;
    /**
     * The popup element.
     */
    popupElement: HTMLElement | null;
    /**
     * The arrow element.
     */
    arrowElement: HTMLDivElement | null;
}
/**
 * Pre-built props for the popover trigger element.
 */
export interface UsePopoverTriggerProps {
    /**
     * Component scope identifier.
     */
    'data-scope': string;
    /**
     * Component part identifier.
     */
    'data-part': string;
    /**
     * Ref callback that registers the anchor element.
     */
    ref: (node: HTMLElement | null) => void;
    /**
     * Click handler that toggles the popover.
     */
    onClick: (e: React.MouseEvent) => void;
    /**
     * Data attribute when the positioner is open.
     */
    'data-positioner-open'?: string;
}
/**
 * Pre-built props for the popover popup element.
 */
export interface UsePopoverPopupProps {
    /**
     * Component scope identifier.
     */
    'data-scope': string;
    /**
     * Component part identifier.
     */
    'data-part': string;
    /**
     * Ref callback that registers the popup element.
     */
    ref: (node: HTMLElement | null) => void;
    /**
     * Tab index for programmatic focus support.
     */
    tabIndex: number;
    /**
     * Data attribute when the popup is open.
     */
    'data-open'?: string;
}
/**
 * Pre-built props for the popover close button.
 */
export interface UsePopoverCloseProps {
    /**
     * Component scope identifier.
     */
    'data-scope': string;
    /**
     * Component part identifier.
     */
    'data-part': string;
    /**
     * The button type attribute.
     */
    type: 'button';
    /**
     * Click handler that closes the popover.
     */
    onClick: (e: React.MouseEvent<HTMLButtonElement>) => void;
}
/**
 * Pre-built props for the popover positioner element.
 */
export interface UsePopoverPositionerProps {
    /**
     * Component scope identifier.
     */
    'data-scope': string;
    /**
     * Component part identifier.
     */
    'data-part': string;
    /**
     * Ref callback that registers the positioner element.
     */
    ref: (node: HTMLDivElement | null) => void;
}
/**
 * Pre-built props for the popover arrow element.
 */
export interface UsePopoverArrowProps {
    /**
     * Component scope identifier.
     */
    'data-scope': string;
    /**
     * Component part identifier.
     */
    'data-part': string;
    /**
     * Ref callback that registers the arrow element.
     */
    ref: (node: HTMLDivElement | null) => void;
}
/**
 * Defines the methods and properties exposed by usePopover.
 */
export interface UsePopoverExposes {
    /**
     * The state of the usePopover.
     */
    state: UsePopoverState;
    /**
     * Sets the open state.
     */
    setOpen: (open: boolean, originalEvent?: Event) => void;
    /**
     * Sets the arrow element.
     */
    setArrowRef: (node: HTMLDivElement | null) => void;
    /**
     * Sets the anchor element.
     */
    setAnchorRef: (node: HTMLElement | null) => void;
    /**
     * Sets the fallback anchor element, used when the primary anchor is not set.
     */
    setAnchorFallbackRef: (node: HTMLElement | null) => void;
    /**
     * Sets the positioner element.
     */
    setPositionerRef: (node: HTMLDivElement | null) => void;
    /**
     * Sets the popup element.
     */
    setPopupRef: (node: HTMLElement | null) => void;
    /**
     * Sets the rendered state of the positioner.
     */
    setRendered: (rendered: boolean) => void;
    /**
     * Pre-built props for the trigger element.
     */
    triggerProps: UsePopoverTriggerProps;
    /**
     * Pre-built props for the popup element.
     */
    popupProps: UsePopoverPopupProps;
    /**
     * Pre-built props for the close button.
     */
    closeProps: UsePopoverCloseProps;
    /**
     * Pre-built props for the positioner element.
     */
    positionerProps: UsePopoverPositionerProps;
    /**
     * Pre-built props for the arrow element.
     */
    arrowProps: UsePopoverArrowProps;
    /**
     * Focus trap exposes for sentinel rendering when trapped is true.
     */
    focusTrap: UseFocusTrapExposes;
    /**
     * Callback to invoke after the popover open animation completes.
     * Handles autoFocus behavior internally.
     */
    onOpenComplete: () => void;
    /**
     * Callback to invoke after the popover close animation completes.
     */
    onCloseComplete: () => void;
}
/**
 * Instance of usePopover headless.
 */
export type UsePopoverInstance = HeadlessInstance<UsePopoverProps, UsePopoverState, UsePopoverExposes>;
