/**
 *
 * The useTreeNode manages the state and functionality of a Tree node.
 *
 * [Live Demo](https://www.primereact.org/tree/)
 *
 * @module useTreeNode
 * @group headless
 *
 */
import type { HeadlessInstance } from '@primereact/types/core';
import * as React from 'react';
import type { TreeNodeData, UseTreeInstance, UseTreeMoveEvent, UseTreeNodeProps as UseTreeNodeElProps } from './useTree.types';
/**
 * Defines valid properties in useTreeNode hook.
 */
export interface UseTreeNodeHookProps {
    /**
     * The tree node data.
     */
    node?: TreeNodeData | undefined;
    /**
     * Index of the node among its siblings.
     */
    index?: number | undefined;
    /**
     * The parent tree instance providing state and methods.
     */
    context?: UseTreeInstance | undefined | null;
    /**
     * The parent tree node instance (if this is a nested node).
     */
    parentContext?: UseTreeNodeInstance | undefined | null;
    /**
     * Callback fired when a drag-and-drop move completes on this node.
     */
    onMove?: (event: UseTreeMoveEvent) => void;
}
/**
 * Defines valid state in useTreeNode.
 */
export interface UseTreeNodeState {
    /**
     * Whether the node is expanded.
     */
    expanded: boolean;
    /**
     * Whether the node is selected (single/multiple mode).
     */
    selected: boolean | undefined;
    /**
     * Whether the node is checked (checkbox mode).
     */
    checked: boolean;
    /**
     * Whether the node is partially checked (checkbox mode).
     */
    partialChecked: boolean;
    /**
     * Whether the node is a leaf node.
     */
    leaf: boolean;
    /**
     * Depth level of the node in the tree (1-based).
     */
    level: number;
    /**
     * Position of this node among its siblings (1-based).
     */
    posInSet: number;
    /**
     * Total number of siblings at this level.
     */
    setSize: number;
    /**
     * Whether this node is currently being dragged over (center zone).
     */
    isNodeDropActive: boolean;
    /**
     * Whether the previous drop point is active.
     */
    isPrevDropPointActive: boolean;
    /**
     * Whether the next drop point is active.
     */
    isNextDropPointActive: boolean;
    /**
     * Whether this node is draggable.
     */
    isDraggable: boolean;
    /**
     * Whether the node was touched (for mobile interactions).
     */
    nodeTouched: boolean;
}
/**
 * Defines the methods and properties exposed by useTreeNode.
 */
export interface UseTreeNodeExposes {
    /**
     * The computed state.
     */
    state: UseTreeNodeState;
    /**
     * The parent tree node instance (for nested traversal).
     */
    parentNode: UseTreeNodeInstance | undefined | null;
    /**
     * Pre-built props for the node root element (li).
     */
    nodeProps: UseTreeNodeElProps | undefined;
    /**
     * Pre-built props for the content element (div).
     */
    contentProps: UseTreeNodeContentElProps;
    /**
     * Pre-built props for drop point indicators.
     */
    dropPointProps: UseTreeNodeDropPointElProps;
}
/**
 * Pre-built props for the content element.
 */
export interface UseTreeNodeContentElProps {
    /**
     * Component scope identifier.
     */
    'data-scope': 'tree';
    /**
     * Component part identifier.
     */
    'data-part': 'content';
    /**
     * Whether this node is draggable.
     */
    draggable: boolean;
    /**
     * Present when the node is currently selected.
     */
    'data-selected'?: '' | undefined;
    /**
     * Present when the node is selectable.
     */
    'data-selectable'?: '' | undefined;
    /**
     * Present when the node is expanded (non-leaf only).
     */
    'data-expanded'?: '' | undefined;
    /**
     * Present when the node is collapsed (non-leaf only).
     */
    'data-collapsed'?: '' | undefined;
    /**
     * Present when the node is a leaf node.
     */
    'data-leaf'?: '' | undefined;
    /**
     * Present when the node is checked (checkbox mode).
     */
    'data-checked'?: '' | undefined;
    /**
     * Present when the node is partially checked (checkbox mode).
     */
    'data-partial-checked'?: '' | undefined;
    /**
     * Present when the node is disabled.
     */
    'data-disabled'?: '' | undefined;
    /**
     * Present when the node is being dragged over (drop target).
     */
    'data-dragover'?: '' | undefined;
    /**
     * Callback fired when a drag operation starts on this node.
     */
    onDragStart: (event: React.DragEvent) => void;
    /**
     * Callback fired when a dragged element is over this node.
     */
    onDragOver: (event: React.DragEvent) => void;
    /**
     * Callback fired when a dragged element enters this node.
     */
    onDragEnter: () => void;
    /**
     * Callback fired when a dragged element leaves this node.
     */
    onDragLeave: () => void;
    /**
     * Callback fired when a drag operation ends on this node.
     */
    onDragEnd: (event: React.DragEvent) => void;
    /**
     * Callback fired when a dragged element is dropped on this node.
     */
    onDrop: (event: React.DragEvent) => void;
    /**
     * Callback fired when this node is clicked.
     */
    onClick: (event: React.MouseEvent) => void;
    /**
     * Callback fired when a touch interaction ends on this node.
     */
    onTouchEnd: () => void;
}
/**
 * Pre-built props for drop point elements.
 */
export interface UseTreeNodeDropPointElProps {
    /**
     * Component scope identifier.
     */
    'data-scope': 'tree';
    /**
     * Component part identifier.
     */
    'data-part': 'drop-point';
    /**
     * Hides the drop point element from assistive technologies.
     */
    'aria-hidden': true;
}
/**
 * Instance of useTreeNode headless.
 */
export type UseTreeNodeInstance = HeadlessInstance<UseTreeNodeHookProps, UseTreeNodeState, UseTreeNodeExposes>;
