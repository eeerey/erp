/**
 *
 * The useTree manages the state and functionality of a tree component.
 *
 * [Live Demo](https://www.primereact.org/tree/)
 *
 * @module useTree
 * @group headless
 *
 */
import type { HeadlessInstance } from '@primereact/types/core';
import * as React from 'react';
/**
 * Defines a tree node structure.
 */
export interface TreeNodeData {
    /**
     * Mandatory unique key of the node.
     */
    key: string;
    /**
     * Label of the node.
     */
    label?: React.ReactNode;
    /**
     * Icon of the node.
     */
    icon?: string;
    /**
     * Children nodes.
     */
    children?: TreeNodeData[];
    /**
     * Whether the node is a leaf node (has no children).
     */
    leaf?: boolean;
    /**
     * Specifies the node loading.
     */
    loading?: boolean;
    /**
     * Whether the node is disabled. Disabled nodes cannot be selected, toggled, or interacted with.
     */
    disabled?: boolean;
    /**
     * Whether the node is draggable.
     */
    draggable?: boolean;
    /**
     * Whether the node is droppable.
     */
    droppable?: boolean;
    /**
     * Used to pass all properties of the HTMLLIElement.
     */
    nodeProps?: React.HTMLAttributes<HTMLLIElement>;
    /**
     * Optional properties
     */
    [key: string]: unknown;
}
/**
 * Defines the structure for expanded keys.
 * A record where the key is the node key and the value is true if expanded.
 */
export type TreeExpandedKeys = Record<string, boolean>;
/**
 * Defines the structure for selection keys in single/multiple mode.
 * A record where the key is the node key and the value is true if selected.
 */
export type TreeSelectionKeys = Record<string, boolean>;
/**
 * Defines the structure for selection keys in checkbox mode.
 * A record where the key is the node key and the value is an object containing checked and partialChecked states.
 */
export type TreeCheckboxSelectionKeys = Record<string, {
    checked: boolean;
    partialChecked: boolean;
}>;
/**
 * Memoized lookup entry kept in `nodeMap`. Provides O(1) access to position metadata.
 */
export interface TreeNodeEntry {
    /**
     * The node data.
     */
    node: TreeNodeData;
    /**
     * Key of the parent node, or null for root-level nodes.
     */
    parentKey: string | null;
    /**
     * 1-based depth in the tree.
     */
    level: number;
    /**
     * 1-based position within the parent's children list.
     */
    posInSet: number;
    /**
     * Total siblings under the same parent.
     */
    setSize: number;
}
/**
 * Event fired when the tree's expanded keys change.
 */
export interface UseTreeExpandedChangeEvent<E = React.SyntheticEvent> {
    /**
     * The original event that triggered the expansion change.
     */
    originalEvent: E;
    /**
     * The new expanded keys.
     */
    node: TreeExpandedKeys;
}
/**
 * Event fired when the tree's selected keys change.
 */
export interface UseTreeSelectionChangeEvent<E = React.SyntheticEvent> {
    /**
     * The original event that triggered the selection change.
     */
    originalEvent: E;
    /**
     * The new selected keys.
     */
    value: TreeSelectionKeys | TreeCheckboxSelectionKeys;
}
/**
 * Event fired when a node is toggled.
 */
export interface UseTreeToggleEvent {
    /**
     * The original event that triggered the toggle.
     */
    originalEvent: React.SyntheticEvent;
    /**
     * The toggled node.
     */
    node: TreeNodeData;
}
/**
 * Event fired when a node is expanded.
 */
export interface UseTreeExpandEvent {
    /**
     * The original event that triggered the toggle.
     */
    originalEvent: React.SyntheticEvent;
    /**
     * The expanded node.
     */
    node: TreeNodeData;
}
/**
 * Event fired when a node is collapsed.
 */
export interface UseTreeCollapseEvent {
    /**
     * The original event that triggered the collapse.
     */
    originalEvent: React.SyntheticEvent;
    /**
     * The collapsed node.
     */
    node: TreeNodeData;
}
/**
 * Event fired when a node is clicked.
 */
export interface UseTreeClickEvent {
    /**
     * The original event that triggered the click.
     */
    originalEvent: React.MouseEvent | React.KeyboardEvent;
    /**
     * The clicked node.
     */
    node: TreeNodeData;
}
/**
 * Event fired when a node is selected.
 */
export interface UseTreeSelectEvent {
    /**
     * The original event that triggered the selection.
     */
    originalEvent: React.MouseEvent | React.KeyboardEvent | React.SyntheticEvent;
    /**
     * The selected node.
     */
    node: TreeNodeData;
}
/**
 * Event fired when a node is unselected.
 */
export interface UseTreeUnselectEvent {
    /**
     * The original event that triggered the unselection.
     */
    originalEvent: React.MouseEvent | React.KeyboardEvent | React.SyntheticEvent;
    /**
     * The unselected node.
     */
    node: TreeNodeData;
}
/**
 * Drop position relative to the drop target node.
 */
export type TreeDropPosition = 'before' | 'inside' | 'after';
/**
 * Event fired when a drag-and-drop move completes. Replaces the legacy
 * `onNodeDrop` / `onDragEnter` / `onDragLeave` / drag-drop `onValueChange` quartet.
 */
export interface UseTreeMoveEvent {
    /**
     * The original drop event.
     */
    originalEvent: React.DragEvent;
    /**
     * The updated tree value after the move.
     */
    value: TreeNodeData[];
    /**
     * The node being moved.
     */
    dragNode: TreeNodeData;
    /**
     * The drop target node, or null if dropped at the root level.
     */
    dropNode: TreeNodeData | null;
    /**
     * The destination index within the parent's children array.
     */
    dropIndex: number;
    /**
     * Where in relation to `dropNode` the drop occurred.
     */
    dropPosition: TreeDropPosition;
}
/**
 * Event fired when the focused key changes (keyboard navigation).
 */
export interface UseTreeFocusedKeyChangeEvent {
    /**
     * The new focused key, or null when focus leaves the tree.
     */
    value: string | null;
}
/**
 * Defines valid properties in useTree.
 */
export interface UseTreeProps {
    /**
     * An array of treenodes.
     */
    value?: TreeNodeData[] | undefined;
    /**
     * A record of keys to represent the state of the tree expansion state in controlled mode.
     */
    expandedKeys?: TreeExpandedKeys | undefined;
    /**
     * The default expanded keys when used in uncontrolled mode.
     */
    defaultExpandedKeys?: TreeExpandedKeys | undefined;
    /**
     * A record of keys to represent the selection state in controlled mode.
     * For single/multiple mode: Record<string, boolean>
     * For checkbox mode: Record<string, { checked: boolean, partialChecked: boolean }>
     */
    selectionKeys?: TreeSelectionKeys | TreeCheckboxSelectionKeys | undefined;
    /**
     * The default selected keys when used in uncontrolled mode.
     */
    defaultSelectionKeys?: TreeSelectionKeys | TreeCheckboxSelectionKeys | undefined;
    /**
     * Defines the selection mode.
     */
    selectionMode?: 'single' | 'multiple' | 'checkbox' | undefined;
    /**
     * Defines how multiple items can be selected, when true metaKey needs to be pressed to select or unselect multiple items. When set to false selection of each item can be toggled individually.
     * @default false
     */
    metaKeySelection?: boolean;
    /**
     * Highlights automatically the first item.
     * @default false
     */
    highlightOnSelect?: boolean | undefined;
    /**
     * Whether the tree nodes can be dragged.
     */
    draggable?: boolean | undefined;
    /**
     * Whether the tree can accept dropped nodes.
     */
    droppable?: boolean | undefined;
    /**
     * Identifier(s) attached to nodes dragged out of this tree. Other trees use this
     * value to decide whether they accept the drop (see `droppableScope`).
     * Accepts a string or an array of strings.
     */
    draggableScope?: string | string[] | undefined;
    /**
     * Scope(s) this tree accepts drops from. A drop is allowed when the dragged
     * node's `draggableScope` overlaps with this value. Accepts a string or an
     * array of strings. When omitted, drops from any scope are accepted.
     */
    droppableScope?: string | string[] | undefined;
    /**
     * Callback fired when the tree's expanded keys change.
     */
    onExpandedChange?: (event: UseTreeExpandedChangeEvent) => void;
    /**
     * Callback fired when the tree's selection changes.
     */
    onSelectionChange?: (event: UseTreeSelectionChangeEvent) => void;
    /**
     * Callback fired when a node is toggled (expanded or collapsed).
     */
    onToggle?: (event: UseTreeToggleEvent) => void;
    /**
     * Callback fired when a node is expanded.
     */
    onExpand?: (event: UseTreeExpandEvent) => void;
    /**
     * Callback fired when a node is collapsed.
     */
    onCollapse?: (event: UseTreeCollapseEvent) => void;
    /**
     * Callback fired when a node is clicked.
     */
    onClick?: (event: UseTreeClickEvent) => void;
    /**
     * Callback fired when a node is selected.
     */
    onSelect?: (event: UseTreeSelectEvent) => void;
    /**
     * Callback fired when a node is unselected.
     */
    onUnselect?: (event: UseTreeUnselectEvent) => void;
    /**
     * Callback fired when a drag-and-drop move completes. Receives the updated tree value plus move metadata.
     */
    onMove?: (event: UseTreeMoveEvent) => void;
    /**
     * Optional validator invoked before a move is committed. Return `false` (sync or async) to cancel the move.
     */
    validateMove?: (event: UseTreeMoveEvent) => boolean | Promise<boolean>;
    /**
     * Callback fired when the focused key changes (keyboard navigation).
     */
    onFocusedKeyChange?: (event: UseTreeFocusedKeyChangeEvent) => void;
    /**
     * Displays a loading overlay over the tree. Pair with `Tree.Loading` to render
     * the spinner / message of your choice.
     */
    loading?: boolean;
}
/**
 * Defines valid state in useTree.
 */
export interface UseTreeState {
    /**
     * The current expanded keys of the tree.
     */
    expandedKeys: TreeExpandedKeys | undefined;
    /**
     * The current selected keys of the tree.
     */
    selectionKeys: TreeSelectionKeys | TreeCheckboxSelectionKeys | undefined;
    /**
     * The key of the currently focused node, or null when none.
     */
    focusedKey: string | null;
    /**
     * Whether the tree is in drag hover state.
     */
    dragHover: boolean;
}
/**
 * Selection query / mutation API exposed by useTree. Consumed by `Tree.Selection`.
 */
export interface UseTreeSelectionApi {
    /**
     * True when the given key is selected (or `checked === true` in checkbox mode).
     */
    isSelected: (key: string) => boolean;
    /**
     * True when the given key is in the tri-state "partial" position (checkbox mode only).
     */
    isPartiallySelected: (key: string) => boolean;
    /**
     * True when **all** visible nodes are selected. Aggregate (header) context.
     */
    isAllSelected: () => boolean;
    /**
     * True when **some** visible nodes (but not all) are selected. Aggregate (header) context.
     */
    isSomeSelected: () => boolean;
    /**
     * Toggle a single node's selection. `mode` picks the toggle behavior:
     * - `'checkbox'`: tri-state cascade.
     * - `'radio'`: select-only, never deselects on re-click.
     * - `'single'`: select / deselect on re-click.
     */
    toggleSelection: (event: React.SyntheticEvent, key: string, mode: 'checkbox' | 'radio' | 'single') => void;
    /**
     * Select or deselect every visible node.
     */
    toggleAll: (event: React.SyntheticEvent) => void;
}
/**
 * Defines the methods and properties exposed by useTree.
 */
export interface UseTreeExposes {
    /**
     * The state of the useTree.
     */
    state: UseTreeState;
    /**
     * Map of every node keyed by `node.key`, carrying parent / level / posInSet / setSize. O(1) lookup.
     */
    nodeMap: Map<string, TreeNodeEntry>;
    /**
     * All node keys in depth-first order, regardless of expansion.
     */
    flatOrder: string[];
    /**
     * Keys of nodes whose ancestors are all expanded (i.e. currently visible in the DOM).
     */
    visibleKeys: string[];
    /**
     * Convenience: `visibleKeys.length`.
     */
    visibleCount: number;
    /**
     * Look up a node by key in O(1).
     */
    findNodeByKey: (key: string) => TreeNodeData | undefined;
    /**
     * Look up a node's position metadata by key.
     */
    findNodeInfo: (key: string) => {
        level: number;
        posInSet: number;
        setSize: number;
        parentKey: string | null;
    };
    /**
     * Gets the list of tree nodes (the original `props.value`).
     */
    getNodes: () => TreeNodeData[];
    /**
     * Set the focused key explicitly (e.g. from a click handler).
     */
    setFocusedKey: (key: string | null) => void;
    /**
     * Replace the entire selection map.
     */
    setSelectionKeys: (keys: TreeSelectionKeys | TreeCheckboxSelectionKeys, event?: React.SyntheticEvent) => void;
    /**
     * Handles keyboard navigation on tree nodes.
     */
    onKeyDown: (event: React.KeyboardEvent, node: TreeNodeData, level: number, expanded: boolean, leaf: boolean) => void;
    /**
     * Toggles the expansion state of a node.
     */
    onNodeToggle: (event: React.SyntheticEvent, node: TreeNodeData) => void;
    /**
     * Handles node click event.
     */
    onNodeClick: (event: React.MouseEvent | React.KeyboardEvent, node: TreeNodeData, nodeTouched?: boolean) => void;
    /**
     * Handles content click event. Automatically delegates to checkbox or node click based on selection mode.
     */
    onClick: (event: React.MouseEvent | React.KeyboardEvent, node: TreeNodeData, nodeTouched?: boolean) => void;
    /**
     * Handles checkbox state change for checkbox selection mode.
     */
    onCheckboxChange: (event: React.SyntheticEvent, node: TreeNodeData) => void;
    /**
     * Checks if a node is selected.
     */
    isNodeSelected: (node: TreeNodeData) => boolean;
    /**
     * Selection query API consumed by `Tree.Selection`. Mirrors the DataTable selection contract.
     */
    selection: UseTreeSelectionApi;
    /**
     * Checks if a node can be dropped based on drag-drop rules.
     */
    allowDrop: (node: TreeNodeData) => boolean;
    /**
     * Returns pre-built props for a tree toggle button element.
     */
    getToggleProps: (node: TreeNodeData) => UseTreeToggleProps;
    /**
     * Returns pre-built props for the leaf-row toggle spacer element.
     */
    getToggleSpacerProps: () => UseTreeToggleSpacerProps;
    /**
     * Returns pre-built props for a toggle indicator element.
     */
    getToggleIndicatorProps: () => UseTreeToggleIndicatorProps;
    /**
     * Returns pre-built props for the tree root element.
     */
    rootProps: UseTreeRootProps;
    /**
     * Returns pre-built props for a tree node element.
     */
    getNodeProps: (node: TreeNodeData, level: number, expanded: boolean, checked: boolean, selected: boolean, leaf: boolean, posInSet: number, setSize: number) => UseTreeNodeProps;
    /**
     * Returns pre-built props for the tree filter element.
     */
    filterProps: UseTreeFilterProps;
    /**
     * Returns pre-built props for the tree loading status element.
     */
    loadingProps: UseTreeLoadingProps;
    /**
     * Returns pre-built props for the flat iterator element.
     */
    getNodesProps: () => UseTreeNodesProps;
}
/**
 * Pre-built props for a tree toggle button element.
 */
export interface UseTreeToggleProps {
    /**
     * Component scope identifier.
     */
    'data-scope': 'tree';
    /**
     * Component part identifier.
     */
    'data-part': 'toggle';
    /**
     * Present when the associated node is expanded.
     */
    'data-expanded'?: '' | undefined;
    /**
     * Present when the associated node is collapsed.
     */
    'data-collapsed'?: '' | undefined;
    /**
     * The type of the button.
     */
    type: 'button';
    /**
     * The tab index of the button.
     */
    tabIndex: -1;
    /**
     * Click handler that stops propagation and toggles the node.
     */
    onClick: (event: React.MouseEvent<HTMLButtonElement>) => void;
}
/**
 * Pre-built props for the leaf-row toggle spacer element.
 */
export interface UseTreeToggleSpacerProps {
    /**
     * Component scope identifier.
     */
    'data-scope': 'tree';
    /**
     * Component part identifier.
     */
    'data-part': 'toggle-spacer';
    /**
     * Hidden from the accessibility tree; the spacer is purely visual alignment.
     */
    'aria-hidden': true;
}
/**
 * Pre-built props for a toggle indicator element.
 */
export interface UseTreeToggleIndicatorProps {
    /**
     * Component scope identifier.
     */
    'data-scope': 'tree';
    /**
     * Component part identifier.
     */
    'data-part': 'toggle-indicator';
    /**
     * Hidden from the accessibility tree; the indicator is purely decorative.
     */
    'aria-hidden': true;
}
/**
 * Pre-built props for the tree root element.
 */
export interface UseTreeRootProps {
    /**
     * Unique identifier for the root element.
     */
    id: string | undefined;
    /**
     * Component scope identifier.
     */
    'data-scope': 'tree';
    /**
     * Component part identifier.
     */
    'data-part': 'root';
    /**
     * Present when the tree root is the active drop target during a drag operation.
     */
    'data-dragover'?: '' | undefined;
    /**
     * Drag over event handler.
     */
    onDragOver: (event: React.DragEvent) => void;
    /**
     * Drag enter event handler.
     */
    onDragEnter: (event: React.DragEvent) => void;
    /**
     * Drag leave event handler.
     */
    onDragLeave: (event: React.DragEvent) => void;
    /**
     * Drop event handler.
     */
    onDrop: (event: React.DragEvent) => void;
}
/**
 * Pre-built props for a tree node element (li).
 */
export interface UseTreeNodeProps {
    /**
     * ARIA role for the node element.
     */
    role: 'treeitem';
    /**
     * Tab index for keyboard navigation.
     */
    tabIndex: number;
    /**
     * ARIA level of the node in the tree hierarchy.
     */
    'aria-level': number;
    /**
     * Total number of nodes at the same level for ARIA.
     */
    'aria-setsize': number;
    /**
     * Position of the node in its set for ARIA.
     */
    'aria-posinset': number;
    /**
     * Whether the node is expanded. Omitted for leaf nodes.
     */
    'aria-expanded': boolean | undefined;
    /**
     * Whether the node is selected (single/multiple selection mode).
     */
    'aria-selected': boolean | undefined;
    /**
     * Whether the node is checked (checkbox selection mode); `'mixed'` for partial.
     */
    'aria-checked': boolean | 'mixed' | undefined;
    /**
     * Component scope identifier.
     */
    'data-scope': 'tree';
    /**
     * Component part identifier.
     */
    'data-part': 'node';
    /**
     * The key of the node for identification.
     */
    'data-node-key': string | undefined;
    /**
     * Present when the node is expanded.
     */
    'data-expanded'?: '' | undefined;
    /**
     * Present when the node is collapsed.
     */
    'data-collapsed'?: '' | undefined;
    /**
     * Present when the node is selected.
     */
    'data-selected'?: '' | undefined;
    /**
     * Present when the node is a leaf node.
     */
    'data-leaf'?: '' | undefined;
    /**
     * Present when the node is disabled.
     */
    'data-disabled'?: '' | undefined;
    /**
     * Present when the node is the currently focused row (keyboard navigation).
     */
    'data-focused'?: '' | undefined;
    /**
     * Keyboard event handler for the node.
     */
    onKeyDown: (event: React.KeyboardEvent) => void;
}
/**
 * Pre-built props for the tree filter element.
 */
export interface UseTreeFilterProps {
    /**
     * Component scope identifier.
     */
    'data-scope': 'tree';
    /**
     * Component part identifier.
     */
    'data-part': 'filter';
    /**
     * Autocomplete attribute for the filter input.
     */
    autoComplete: 'off';
    /**
     * Key up event handler for the filter.
     */
    onKeyUp: (event: React.KeyboardEvent) => void;
}
/**
 * Pre-built props for the tree loading status element.
 */
export interface UseTreeLoadingProps {
    /**
     * Component scope identifier.
     */
    'data-scope': 'tree';
    /**
     * Component part identifier.
     */
    'data-part': 'loading';
    /**
     * Announces the loading region to assistive technology.
     */
    role: 'status';
    /**
     * Politeness level for the live region updates.
     */
    'aria-live': 'polite';
}
/**
 * Pre-built props for the flat iterator element (`<ul role="tree">`).
 */
export interface UseTreeNodesProps {
    /**
     * Component scope identifier.
     */
    'data-scope': 'tree';
    /**
     * Component part identifier.
     */
    'data-part': 'nodes';
    /**
     * ARIA role.
     */
    role: 'tree';
}
/**
 * Instance of useTree headless.
 */
export type UseTreeInstance = HeadlessInstance<UseTreeProps, UseTreeState, UseTreeExposes>;
