/**
 *
 * The usePickList manages the state and functionality of a PickList component.
 *
 * [Live Demo](https://www.primereact.org/picklist/)
 *
 * @module usePickList
 * @group headless
 *
 */
import type { HeadlessInstance } from '@primereact/types/core';
/**
 * Event fired when the picklist value changes.
 */
export interface UsePickListValueChangeEvent {
    /**
     * The original event that triggered the change.
     */
    originalEvent?: React.SyntheticEvent | Event;
    /**
     * Updated source list after the change.
     */
    source: unknown[];
    /**
     * Updated target list after the change.
     */
    target: unknown[];
}
/**
 * Event fired when the picklist selection changes.
 */
export interface UsePickListSelectionChangeEvent {
    /**
     * The original event that triggered the change.
     */
    originalEvent?: React.SyntheticEvent;
    /**
     * Updated selection array after the change.
     */
    value: unknown[];
}
/**
 * Defines valid properties in usePickList.
 */
export interface UsePickListProps<T = unknown> {
    /**
     * An array of objects for the source list.
     */
    source?: T[];
    /**
     * An array of objects for the target list.
     */
    target?: T[];
    /**
     * An array of selected items across source and target lists.
     */
    selection?: T[];
    /**
     * When present, it specifies that the component should be disabled.
     * @default false
     */
    disabled?: boolean;
    /**
     * When present, items can be reordered via drag and drop.
     * @default false
     */
    draggable?: boolean;
    /**
     * Controls the placeholder element left behind during drag.
     * - `"clone"` creates a visual copy of the dragged item.
     * - `"empty"` creates an empty element that only preserves the height.
     * @default "empty"
     */
    placeholder?: 'clone' | 'empty';
    /**
     * Callback fired when items are moved between source and target lists.
     * @param event The event that triggered the change.
     * @param event.originalEvent The original event that triggered the change.
     * @param event.source Updated source list.
     * @param event.target Updated target list.
     * @returns void
     */
    onValueChange?: (event: UsePickListValueChangeEvent) => void;
    /**
     * Callback fired when the selection changes.
     * @param event The event that triggered the change.
     * @param event.originalEvent The original event that triggered the change.
     * @param event.value Updated selection array.
     * @returns void
     */
    onSelectionChange?: (event: UsePickListSelectionChangeEvent) => void;
}
/**
 * Defines valid state in usePickList.
 */
export interface UsePickListState<T = unknown> {
    /**
     * Current source list.
     */
    source: T[];
    /**
     * Current target list.
     */
    target: T[];
    /**
     * Currently selected items across both lists.
     */
    selection: T[];
    /**
     * Whether a drag operation is currently in progress.
     */
    dragging: boolean;
}
/**
 * Defines the methods and properties exposed by usePickList.
 */
export interface UsePickListExposes<T = unknown> {
    /**
     * The state of the usePickList.
     */
    state: UsePickListState<T>;
    /**
     * Pre-built props for a list item element.
     * @param item The item data.
     * @param index Index of the item in the list.
     * @param side Whether the item belongs to the source or target list.
     */
    getOptionProps: (item: unknown, index: number, side: 'source' | 'target') => Record<string, unknown>;
    /**
     * Moves selected items from source to target.
     * @param event The event that triggered the action.
     */
    moveToTarget: (event?: React.SyntheticEvent) => void;
    /**
     * Moves selected items from target to source.
     * @param event The event that triggered the action.
     */
    moveToSource: (event?: React.SyntheticEvent) => void;
    /**
     * Moves all items from source to target.
     * @param event The event that triggered the action.
     */
    moveAllToTarget: (event?: React.SyntheticEvent) => void;
    /**
     * Moves all items from target to source.
     * @param event The event that triggered the action.
     */
    moveAllToSource: (event?: React.SyntheticEvent) => void;
    /**
     * Pre-built data attribute props for the root element.
     */
    rootProps: Record<string, unknown>;
    /**
     * Pre-built data attribute props for the source list element.
     */
    sourceListProps: Record<string, unknown>;
    /**
     * Pre-built data attribute props for the target list element.
     */
    targetListProps: Record<string, unknown>;
    /**
     * Pre-built data attribute props for the source controls container element.
     */
    sourceControlsProps: Record<string, unknown>;
    /**
     * Pre-built data attribute props for the target controls container element.
     */
    targetControlsProps: Record<string, unknown>;
    /**
     * Pre-built props for the "move to target" button element.
     */
    moveToTargetProps: Record<string, unknown>;
    /**
     * Pre-built props for the "move all to target" button element.
     */
    moveAllToTargetProps: Record<string, unknown>;
    /**
     * Pre-built props for the "move to source" button element.
     */
    moveToSourceProps: Record<string, unknown>;
    /**
     * Pre-built props for the "move all to source" button element.
     */
    moveAllToSourceProps: Record<string, unknown>;
    /**
     * Pre-built props for the source list "move previous" button element.
     */
    sourcePrevProps: Record<string, unknown>;
    /**
     * Pre-built props for the source list "move next" button element.
     */
    sourceNextProps: Record<string, unknown>;
    /**
     * Pre-built props for the source list "move first" button element.
     */
    sourceFirstProps: Record<string, unknown>;
    /**
     * Pre-built props for the source list "move last" button element.
     */
    sourceLastProps: Record<string, unknown>;
    /**
     * Pre-built props for the target list "move previous" button element.
     */
    targetPrevProps: Record<string, unknown>;
    /**
     * Pre-built props for the target list "move next" button element.
     */
    targetNextProps: Record<string, unknown>;
    /**
     * Pre-built props for the target list "move first" button element.
     */
    targetFirstProps: Record<string, unknown>;
    /**
     * Pre-built props for the target list "move last" button element.
     */
    targetLastProps: Record<string, unknown>;
}
/**
 * Instance of usePickList headless.
 */
export type UsePickListInstance<T = unknown> = HeadlessInstance<UsePickListProps<T>, UsePickListState<T>, UsePickListExposes<T>>;
