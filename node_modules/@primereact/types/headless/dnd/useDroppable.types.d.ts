/**
 *
 * The useDroppable hook registers an element as a drop target.
 *
 * @module useDroppable
 * @group headless
 *
 */
import type { HeadlessInstance } from '@primereact/types/core';
import type { DndInstance, DragData, UniqueId } from '@primereact/types/primitive/dnd';
/**
 * Defines valid properties in useDroppable.
 */
export interface UseDroppableProps {
    /**
     * The DndInstance to register with.
     */
    dnd?: DndInstance;
    /**
     * Unique identifier for this droppable.
     */
    id?: UniqueId;
    /**
     * Whether the droppable is disabled.
     * @default false
     */
    disabled?: boolean;
    /**
     * Accepted scopes for incoming draggables.
     */
    accept?: string[];
    /**
     * Data attached to this droppable.
     */
    data?: DragData;
    /**
     * Custom predicate to determine if a draggable can be dropped here.
     */
    canDrop?: (activeData: DragData) => boolean;
}
/**
 * Defines valid state in useDroppable.
 */
export interface UseDroppableState {
    /**
     * Whether a draggable is currently over this droppable.
     */
    isOver: boolean;
}
/**
 * Data attribute props for the droppable element.
 */
export interface UseDroppableRootProps {
    'data-scope': string;
    'data-part': string;
    'data-over'?: string;
    'data-disabled'?: string;
}
/**
 * Defines the methods and properties exposed by useDroppable.
 */
export interface UseDroppableExposes {
    /**
     * The reactive droppable state.
     */
    state: UseDroppableState;
    /**
     * Pre-built data attribute props for the droppable element.
     */
    rootProps: UseDroppableRootProps;
}
/**
 * Instance of useDroppable headless.
 */
export type UseDroppableInstance = HeadlessInstance<UseDroppableProps, UseDroppableState, UseDroppableExposes>;
