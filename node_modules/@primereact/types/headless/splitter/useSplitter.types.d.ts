/**
 *
 * The useSplitter manages the functionality of a splitter component.
 *
 * [Live Demo](https://www.primereact.org/splitter/)
 *
 * @module useSplitter
 * @group headless
 *
 */
import type { HeadlessInstance } from '@primereact/types/core';
import type * as React from 'react';
/**
 * Information about a registered panel in the splitter.
 */
export interface UseSplitterPanelInfo {
    /**
     * The unique identifier of the panel element.
     */
    id: string | undefined;
    /**
     * The index of the panel.
     */
    index: number;
    /**
     * Minimum size of the panel relative to 100%.
     */
    minSize: number;
    /**
     * Maximum size of the panel relative to 100%.
     */
    maxSize: number;
    /**
     * Whether the panel can be collapsed.
     */
    collapsible: boolean;
    /**
     * Size of the panel when collapsed, relative to 100%.
     */
    collapsedSize: number;
    /**
     * Whether the panel is currently collapsed.
     */
    collapsed: boolean;
}
/**
 * Props accepted by registerPanel to capture panel configuration.
 */
export interface UseSplitterRegisterPanelProps {
    /**
     * The unique identifier of the panel element.
     */
    id?: string | undefined;
    /**
     * Minimum size of the panel relative to 100%.
     */
    minSize?: number;
    /**
     * Maximum size of the panel relative to 100%.
     */
    maxSize?: number;
    /**
     * Whether the panel can be collapsed when resized below minSize.
     */
    collapsible?: boolean;
    /**
     * Size of the panel when collapsed, relative to 100%.
     */
    collapsedSize?: number;
}
/**
 * Event data passed to splitter resize callbacks.
 */
export interface UseSplitterResizeEvent {
    /**
     * Array of panel sizes as percentages.
     */
    sizes: number[];
}
/**
 * Event data passed to splitter collapse callbacks.
 */
export interface UseSplitterCollapseEvent {
    /**
     * Index of the panel that collapsed or expanded.
     */
    index: number;
    /**
     * Whether the panel is now collapsed.
     */
    collapsed: boolean;
    /**
     * Array of panel sizes as percentages.
     */
    sizes: number[];
}
/**
 * Declarative panel configuration for headless usage.
 * When provided via the `panels` prop, eliminates the need for `registerPanel`.
 */
export interface UseSplitterPanelConfig {
    /**
     * Minimum size of the panel relative to 100%.
     * @default 0
     */
    minSize?: number;
    /**
     * Maximum size of the panel relative to 100%.
     * @default 100
     */
    maxSize?: number;
    /**
     * Whether the panel can be collapsed when resized below minSize.
     * @default false
     */
    collapsible?: boolean;
    /**
     * Size of the panel when collapsed, relative to 100%.
     * @default 0
     */
    collapsedSize?: number;
}
/**
 * Defines valid properties in useSplitter.
 */
export interface UseSplitterProps {
    /**
     * Declarative panel configuration array. When provided, the hook auto-registers
     * panels from this config — no `registerPanel` calls needed.
     * Each entry defines constraints for the panel at that index.
     */
    panels?: UseSplitterPanelConfig[] | undefined;
    /**
     * Controlled panel sizes as percentages (must sum to ~100).
     * When provided, the splitter operates in controlled mode — sizes are driven by this prop
     * and should be updated via onResize or onResizeEnd callbacks.
     */
    sizes?: number[] | undefined;
    /**
     * Default initial panel sizes as percentages (must sum to ~100).
     * Used for uncontrolled mode — sets the initial sizes on mount, after which internal state takes over.
     */
    defaultSizes?: number[] | undefined;
    /**
     * Orientation of the panels.
     * @default horizontal
     */
    orientation?: 'horizontal' | 'vertical' | undefined;
    /**
     * When true, disables all resize interactions.
     * @default false
     */
    disabled?: boolean | undefined;
    /**
     * Step factor to increment/decrement the size of the panels while pressing the arrow keys.
     * @default 5
     */
    step?: number | undefined;
    /**
     * Callback invoked when resize starts.
     * @param {UseSplitterResizeEvent} event - Custom resize start event.
     * @param {number[]} event.sizes - Array of panel sizes as percentages.
     */
    onResizeStart?: ((event: UseSplitterResizeEvent) => void) | undefined;
    /**
     * Callback invoked during resize with current panel sizes.
     * @param {UseSplitterResizeEvent} event - Custom resize event.
     * @param {number[]} event.sizes - Array of panel sizes as percentages.
     */
    onResize?: ((event: UseSplitterResizeEvent) => void) | undefined;
    /**
     * Callback invoked when resize ends.
     * @param {UseSplitterResizeEvent} event - Custom resize end event.
     * @param {number[]} event.sizes - Array of panel sizes as percentages.
     */
    onResizeEnd?: ((event: UseSplitterResizeEvent) => void) | undefined;
    /**
     * Callback invoked when a panel collapses or expands.
     * @param {UseSplitterCollapseEvent} event - Custom collapse event.
     * @param {number} event.index - Index of the panel that collapsed or expanded.
     * @param {boolean} event.collapsed - Whether the panel is now collapsed.
     * @param {number[]} event.sizes - Array of panel sizes as percentages.
     */
    onCollapse?: ((event: UseSplitterCollapseEvent) => void) | undefined;
}
/**
 * Defines valid state in useSplitter.
 */
export interface UseSplitterState {
    /**
     * Whether the splitter is currently being resized.
     */
    resizing: boolean;
}
/**
 * Defines the methods and properties exposed by useSplitter.
 */
export interface UseSplitterExposes {
    /**
     * The state of the useSplitter.
     */
    state: UseSplitterState;
    /**
     * Registers a new panel or updates an existing one.
     * @param {number | null} index - The panel index. Pass null for first registration.
     * @param {UseSplitterRegisterPanelProps} panelProps - The panel configuration.
     * @returns {number} The assigned panel index.
     */
    registerPanel: (index: number | null, panelProps: UseSplitterRegisterPanelProps) => number;
    /**
     * Counter tracking the number of registered panels.
     */
    panelCounter: React.RefObject<number | null>;
    /**
     * Registry of panel info objects.
     */
    panelRegistry: React.RefObject<UseSplitterPanelInfo[]>;
    /**
     * Array storing the current size of each panel as percentages.
     */
    panelSizes: number[] | undefined;
    /**
     * Handler for resize start events.
     * @param {React.PointerEvent | KeyboardEvent} event - The triggering event.
     * @param {number} index - The gutter index.
     * @param {boolean} [isKeyDown] - Whether triggered by keyboard.
     */
    onResizeStart: (event: React.PointerEvent | KeyboardEvent, index: number, isKeyDown?: boolean) => void;
    /**
     * Handler for resize events.
     * @param {React.PointerEvent | KeyboardEvent} event - The triggering event.
     * @param {number} [step] - Step size for keyboard resize.
     * @param {boolean} [isKeyDown] - Whether triggered by keyboard.
     */
    onResize: (event: React.PointerEvent | KeyboardEvent, step?: number, isKeyDown?: boolean) => void;
    /**
     * Handler for resize end events.
     */
    onResizeEnd: () => void;
    /**
     * Handler for pointer down events on gutters.
     * @param {React.PointerEvent} event - The pointer event.
     * @param {number} index - The gutter index.
     * @param {boolean} [gutterDisabled] - Whether this specific gutter is disabled.
     */
    onGutterPointerDown: (event: React.PointerEvent, index: number, gutterDisabled?: boolean) => void;
    /**
     * Handler for key up events on gutters.
     */
    onGutterKeyUp: () => void;
    /**
     * Handler for key down events on gutters.
     * @param {React.KeyboardEvent} event - The keyboard event.
     * @param {number} index - The gutter index.
     * @param {boolean} [gutterDisabled] - Whether this specific gutter is disabled.
     */
    onGutterKeyDown: (event: React.KeyboardEvent, index: number, gutterDisabled?: boolean) => void;
    /**
     * Registers a new gutter and returns its index.
     * Used internally by compound components. Guard with a ref to prevent double-registration in StrictMode.
     * @returns {number} The assigned gutter index.
     * @internal
     */
    registerGutter: () => number;
    /**
     * Returns pre-built props for a gutter element.
     * @param {number} index - The gutter index (0-based, corresponds to the gap between panel[index] and panel[index+1]).
     * @param {object} [options] - Optional overrides.
     * @param {string} [options.id] - The gutter element id.
     * @param {boolean} [options.disabled] - Whether this specific gutter is disabled.
     * @returns {UseSplitterGutterProps} The gutter element props.
     */
    getGutterProps: (index: number, options?: {
        id?: string;
        disabled?: boolean;
    }) => UseSplitterGutterProps;
    /**
     * Pre-built data attribute props for the root element.
     */
    rootProps: UseSplitterRootProps;
    /**
     * Returns pre-built data attribute and style props for a panel element.
     * Flex grow is computed internally from panelSizes.
     * @param {number} index - The panel index.
     * @returns {UseSplitterPanelProps} The panel element props.
     */
    getPanelProps: (index: number) => UseSplitterPanelProps;
    /**
     * Pre-built data attribute props for the handle element.
     */
    handleProps: UseSplitterHandleProps;
}
/**
 * Pre-built data attribute props for the splitter root element.
 */
export interface UseSplitterRootProps {
    /**
     * Ref callback for the root element.
     */
    ref: React.RefObject<HTMLDivElement>;
    /**
     * Component scope identifier.
     */
    'data-scope': string;
    /**
     * Component part identifier.
     */
    'data-part': string;
    /**
     * Data attribute for orientation.
     */
    'data-orientation'?: 'horizontal' | 'vertical';
    /**
     * Present when the splitter is being resized.
     */
    'data-resizing'?: '';
    /**
     * Present when the splitter is disabled.
     */
    'data-disabled'?: '';
}
/**
 * Pre-built data attribute props for a splitter panel element.
 */
export interface UseSplitterPanelProps {
    /**
     * Component scope identifier.
     */
    'data-scope': string;
    /**
     * Component part identifier.
     */
    'data-part': string;
    /**
     * Data attribute for orientation.
     */
    'data-orientation'?: 'horizontal' | 'vertical';
    /**
     * Data attribute for panel index.
     */
    'data-index': number;
    /**
     * Inline style containing the flex grow value.
     */
    style: React.CSSProperties;
    /**
     * Present when the splitter is being resized.
     */
    'data-resizing'?: '';
}
/**
 * Pre-built data attribute props for a splitter handle element.
 */
export interface UseSplitterHandleProps {
    /**
     * Component scope identifier.
     */
    'data-scope': string;
    /**
     * Component part identifier.
     */
    'data-part': string;
    /**
     * Data attribute for orientation.
     */
    'data-orientation'?: 'horizontal' | 'vertical';
    /**
     * Present when the splitter is being resized.
     */
    'data-resizing'?: '';
}
/**
 * Pre-built props for a splitter gutter element.
 */
export interface UseSplitterGutterProps {
    /**
     * Component scope identifier.
     */
    'data-scope': string;
    /**
     * Component part identifier.
     */
    'data-part': string;
    /**
     * Optional element id.
     */
    id?: string;
    /**
     * ARIA role for the gutter separator.
     */
    role: 'separator';
    /**
     * Tab index (0 when active, -1 when disabled).
     */
    tabIndex: number;
    /**
     * Current panel size as a percentage.
     */
    'aria-valuenow': number;
    /**
     * Minimum panel size as a percentage.
     */
    'aria-valuemin': number;
    /**
     * Maximum panel size as a percentage.
     */
    'aria-valuemax': number;
    /**
     * Space-separated panel ids this gutter controls.
     */
    'aria-controls'?: string;
    /**
     * Whether the gutter is disabled.
     */
    'aria-disabled': boolean | undefined;
    /**
     * Base layout styles for the gutter element.
     */
    style: React.CSSProperties;
    /**
     * Data attribute for orientation.
     */
    'data-orientation'?: 'horizontal' | 'vertical';
    /**
     * Present when the splitter is being resized.
     */
    'data-resizing'?: '';
    /**
     * Present when the gutter is disabled.
     */
    'data-disabled'?: '';
    /**
     * Pointer down handler.
     */
    onPointerDown: (event: React.PointerEvent) => void;
    /**
     * Key down handler.
     */
    onKeyDown: (event: React.KeyboardEvent) => void;
    /**
     * Key up handler.
     */
    onKeyUp: () => void;
}
/**
 * Instance of useSplitter headless.
 */
export type UseSplitterInstance = HeadlessInstance<UseSplitterProps, UseSplitterState, UseSplitterExposes>;
