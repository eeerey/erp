/**
 *
 * The useTabs manages the state and functionality of a tabs component.
 *
 * [Live Demo](https://www.primereact.org/tabs/)
 *
 * @module useTabs
 * @group headless
 *
 */
import type { HeadlessInstance } from '@primereact/types/core';
/**
 * Event fired when the tabs's checked state changes.
 */
export interface UseTabsChangeEvent {
    /**
     * The value of the tabs.
     */
    value: string | number | undefined;
}
/**
 * Defines valid properties in useTabs.
 */
export interface UseTabsProps {
    /**
     * Value of the active tab. Use for controlled mode.
     * @default undefined
     */
    value?: string | number | undefined;
    /**
     * Default value of the active tab. Use for uncontrolled mode.
     * @default undefined
     */
    defaultValue?: string | number | undefined;
    /**
     * When enabled, the focused tab is activated.
     * @default false
     */
    selectOnFocus?: boolean;
    /**
     * Defines how the active tab is scrolled into view when it changes.
     * - `'nearest'`: scrolls only if the tab is clipped or too close to an edge, with padding.
     * - `'center'`: always centers the active tab in the viewport.
     * - `false`: disables auto-scrolling.
     * - `function`: a custom scroll function receiving the content element and active tab element.
     * @default 'nearest'
     */
    scrollStrategy?: 'nearest' | 'center' | false | ((content: HTMLElement, tab: HTMLElement) => void);
    /**
     * The tabIndex of the active tab.
     * @default 0
     */
    tabIndex?: number;
    /**
     * Callback fired when the tabs's value changes.
     * @param event The event that triggered the change.
     * @param event.value The value of the tabs.
     * @returns void
     */
    onValueChange?: (event: UseTabsChangeEvent) => void;
}
/**
 * Defines valid state in useTabs.
 */
export interface UseTabsState {
    /**
     * Value of the active tab or an array of values in multiple mode.
     */
    activeTab: string | number | undefined;
    /**
     * Whether the previous scroll button should be enabled.
     */
    canScrollPrev: boolean;
    /**
     * Whether the next scroll button should be enabled.
     */
    canScrollNext: boolean;
}
/**
 * Props returned by getTabProps for a tab element.
 */
export interface UseTabsTabProps {
    /**
     * The role of the tab element.
     */
    role: 'tab';
    /**
     * The tabIndex of the tab element.
     */
    tabIndex: number;
    /**
     * Ref for the active tab element.
     */
    ref: React.RefObject<HTMLButtonElement> | undefined;
    /**
     * Whether the tab is disabled.
     */
    disabled: boolean | undefined;
    /**
     * Whether the tab is selected.
     */
    'aria-selected': boolean;
    /**
     * Whether the tab is disabled (aria).
     */
    'aria-disabled': boolean | undefined;
    /**
     * The id of the tabpanel controlled by this tab.
     */
    'aria-controls': string;
    /**
     * Present when the tab is active.
     */
    'data-active'?: '';
    /**
     * Present when the tab is disabled.
     */
    'data-disabled'?: '';
    /**
     * Present when the tab list can scroll to the previous page.
     */
    'data-can-scroll-prev'?: '';
    /**
     * Present when the tab list can scroll to the next page.
     */
    'data-can-scroll-next'?: '';
    /**
     * Click event handler for the tab.
     */
    onClick: (event: React.MouseEvent<HTMLButtonElement>) => void;
    /**
     * Focus event handler for the tab.
     */
    onFocus: (event: React.FocusEvent<HTMLButtonElement>) => void;
    /**
     * Keyboard event handler for the tab.
     */
    onKeyDown: (event: React.KeyboardEvent<HTMLButtonElement>) => void;
}
/**
 * Props returned by getIndicatorProps for the active tab indicator element.
 */
export interface UseTabsIndicatorProps {
    /**
     * Ref for the active bar element.
     */
    ref: React.RefObject<HTMLSpanElement>;
    /**
     * CSS custom properties for positioning the indicator.
     */
    style: React.CSSProperties;
}
/**
 * Defines the methods and properties exposed by useTabs.
 */
export interface UseTabsExposes {
    /**
     * The method to update the value of the active tab.
     * @param value The value of the active tab.
     * @returns void
     */
    updateValue: (value: string | number | undefined) => void;
    /**
     * The method to check if the tab is active.
     * @param key The key of the tab.
     * @returns boolean
     */
    isItemActive: (key: null | undefined | string | number) => boolean;
    /**
     * Moves focus to a tab in the given direction.
     * @param tabElement The current tab element.
     * @param direction The direction to move focus.
     */
    focusTab: (tabElement: HTMLElement | null, direction: 'next' | 'previous' | 'first' | 'last') => void;
    /**
     * Keyboard event handler for tabs.
     */
    onTabKeyDown: (event: React.KeyboardEvent<HTMLButtonElement>, value: undefined | string | number) => void;
    /**
     * Click event handler for tabs.
     */
    onTabClick: (event: React.MouseEvent<HTMLButtonElement>, value: undefined | string | number) => void;
    /**
     * Focus event handler for tabs.
     */
    onTabFocus: (event: React.FocusEvent<HTMLButtonElement>, value: undefined | string | number) => void;
    /**
     * Scrolls the tab list to the previous page.
     */
    scrollPrev: () => void;
    /**
     * Scrolls the tab list to the next page.
     */
    scrollNext: () => void;
    /**
     * Updates the scroll button visibility state.
     */
    updateScrollState: () => void;
    /**
     * Scroll event handler for the content container.
     */
    onContentScroll: () => void;
    /**
     * Ref for the scroll content container.
     */
    contentRef: React.RefObject<HTMLDivElement | null>;
    /**
     * Ref for the previous scroll button.
     */
    prevButtonRef: React.RefObject<HTMLButtonElement | null>;
    /**
     * Ref for the next scroll button.
     */
    nextButtonRef: React.RefObject<HTMLButtonElement | null>;
    /**
     * Ref for the active bar element.
     */
    activeBarRef: React.RefObject<HTMLElement | null>;
    /**
     * Ref for the active tab element.
     */
    activeTabRef: React.RefObject<HTMLElement | null>;
    /**
     * Updates the active bar position and size based on the active tab.
     */
    updateActiveBar: () => void;
    /**
     * Pre-built data attributes reflecting scroll state (data-can-scroll-prev, data-can-scroll-next).
     */
    scrollStateAttrs: Record<string, ''>;
    /**
     * Pre-built props for the scroll content container, includes onScroll handler and scroll state attributes.
     */
    contentProps: {
        onScroll: () => void;
        'data-can-scroll-prev'?: '';
        'data-can-scroll-next'?: '';
    };
    /**
     * Returns accessibility and interaction props for a tab element.
     * @param value The value of the tab.
     * @param disabled Whether the tab is disabled.
     */
    getTabProps: (value: string | number | undefined, disabled?: boolean) => UseTabsTabProps;
    /**
     * Returns props for the active tab indicator element.
     */
    getIndicatorProps: () => UseTabsIndicatorProps;
}
/**
 * Instance of useTabs headless.
 */
export type UseTabsInstance = HeadlessInstance<UseTabsProps, UseTabsState, UseTabsExposes>;
