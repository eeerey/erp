/**
 *
 * The useOrderList manages the state and functionality of an OrderList component.
 *
 * [Live Demo](https://www.primereact.org/orderlist/)
 *
 * @module useOrderList
 * @group headless
 *
 */
import type { HeadlessInstance } from '@primereact/types/core';
import type * as React from 'react';
/**
 * Event fired when the orderlist value changes.
 */
export interface UseOrderListValueChangeEvent {
    /**
     * The original event that triggered the change.
     */
    originalEvent?: React.SyntheticEvent | Event;
    /**
     * Updated list after the reorder.
     */
    value: unknown[];
}
/**
 * Defines valid properties in useOrderList.
 */
export interface UseOrderListProps<T = unknown> {
    /**
     * An array of objects to display and reorder.
     */
    value?: T[];
    /**
     * An array of currently selected items.
     */
    selection?: T[];
    /**
     * When present, it specifies that the component should be disabled.
     * @default false
     */
    disabled?: boolean;
    /**
     * When present, items can be reordered via drag and drop.
     * @default false
     */
    draggable?: boolean;
    /**
     * Controls the placeholder element left behind during drag.
     * - `"clone"` creates a visual copy of the dragged item.
     * - `"empty"` creates an empty element that only preserves the height.
     * @default "empty"
     */
    placeholder?: 'clone' | 'empty';
    /**
     * Callback fired when the list order changes.
     * @param event The event that triggered the change.
     * @param event.originalEvent The original event that triggered the change.
     * @param event.value Updated list after the reorder.
     * @returns void
     */
    onValueChange?: (event: UseOrderListValueChangeEvent) => void;
}
/**
 * Defines valid state in useOrderList.
 */
export interface UseOrderListState<T = unknown> {
    /**
     * Current ordered list of items.
     */
    value: T[];
    /**
     * Currently selected items.
     */
    selection: T[];
    /**
     * Whether a drag operation is currently in progress.
     */
    dragging: boolean;
}
/**
 * Defines the methods and properties exposed by useOrderList.
 */
export interface UseOrderListExposes<T = unknown> {
    /**
     * The state of the useOrderList.
     */
    state: UseOrderListState<T>;
    /**
     * Pre-built props for a list item element.
     * @param item The item data.
     * @param index Index of the item in the list.
     */
    getOptionProps: (item: unknown, index: number) => Record<string, unknown>;
    /**
     * Ref to attach to the element wrapping the option list. When set, moved items are scrolled
     * into view after a reorder.
     */
    listRef: React.RefObject<HTMLElement | null>;
    /**
     * Moves selected items one position up.
     * @param event The event that triggered the action.
     */
    moveUp: (event?: React.SyntheticEvent) => void;
    /**
     * Moves selected items one position down.
     * @param event The event that triggered the action.
     */
    moveDown: (event?: React.SyntheticEvent) => void;
    /**
     * Moves selected items to the top of the list.
     * @param event The event that triggered the action.
     */
    moveTop: (event?: React.SyntheticEvent) => void;
    /**
     * Moves selected items to the bottom of the list.
     * @param event The event that triggered the action.
     */
    moveBottom: (event?: React.SyntheticEvent) => void;
    /**
     * Pre-built data attribute props for the root element.
     */
    rootProps: Record<string, unknown>;
    /**
     * Pre-built data attribute props for the controls container element.
     */
    controlsProps: Record<string, unknown>;
    /**
     * Pre-built data attribute props for the list element.
     */
    listProps: Record<string, unknown>;
    /**
     * Pre-built props for the "move previous" button element.
     */
    prevProps: Record<string, unknown>;
    /**
     * Pre-built props for the "move next" button element.
     */
    nextProps: Record<string, unknown>;
    /**
     * Pre-built props for the "move first" button element.
     */
    firstProps: Record<string, unknown>;
    /**
     * Pre-built props for the "move last" button element.
     */
    lastProps: Record<string, unknown>;
}
/**
 * Instance of useOrderList headless.
 */
export type UseOrderListInstance<T = unknown> = HeadlessInstance<UseOrderListProps<T>, UseOrderListState<T>, UseOrderListExposes<T>>;
