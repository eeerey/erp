/**
 *
 * The useMenuSubmenu manages the state and functionality of a submenu component.
 *
 * [Live Demo](https://www.primereact.org/menu/)
 *
 * @module useMenuSubmenu
 * @group headless
 *
 */
import type { HeadlessInstance } from '@primereact/types/core';
import type { UsePopoverExposes } from '@primereact/types/headless/popover';
import * as React from 'react';
/**
 * Event object for the onOpenChange callback.
 */
export interface UseMenuSubmenuOpenChangeEvent {
    /**
     * The original browser event that triggered the change.
     */
    originalEvent?: Event;
    /**
     * The open state of the submenu.
     */
    value: boolean;
}
/**
 * Props for the useMenuSubmenu hook.
 */
export interface UseMenuSubmenuProps {
    /**
     * Controlled open state of the submenu.
     */
    open?: boolean;
    /**
     * Default open state for uncontrolled mode.
     * @default false
     */
    defaultOpen?: boolean;
    /**
     * Whether the submenu is disabled.
     * @default false
     */
    disabled?: boolean;
    /**
     * The delay in milliseconds before opening the submenu when hovering the subtrigger.
     * Only applies when `openOnHover` is true.
     * @default 100
     */
    openDelay?: number | undefined;
    /**
     * The delay in milliseconds before closing the submenu when the pointer leaves the subtrigger.
     * Only applies when `openOnHover` is true.
     * @default 0
     */
    closeDelay?: number | undefined;
    /**
     * When true, the submenu opens on pointer hover over the subtrigger.
     * @default true
     */
    openOnHover?: boolean | undefined;
    /**
     * Callback to invoke when the open state changes.
     */
    onOpenChange?: (event: UseMenuSubmenuOpenChangeEvent) => void;
}
/**
 * Defines valid state in useMenuSubmenu.
 */
export interface UseMenuSubmenuState {
    /**
     * Whether the submenu is open.
     */
    open: boolean;
}
/**
 * Pre-built props for the sub wrapper element.
 */
export interface UseMenuSubmenuWrapperProps {
    /**
     * Present when the submenu is open.
     */
    'data-open'?: '';
    /**
     * Component scope identifier.
     */
    'data-scope': 'menu';
    /**
     * Component part identifier.
     */
    'data-part': 'sub';
}
/**
 * Pre-built props for the submenu list element.
 */
export interface UseMenuSubmenuListProps {
    /**
     * Ref callback that registers the list element.
     */
    ref: (node: HTMLDivElement | null) => void;
    /**
     * Component scope identifier.
     */
    'data-scope': 'menu';
    /**
     * Component part identifier.
     */
    'data-part': 'list';
}
/**
 * Defines the methods and properties exposed by useMenuSubmenu.
 */
export interface UseMenuSubmenuExposes {
    /**
     * The state of the useMenuSubmenu.
     */
    state: UseMenuSubmenuState;
    /**
     * The popover instance for managing popup state and positioning.
     */
    popover: UsePopoverExposes;
    /**
     * Reference to the list element.
     */
    listRef: React.RefObject<HTMLDivElement | null>;
    /**
     * Props for the subtrigger element.
     */
    subtriggerProps: {
        /**
         * Ref callback that registers the anchor element for positioning.
         */
        ref: (node: HTMLElement | null) => void;
    };
    /**
     * Props for the sub wrapper element.
     */
    subProps: UseMenuSubmenuWrapperProps;
    /**
     * Props for the list element.
     */
    listProps: UseMenuSubmenuListProps;
    /**
     * Toggle the submenu open/close state.
     */
    toggle: () => void;
    /**
     * Open the submenu (composite mode).
     */
    open: () => void;
    /**
     * Close the submenu (composite mode).
     */
    close: () => void;
    /**
     * Open delay in ms (echoed for the parent Menu's hover handler).
     */
    openDelay: number;
    /**
     * Close delay in ms (echoed for the parent Menu's hover handler).
     */
    closeDelay: number;
    /**
     * Whether hover should trigger open/close (echoed for the parent Menu's
     * hover handler).
     */
    openOnHover: boolean;
}
/**
 * Instance of useMenuSubmenu headless.
 */
export type UseMenuSubmenuInstance = HeadlessInstance<UseMenuSubmenuProps, UseMenuSubmenuState, UseMenuSubmenuExposes>;
