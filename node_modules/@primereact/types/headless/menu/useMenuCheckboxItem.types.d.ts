/**
 *
 * The useMenuCheckboxItem manages the state and functionality of a checkbox menu item.
 *
 * [Live Demo](https://www.primereact.org/menu/)
 *
 * @module useMenuCheckboxItem
 * @group headless
 *
 */
import type { HeadlessInstance } from '@primereact/types/core';
import type { UseCheckboxExposes, UseCheckboxProps } from '@primereact/types/headless/checkbox';
import type * as React from 'react';
import type { UseMenuItemSelectEvent } from './useMenu.types';
/**
 * Event object for the onCheckedChange callback.
 */
export interface UseMenuCheckboxItemCheckedChangeEvent {
    /**
     * The original browser event that triggered the change.
     */
    originalEvent: React.SyntheticEvent;
    /**
     * The checked state of the checkbox item.
     */
    checked: boolean;
}
/**
 * Props for the useMenuCheckboxItem hook.
 */
export interface UseMenuCheckboxItemProps extends Omit<UseCheckboxProps, 'onCheckedChange'> {
    /**
     * Whether the item is disabled.
     * @default false
     */
    disabled?: boolean | undefined;
    /**
     * Whether activating this checkbox-item should dismiss the parent menu.
     * Defaults to `false` because toggling a checkbox normally keeps the menu open.
     * @default false
     */
    closeOnSelect?: boolean | undefined;
    /**
     * Callback fired when the item is activated (mouse or keyboard).
     * Calling `event.preventDefault()` keeps the menu open regardless of `closeOnSelect`.
     */
    onSelect?: (event: UseMenuItemSelectEvent) => void;
    /**
     * Callback fired when the checkbox-item's checked state changes.
     */
    onCheckedChange?: (event: UseMenuCheckboxItemCheckedChangeEvent) => void;
}
/**
 * Defines valid state in useMenuCheckboxItem.
 */
export interface UseMenuCheckboxItemState {
    /**
     * Whether the checkbox-item is checked.
     */
    checked: boolean;
}
/**
 * Defines the methods and properties exposed by useMenuCheckboxItem.
 * @extends UseCheckboxExposes
 */
export interface UseMenuCheckboxItemExposes extends Omit<UseCheckboxExposes, 'state'> {
    /**
     * Resolved state of the checkbox-item, merging the underlying useCheckbox state with a normalized boolean `checked`.
     */
    state: UseMenuCheckboxItemState & UseCheckboxExposes['state'];
    /**
     * Resolved `closeOnSelect` value forwarded to the parent menu's `getItemProps`.
     */
    closeOnSelect: boolean;
    /**
     * Composite `onSelect` handler ready to plug into the parent menu's `getItemProps`.
     * Toggles the checkbox value and forwards to the user-supplied `onSelect`.
     */
    onSelect: (event: UseMenuItemSelectEvent) => void;
}
/**
 * Instance of useMenuCheckboxItem headless.
 */
export type UseMenuCheckboxItemInstance = HeadlessInstance<UseMenuCheckboxItemProps, UseMenuCheckboxItemState, UseMenuCheckboxItemExposes>;
