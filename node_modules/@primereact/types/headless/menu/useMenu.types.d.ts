/**
 *
 * The useMenu manages the state and functionality of a menu component.
 *
 * [Live Demo](https://www.primereact.org/menu/)
 *
 * @module useMenu
 * @group headless
 *
 */
import type { HeadlessInstance } from '@primereact/types/core';
import * as React from 'react';
import type { UsePopoverArrowProps, UsePopoverExposes, UsePopoverPopupProps, UsePopoverPositionerProps } from '@primereact/types/headless/popover';
import type { UseMenuSubmenuExposes } from './useMenuSubmenu.types';
/**
 * Event object for the onOpenChange callback.
 */
export interface UseMenuOpenChangeEvent {
    /**
     * The original browser event that triggered the change.
     */
    originalEvent?: Event;
    /**
     * The open state of the menu.
     */
    value: boolean;
}
/**
 * Props for the useMenu hook.
 */
export interface UseMenuProps {
    /**
     * Controlled open state of the menu.
     */
    open?: boolean;
    /**
     * Default open state for uncontrolled mode.
     * @default false
     */
    defaultOpen?: boolean;
    /**
     * Whether the menu is in composite mode (navigationmenu).
     * In composite mode, submenus open on hover and focusedOptionId is an array tracking the focus path.
     * @default false
     */
    composite?: boolean;
    /**
     * The element to which the overlay is appended.
     * @default 'body'
     */
    appendTo?: 'body' | 'self' | HTMLElement;
    /**
     * Base zIndex value to use in layering.
     * @default 0
     */
    baseZIndex?: number;
    /**
     * Whether to automatically manage layering.
     * @default true
     */
    autoZIndex?: boolean;
    /**
     * Index of the element in tabbing order.
     * @default 0
     */
    tabIndex?: number;
    /**
     * The delay in milliseconds before opening the menu when hovering the trigger.
     * Only applies when `openOnHover` is true.
     * @default 100
     */
    openDelay?: number | undefined;
    /**
     * The delay in milliseconds before closing the menu when the pointer leaves the trigger.
     * Only applies when `openOnHover` is true.
     * @default 0
     */
    closeDelay?: number | undefined;
    /**
     * When true, the menu opens on pointer hover over the trigger (in addition to click).
     * @default false
     */
    openOnHover?: boolean | undefined;
    /**
     * Callback to invoke when the open state changes.
     * @param event The event that triggered the change.
     * @returns void
     */
    onOpenChange?: (event: UseMenuOpenChangeEvent) => void;
    /**
     * Optional parent submenu context whose popover state should drive this menu.
     * When provided, useMenu reuses the parent submenu's popover (anchor, open
     * state, positioner) instead of creating its own. Forwarded by Menu.Root
     * automatically when nested inside a Menu.Submenu.
     */
    context?: UseMenuSubmenuExposes | undefined;
    /**
     * Optional parent NavigationMenu context. When provided, ArrowRight / ArrowLeft
     * pressed on a list item that is not a submenu trigger will move focus to
     * the next / previous sibling navigationmenu trigger (WAI-ARIA NavigationMenu pattern).
     * Forwarded by Menu.Root when nested inside a `NavigationMenu`.
     */
    navigationmenu?: import('@primereact/types/headless/navigationmenu').UseNavigationMenuExposes | undefined;
    /**
     * The id this menu is registered under in the parent NavigationMenu's trigger
     * registry. Required for `navigationmenu` sibling navigation to know which
     * trigger is "current".
     */
    navigationmenuMenuId?: string | undefined;
}
/**
 * Defines valid state in useMenu.
 */
export interface UseMenuState {
    /**
     * Whether the menu is open.
     */
    open: boolean;
    /**
     * Whether the menu is focused.
     */
    focused: boolean;
    /**
     * The ID of the focused option (HTML id attribute).
     * In composite mode, this is an array representing the focus path (e.g., ['menu_0', 'menu_0_1']).
     * The last element is used for aria-activedescendant.
     */
    focusedOptionId: string | string[];
}
/**
 * Props for the trigger element of the menu.
 */
export interface UseMenuTriggerProps {
    /**
     * Indicates the element has a popup.
     */
    'aria-haspopup': 'true';
    /**
     * Whether the menu popup is currently expanded.
     */
    'aria-expanded': boolean;
    /**
     * Component scope identifier.
     */
    'data-scope': 'menu';
    /**
     * Component part identifier.
     */
    'data-part': 'trigger';
    /**
     * Present when the menu is open.
     */
    'data-open'?: '';
    /**
     * Present when the menu is expanded.
     */
    'data-expanded'?: '';
    /**
     * Data attribute when the positioner is open.
     */
    'data-positioner-open'?: string;
    /**
     * Ref callback that registers the trigger element as the popover anchor.
     */
    ref: (node: HTMLElement | null) => void;
    /**
     * Click handler to toggle the menu.
     */
    onClick: () => void;
}
/**
 * Event passed to the `onSelect` handler when a menu item is activated.
 */
export interface UseMenuItemSelectEvent {
    /**
     * Original DOM event (mouse or keyboard) that triggered the activation.
     */
    originalEvent: React.SyntheticEvent;
    /**
     * The activated item's semantic value.
     */
    value: string;
    /**
     * Calling this prevents the default close-on-select behavior so the menu
     * stays open after the item is activated.
     */
    preventDefault: () => void;
    /**
     * Whether `preventDefault` has been called on this event.
     */
    readonly defaultPrevented: boolean;
}
/**
 * Input options for getItemProps.
 */
export interface UseMenuGetItemPropsInput {
    /**
     * The semantic value that uniquely identifies this item within the menu.
     * Used to generate a deterministic DOM id.
     */
    value: string;
    /**
     * Whether the item is disabled.
     */
    disabled?: boolean;
    /**
     * The type of the menu item.
     * @default 'item'
     */
    type?: 'item' | 'checkbox' | 'radio';
    /**
     * Whether the item is checked (for checkbox/radio types).
     */
    checked?: boolean;
    /**
     * Whether the item is rendered inside a portal.
     */
    inPortal?: boolean;
    /**
     * Whether the menu should close when this item is activated. Item-level
     * value overrides the menu-root `closeOnSelect` setting.
     * @default true
     */
    closeOnSelect?: boolean;
    /**
     * Callback invoked when the item is activated (mouse down or keyboard).
     * Calling `event.preventDefault()` keeps the menu open regardless of
     * `closeOnSelect`.
     */
    onSelect?: (event: UseMenuItemSelectEvent) => void;
}
/**
 * Return value of getItemProps.
 */
export interface UseMenuGetItemPropsReturn {
    /**
     * Ref callback that registers/unregisters the item element with the menu.
     */
    ref: (element: HTMLElement | null) => void;
    /**
     * Unique identifier for the menu item element.
     */
    id: string;
    /**
     * ARIA role for the menu item.
     */
    role: 'menuitem' | 'menuitemcheckbox' | 'menuitemradio';
    /**
     * Tab index of the menu item. Always -1 since focus is managed programmatically.
     */
    tabIndex: -1;
    /**
     * Indicates whether the menu item is disabled.
     */
    'aria-disabled'?: boolean;
    /**
     * Indicates whether the menu item is checked. Applicable for menuitemcheckbox and menuitemradio roles.
     */
    'aria-checked'?: boolean;
    /**
     * Component scope identifier.
     */
    'data-scope': 'menu';
    /**
     * Component part identifier.
     */
    'data-part': 'item';
    /**
     * The semantic value of the menu item.
     */
    'data-value': string;
    /**
     * Present when the menu item is focused.
     */
    'data-focused'?: '';
    /**
     * Present when the menu item is checked.
     */
    'data-checked'?: '';
    /**
     * Present when the menu item is disabled.
     */
    'data-disabled'?: '';
    /**
     * Handler for mouse down events on the menu item.
     */
    onMouseDown: (event: React.MouseEvent) => void;
    /**
     * Handler for mouse move events on the menu item.
     */
    onMouseMove: () => void;
    /**
     * Handler for mouse enter events on the menu item.
     */
    onMouseEnter?: () => void;
}
/**
 * Input options for getSubTriggerProps.
 */
export interface UseMenuGetSubTriggerPropsInput {
    /**
     * The semantic value that uniquely identifies this subtrigger within the menu.
     * Used to generate a deterministic DOM id.
     */
    value: string;
    /**
     * Whether the subtrigger is disabled.
     */
    disabled?: boolean;
    /**
     * Whether the item is rendered inside a portal.
     */
    inPortal?: boolean;
    /**
     * The useMenuSubmenu instance. When provided, open state, submenu methods and listRef
     * are extracted automatically from the sub instance.
     */
    sub?: UseMenuSubmenuExposes;
}
/**
 * Return value of getSubTriggerProps.
 */
export interface UseMenuGetSubTriggerPropsReturn {
    /**
     * Ref callback that registers/unregisters the subtrigger element with the menu.
     */
    ref: (element: HTMLElement | null) => void;
    /**
     * Unique identifier for the subtrigger element.
     */
    id: string;
    /**
     * ARIA role for the subtrigger element.
     */
    role: 'menuitem';
    /**
     * Tab index of the subtrigger. Always -1 since focus is managed programmatically.
     */
    tabIndex: -1;
    /**
     * Indicates whether the associated submenu is expanded.
     */
    'aria-expanded'?: boolean;
    /**
     * Indicates whether the subtrigger is disabled.
     */
    'aria-disabled'?: boolean;
    /**
     * Component scope identifier.
     */
    'data-scope': 'menu';
    /**
     * Component part identifier.
     */
    'data-part': 'subtrigger';
    /**
     * The semantic value of the subtrigger.
     */
    'data-value': string;
    /**
     * Present when the subtrigger is focused.
     */
    'data-focused'?: '';
    /**
     * Present when the subtrigger is disabled.
     */
    'data-disabled'?: '';
    /**
     * Present when the associated submenu is open.
     */
    'data-open'?: '';
    /**
     * Handler for mouse down events on the subtrigger.
     */
    onMouseDown: (event: React.MouseEvent) => void;
    /**
     * Handler for mouse move events on the subtrigger.
     */
    onMouseMove: () => void;
    /**
     * Handler for mouse enter events on the subtrigger.
     */
    onMouseEnter: () => void;
    /**
     * Handler for mouse leave events on the subtrigger.
     */
    onMouseLeave: (event: React.MouseEvent) => void;
}
/**
 * Input options for getIndicatorProps.
 */
export interface UseMenuGetIndicatorPropsInput {
    /**
     * The type of the menu item.
     */
    type?: 'item' | 'checkbox' | 'radio';
    /**
     * Whether the item is checked (for checkbox/radio types).
     */
    checked?: boolean;
    /**
     * Whether the submenu is open.
     */
    submenuOpen?: boolean;
    /**
     * Determines visibility based on the state of the parent menu element.
     */
    match?: 'open' | 'closed' | 'checked' | 'unchecked';
}
/**
 * Static data attributes shared by checkbox, radio and submenu indicators.
 */
export interface UseMenuIndicatorProps {
    /**
     * Component scope identifier.
     */
    'data-scope': 'menu';
    /**
     * Component part identifier.
     */
    'data-part': 'indicator';
}
/**
 * Return value of getIndicatorProps.
 */
export interface UseMenuGetIndicatorPropsReturn {
    /**
     * Whether the indicator content should be visible based on the match prop.
     */
    matchVisible: boolean;
    /**
     * Component scope identifier.
     */
    'data-scope': 'menu';
    /**
     * Component part identifier.
     */
    'data-part': 'indicator';
    /**
     * Present when the submenu is open.
     */
    'data-open'?: '';
    /**
     * Present when the submenu is closed.
     */
    'data-closed'?: '';
    /**
     * Present when the checkbox/radio item is checked.
     */
    'data-checked'?: '';
    /**
     * Present when the checkbox/radio item is unchecked.
     */
    'data-unchecked'?: '';
}
/**
 * Input options for getListProps.
 */
export interface UseMenuGetListPropsInput {
    /**
     * The semantic value for this list. Only needed for submenu lists.
     * Used to generate a deterministic DOM id.
     */
    value?: string;
    /**
     * The useMenuSubmenu instance. When provided, the list is treated as a submenu list
     * and open state is extracted from the sub instance.
     */
    sub?: UseMenuSubmenuExposes;
}
/**
 * Return value of getListProps.
 */
export interface UseMenuGetListPropsReturn {
    /**
     * Ref callback that registers the list element (root list only).
     */
    ref?: (node: HTMLDivElement | null) => void;
    /**
     * Unique identifier for the list element.
     */
    id?: string;
    /**
     * ARIA role for the list element.
     */
    role: 'menu';
    /**
     * The id of the currently active (focused) descendant item.
     */
    'aria-activedescendant'?: string;
    /**
     * Component scope identifier.
     */
    'data-scope': 'menu';
    /**
     * Component part identifier.
     */
    'data-part': 'list';
    /**
     * Tab index of the list element.
     */
    tabIndex: number;
    /**
     * Handler for keyboard events on the list.
     */
    onKeyDown?: (event: React.KeyboardEvent) => void;
    /**
     * Handler for focus events on the list.
     */
    onFocus?: () => void;
    /**
     * Handler for blur events on the list.
     */
    onBlur?: () => void;
    /**
     * Handler for mouse enter on the list. Submenu lists use this to cancel
     * any pending close timer scheduled by the parent subtrigger on leave.
     */
    onMouseEnter?: (event: React.MouseEvent) => void;
    /**
     * Inline styles applied to the list element.
     */
    style?: React.CSSProperties;
}
/**
 * Pre-built props for the menu label element.
 */
export interface UseMenuLabelProps {
    /**
     * ARIA role for the label element.
     */
    role: 'none';
    /**
     * Component scope identifier.
     */
    'data-scope': 'menu';
    /**
     * Component part identifier.
     */
    'data-part': 'label';
}
/**
 * Pre-built props for the menu separator element.
 */
export interface UseMenuSeparatorProps {
    /**
     * ARIA role for the separator element.
     */
    role: 'separator';
    /**
     * Component scope identifier.
     */
    'data-scope': 'menu';
    /**
     * Component part identifier.
     */
    'data-part': 'separator';
}
/**
 * Pre-built props for the menu radio group element.
 */
export interface UseMenuRadioGroupProps {
    /**
     * ARIA role for the radio group element.
     */
    role: 'group';
    /**
     * Component scope identifier.
     */
    'data-scope': 'menu';
    /**
     * Component part identifier.
     */
    'data-part': 'radiogroup';
}
/**
 * Pre-built props for the menu root element.
 */
export interface UseMenuRootProps {
    /**
     * The unique identifier for the menu root element.
     */
    id: string | undefined;
    /**
     * Component scope identifier.
     */
    'data-scope': 'menu';
    /**
     * Component part identifier.
     */
    'data-part': 'root';
}
/**
 * Defines the methods and properties exposed by useMenu.
 */
export interface UseMenuExposes {
    /**
     * The state of the useMenu.
     */
    state: UseMenuState;
    /**
     * The popover instance for managing popup state and positioning.
     */
    popover: UsePopoverExposes;
    /**
     * Props for the root element.
     */
    rootProps: UseMenuRootProps;
    /**
     * Props for the trigger element.
     */
    triggerProps: UseMenuTriggerProps;
    /**
     * Props for the arrow element.
     */
    arrowProps: UsePopoverArrowProps;
    /**
     * Props for the popup element.
     */
    popupProps: UsePopoverPopupProps;
    /**
     * Props for the positioner element.
     */
    positionerProps: UsePopoverPositionerProps;
    /**
     * Props for the label element.
     */
    labelProps: UseMenuLabelProps;
    /**
     * Props for the separator element.
     */
    separatorProps: UseMenuSeparatorProps;
    /**
     * Props for the radio group element.
     */
    radioGroupProps: UseMenuRadioGroupProps;
    /**
     * Generate a deterministic DOM id for an item given its value.
     * @param value The semantic value of the item.
     * @returns The DOM id string.
     */
    getItemId: (value: string) => string;
    /**
     * Generate a deterministic DOM id for a list given an optional value.
     * @param value The semantic value (for submenu lists). Omit for root list.
     * @returns The DOM id string.
     */
    getListId: (value?: string) => string;
    /**
     * Get props for a menu item element. Returns ARIA attributes, data attributes, and mouse handlers.
     * @param input The item configuration.
     * @returns Props object to spread on the item element.
     */
    getItemProps: (input: UseMenuGetItemPropsInput) => UseMenuGetItemPropsReturn;
    /**
     * Get props for a submenu trigger element. Returns ARIA attributes, data attributes, and mouse handlers.
     * @param input The subtrigger configuration.
     * @returns Props object to spread on the subtrigger element.
     */
    getSubTriggerProps: (input: UseMenuGetSubTriggerPropsInput) => UseMenuGetSubTriggerPropsReturn;
    /**
     * Get props for a menu list element. Returns role, ARIA attributes, keyboard handlers, and focus handlers.
     * @param input The list configuration.
     * @returns Props object to spread on the list element.
     */
    getListProps: (input?: UseMenuGetListPropsInput) => UseMenuGetListPropsReturn;
    /**
     * Get props for a menu indicator element. Returns data attributes and matchVisible flag based on indicator context.
     * @param input The indicator configuration.
     * @returns Props object with data attributes and matchVisible flag.
     */
    getIndicatorProps: (input: UseMenuGetIndicatorPropsInput) => UseMenuGetIndicatorPropsReturn;
    /**
     * Static data attributes for the checkbox item indicator element.
     */
    checkboxItemIndicatorProps: UseMenuIndicatorProps;
    /**
     * Static data attributes for the radio item indicator element.
     */
    radioItemIndicatorProps: UseMenuIndicatorProps;
    /**
     * Static data attributes for the submenu indicator element.
     */
    submenuIndicatorProps: UseMenuIndicatorProps;
    /**
     * Check whether a given item ID is currently focused.
     * @param id The item ID to check.
     * @returns Whether the item is focused.
     */
    isFocused: (id: string) => boolean;
    /**
     * Update ARIA level/position attributes on an item element using DOM-based calculation.
     * Should be called after the element is mounted (e.g., in registerItem or useLayoutEffect).
     * @param element The item's DOM element.
     */
    updateItemAria: (element: HTMLElement) => void;
    /**
     * Handle trigger click event.
     * @param event The event that triggered the click.
     * @returns void
     */
    onTriggerClick: (event?: React.MouseEvent) => void;
    /**
     * Handle overlay enter event.
     * @returns void
     */
    onOverlayEnter: () => void;
    /**
     * Change the visible state.
     * @param isVisible The visible state.
     * @returns void
     */
    changeVisibleState: (isVisible: boolean) => void;
    /**
     * Handle list focus event.
     * @returns void
     */
    onListFocus: () => void;
    /**
     * Handle list blur event.
     * @returns void
     */
    onListBlur: () => void;
    /**
     * Handle keyboard events on the list.
     * @param event The keyboard event.
     * @returns void
     */
    onListKeyDown: (event: React.KeyboardEvent) => void;
    /**
     * Mark that a mouse interaction is in progress.
     * Prevents onListFocus from auto-focusing the first item.
     * @param isMouseInteraction The mouse interaction state.
     * @returns void
     */
    markMouseInteraction: (isMouseInteraction: boolean) => void;
    /**
     * Change the focused option ID.
     * In composite mode, uses DOM-based level detection.
     * @param id The id of the option.
     * @returns void
     */
    changeFocusedOptionId: (id: string) => void;
    /**
     * Hide all submenus at or after a specific level (composite mode only).
     * Uses DOM-based level detection.
     * @param targetItemId The id of the target item.
     * @returns void
     */
    hideSubmenusAfterLevel?: (targetItemId: string) => void;
    /**
     * Hide all open submenus and reset focus (composite mode only).
     */
    hideAllSubmenus: () => void;
    /**
     * Handle item click event.
     * @param event The event that triggered the click.
     * @param isBlur Whether the item should be blurred after the click.
     * @param isPortal Whether the item is in a portal.
     * @returns void
     */
    onItemClick: (event: React.MouseEvent | React.KeyboardEvent, isBlur?: boolean, isPortal?: boolean) => void;
}
/**
 * Instance of useMenu headless.
 */
export type UseMenuInstance = HeadlessInstance<UseMenuProps, UseMenuState, UseMenuExposes>;
