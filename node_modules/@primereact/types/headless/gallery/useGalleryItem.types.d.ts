/**
 *
 * The useGalleryItem manages the state and functionality of a GalleryItem component.
 *
 * [Live Demo](https://www.primereact.org/gallery/)
 *
 * @module useGalleryItem
 * @group headless
 *
 */
import type { HeadlessInstance } from '@primereact/types/core';
import type { UseGalleryExposes, UseGalleryState } from './useGallery.types';
/**
 * Minimal gallery interface required by useGalleryItem.
 * Satisfied by both UseGalleryInstance (headless) and GalleryRootInstance (primitive).
 */
export interface UseGalleryItemGalleryRef extends UseGalleryExposes {
    state: UseGalleryState;
}
/**
 * Event fired when the gallery item's active index changes.
 */
export interface UseGalleryItemChangeEvent<E = React.SyntheticEvent> {
    /**
     * The original event that triggered the change.
     */
    originalEvent: E;
    /**
     * The active index of the gallery item.
     */
    value: null | undefined | string | number | (string | number)[];
}
/**
 * Defines valid properties in useGalleryItem component.
 */
export interface UseGalleryItemProps {
    /**
     * The parent gallery instance. The primitive `Gallery.Item` reads it from the
     * nearest `GalleryProvider` and passes it here; supply it directly for standalone
     * headless usage. The headless hook itself never reads React context, keeping the
     * headless layer independent of the primitive layer.
     */
    context?: UseGalleryItemGalleryRef;
    /**
     * The normal scale of the gallery item.
     * @default 1
     */
    normalScale?: number;
    /**
     * The zoomed scale of the gallery item.
     * @default 3
     */
    zoomedScale?: number;
}
/**
 * Defines valid state in useGalleryItem component.
 */
export interface UseGalleryItemState {
    /**
     * The index of the item in the gallery.
     */
    index: number;
    /**
     * Whether the item is the active (visible) item.
     */
    isActive: boolean;
    /**
     * The current pan position.
     */
    position: {
        x: number;
        y: number;
    };
    /**
     * The current zoom scale.
     */
    scale: number;
    /**
     * The current rotation in degrees.
     */
    rotation: number;
    /**
     * The current flip state.
     */
    flip: {
        x: number;
        y: number;
    };
}
/**
 * Pre-built props for a gallery item element.
 *
 * The `style` object includes the following CSS custom properties for transforms:
 * - `--position-x`: The horizontal pan position (e.g. `"10px"`).
 * - `--position-y`: The vertical pan position (e.g. `"10px"`).
 * - `--scale`: The current zoom scale (e.g. `"1"`).
 * - `--rotation`: The current rotation (e.g. `"90deg"`).
 * - `--flip-x`: The horizontal flip multiplier (1 or -1).
 * - `--flip-y`: The vertical flip multiplier (1 or -1).
 */
export interface UseGalleryItemElementProps {
    /**
     * Data attribute for scope identification.
     */
    'data-scope': 'gallery';
    /**
     * Data attribute for part identification.
     */
    'data-part': 'item';
    /**
     * Data attribute for the item index.
     */
    'data-index': number;
    /**
     * Data attribute present when the item is active.
     */
    'data-active'?: '';
    /**
     * Data attribute present while the item is rotating.
     */
    'data-rotating'?: '';
    /**
     * Click handler for the item.
     */
    onClick: (e: React.MouseEvent<HTMLDivElement>) => void;
    /**
     * Pointer down handler for the item.
     */
    onPointerDown: (e: React.PointerEvent<HTMLDivElement>) => void;
    /**
     * Pointer move handler for the item.
     */
    onPointerMove: (e: React.PointerEvent<HTMLDivElement>) => void;
    /**
     * Pointer up handler for the item.
     */
    onPointerUp: (e: React.PointerEvent<HTMLDivElement>) => void;
    /**
     * Drag start handler for the item.
     */
    onDragStart: (e: React.DragEvent<HTMLDivElement>) => void;
    /**
     * Structural style including CSS custom properties for transforms.
     */
    style: React.CSSProperties;
}
/**
 * Defines the methods and properties exposed by useGalleryItem component.
 */
export interface UseGalleryItemExposes {
    /**
     * The state of the useGalleryItem.
     */
    state: UseGalleryItemState;
    /**
     * Pre-built props for the item root element.
     */
    itemProps: UseGalleryItemElementProps;
    /**
     * Handles the drag start event to prevent default browser drag behavior.
     * @param e - The drag event.
     * @returns void
     */
    handleDragStart: (e: React.DragEvent<HTMLDivElement>) => void;
    /**
     * Handles the pointer up event to end panning.
     * @param e - The pointer event.
     * @returns void
     */
    handlePointerUp: (e: React.PointerEvent<HTMLDivElement>) => void;
    /**
     * Handles the pointer move event for panning while zoomed.
     * @param e - The pointer event.
     * @returns void
     */
    handlePointerMove: (e: React.PointerEvent<HTMLDivElement>) => void;
    /**
     * Handles the pointer down event to start panning.
     * @param e - The pointer event.
     * @returns void
     */
    handlePointerDown: (e: React.PointerEvent<HTMLDivElement>) => void;
    /**
     * Handles the click event to toggle zoom.
     * @param e - The mouse event.
     * @returns void
     */
    handleClick: (e: React.MouseEvent<HTMLDivElement>) => void;
    /**
     * Zooms in the gallery item.
     * @returns void
     */
    zoomIn: () => void;
    /**
     * Zooms out the gallery item.
     * @returns void
     */
    zoomOut: () => void;
    /**
     * Rotates the gallery item left by 90 degrees.
     * @returns void
     */
    rotateLeft: () => void;
    /**
     * Rotates the gallery item right by 90 degrees.
     * @returns void
     */
    rotateRight: () => void;
    /**
     * Flips the gallery item horizontally.
     * @returns void
     */
    flipX: () => void;
    /**
     * Flips the gallery item vertically.
     * @returns void
     */
    flipY: () => void;
    /**
     * Downloads the gallery item.
     * @returns void
     */
    download: () => void;
    /**
     * Calculates the pan constraints for the gallery item based on its current scale.
     * @returns The min/max constraints for x and y axes.
     */
    calculateConstraints: () => {
        minX: number;
        maxX: number;
        minY: number;
        maxY: number;
    };
    /**
     * Recalculates the size of the gallery item element.
     * @returns void
     */
    calculateItemSize: () => void;
}
/**
 * Instance of useGalleryItem headless.
 */
export type UseGalleryItemInstance = HeadlessInstance<UseGalleryItemProps, UseGalleryItemState, UseGalleryItemExposes>;
