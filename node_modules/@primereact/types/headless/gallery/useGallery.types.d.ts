/**
 *
 * The useGallery manages the state and functionality of a Gallery component.
 *
 * [Live Demo](https://www.primereact.org/gallery/)
 *
 * @module useGallery
 * @group headless
 *
 */
import type { HeadlessInstance } from '@primereact/types/core';
import type { CarouselRootInstance } from '@primereact/types/primitive/carousel';
/**
 * Represents a pending action dispatched from toolbar to the active gallery item.
 */
export interface GalleryPendingAction {
    /**
     * The type of the action.
     */
    type: 'zoom-in' | 'zoom-out' | 'rotate-left' | 'rotate-right' | 'flip-x' | 'flip-y' | 'download';
    /**
     * Timestamp to ensure repeated identical actions trigger new effects.
     */
    timestamp: number;
}
/**
 * Event fired when the gallery's active index changes.
 */
export interface UseGalleryChangeEvent<E = React.SyntheticEvent> {
    /**
     * The original event that triggered the change. Absent when the change is
     * triggered programmatically (e.g. via the `next()` / `prev()` API).
     */
    originalEvent?: E;
    /**
     * The value of the gallery.
     */
    value: number | undefined;
}
/**
 * Defines valid properties in useGallery.
 */
export interface UseGalleryProps {
    /**
     * The index of the active item (controlled). Use together with `onActiveIndexChange`.
     * When provided, the gallery is controlled and its active item is driven by this value.
     */
    activeIndex?: number;
    /**
     * The initial index of the active item for uncontrolled usage.
     * @default 0
     */
    defaultActiveIndex?: number;
    /**
     * Whether the gallery is rendered fullscreen via a portal. When `true`, the gallery escapes its parent layout
     * and covers the viewport — useful for opening a single image in a modal-style viewer.
     * @default false
     */
    fullscreen?: boolean;
    /**
     * Whether pressing Escape closes the gallery when in fullscreen mode.
     * @default true
     */
    closeOnEscape?: boolean;
    /**
     * Callback fired when the gallery's fullscreen state changes (via close button, escape key, etc.).
     */
    onFullscreenChange?: (value: boolean) => void;
    /**
     * Callback fired when the active item's zoom scale changes.
     */
    onZoomChange?: (scale: number) => void;
    /**
     * Callback fired when the active item's rotation changes.
     */
    onRotateChange?: (rotation: number) => void;
    /**
     * Callback fired when the active item's flip state changes.
     */
    onFlipChange?: (flip: {
        x: number;
        y: number;
    }) => void;
    /**
     * Callback fired when the gallery's active index changes.
     * @param event The event that triggered the change.
     * @param event.originalEvent The original event that triggered the change.
     * @param event.value The value of the gallery.
     * @returns void
     */
    onActiveIndexChange?: (event: UseGalleryChangeEvent) => void;
}
/**
 * Defines valid state in useGallery.
 */
export interface UseGalleryState {
    /**
     * Whether the gallery is in fullscreen mode.
     */
    isFullscreen: boolean;
    /**
     * The index of the active item.
     */
    activeIndex: number;
    /**
     * Whether the active item is zoomed in.
     */
    zoomed: boolean;
    /**
     * Whether the active item is rotated.
     */
    rotated: boolean;
    /**
     * Whether the active item is flipped.
     */
    flipped: boolean;
    /**
     * The pending action for the active item to consume.
     */
    pendingAction: GalleryPendingAction | null;
}
/**
 * Defines the available actions for the gallery.
 */
type useGalleryActions = {
    /**
     * Zooms in the gallery.
     * @returns void
     */
    zoomIn: () => void;
    /**
     * Zooms out the gallery.
     * @returns void
     */
    zoomOut: () => void;
    /**
     * Rotates the gallery left.
     * @returns void
     */
    rotateLeft: () => void;
    /**
     * Rotates the gallery right.
     * @returns void
     */
    rotateRight: () => void;
    /**
     * Flips the gallery horizontally.
     * @returns void
     */
    flipX: () => void;
    /**
     * Flips the gallery vertically.
     * @returns void
     */
    flipY: () => void;
    /**
     * Downloads the active gallery item.
     * @returns void
     */
    download: () => void;
    /**
     * Navigates to the next item.
     * @returns void
     */
    next: (originalEvent?: React.SyntheticEvent) => void;
    /**
     * Navigates to the previous item.
     * @returns void
     */
    prev: (originalEvent?: React.SyntheticEvent) => void;
    /**
     * Toggles the fullscreen state.
     * @returns void
     */
    toggleFullScreen: () => void;
};
/**
 * Pre-built props for the gallery root element.
 */
export interface UseGalleryRootProps {
    /**
     * The unique identifier of the gallery.
     */
    id?: string;
    /**
     * Data attribute for scope identification.
     */
    'data-scope': 'gallery';
    /**
     * Data attribute for part identification.
     */
    'data-part': 'root';
    /**
     * Data attribute when the gallery is in fullscreen mode.
     */
    'data-fullscreen'?: '';
    /**
     * Data attribute when the active item is zoomed.
     */
    'data-zoomed'?: '';
    /**
     * Data attribute when the active item is rotated.
     */
    'data-rotated'?: '';
    /**
     * Data attribute when the active item is flipped.
     */
    'data-flipped'?: '';
}
/**
 * Pre-built props for the gallery next button element.
 */
export interface UseGalleryNextProps {
    /**
     * Data attribute for scope identification.
     */
    'data-scope': 'gallery';
    /**
     * Data attribute for part identification.
     */
    'data-part': 'next';
    /**
     * Click handler to navigate to the next item.
     */
    onClick: () => void;
}
/**
 * Pre-built props for the gallery previous button element.
 */
export interface UseGalleryPrevProps {
    /**
     * Data attribute for scope identification.
     */
    'data-scope': 'gallery';
    /**
     * Data attribute for part identification.
     */
    'data-part': 'prev';
    /**
     * Click handler to navigate to the previous item.
     */
    onClick: () => void;
}
/**
 * Pre-built props for the gallery thumbnail element.
 */
export interface UseGalleryThumbnailProps {
    /**
     * Data attribute for scope identification.
     */
    'data-scope': 'gallery';
    /**
     * Data attribute for part identification.
     */
    'data-part': 'thumbnail';
    /**
     * The active slide index passed to the carousel.
     */
    slide: number;
}
/**
 * Pre-built props for the gallery thumbnail content element.
 */
export interface UseGalleryThumbnailContentProps {
    /**
     * Data attribute for scope identification.
     */
    'data-scope': 'gallery';
    /**
     * Data attribute for part identification.
     */
    'data-part': 'thumbnailContent';
}
/**
 * Pre-built props for a gallery thumbnail item element.
 */
export interface UseGalleryThumbnailItemProps {
    /**
     * Data attribute for scope identification.
     */
    'data-scope': 'gallery';
    /**
     * Data attribute for part identification.
     */
    'data-part': 'thumbnailItem';
    /**
     * ARIA role for the clickable thumbnail.
     */
    role: 'button';
    /**
     * Marks the thumbnail of the currently shown slide.
     */
    'aria-current'?: true;
    /**
     * Accessible name for the thumbnail (e.g. "Slide 2").
     */
    'aria-label'?: string;
    /**
     * Data attribute when the thumbnail item is active.
     */
    'data-active'?: '';
    /**
     * Click handler to select this thumbnail item.
     */
    onClick: (event: React.MouseEvent) => void;
}
/**
 * Pre-built props for the gallery toolbar element.
 */
export interface UseGalleryToolbarProps {
    /**
     * Data attribute for scope identification.
     */
    'data-scope': 'gallery';
    /**
     * Data attribute for part identification.
     */
    'data-part': 'toolbar';
}
/**
 * Pre-built props for a gallery toolbar item element.
 */
export interface UseGalleryToolbarItemProps {
    /**
     * Data attribute for scope identification.
     */
    'data-scope': 'gallery';
    /**
     * Data attribute for part identification.
     */
    'data-part': 'toolbarItem';
    /**
     * Click handler to execute the toolbar action.
     */
    onClick: () => void;
}
/**
 * Pre-built props for the gallery header element.
 */
export interface UseGalleryHeaderProps {
    /**
     * Data attribute for scope identification.
     */
    'data-scope': 'gallery';
    /**
     * Data attribute for part identification.
     */
    'data-part': 'header';
}
/**
 * Pre-built props for the gallery footer element.
 */
export interface UseGalleryFooterProps {
    /**
     * Data attribute for scope identification.
     */
    'data-scope': 'gallery';
    /**
     * Data attribute for part identification.
     */
    'data-part': 'footer';
}
/**
 * Pre-built props for the gallery backdrop element.
 */
export interface UseGalleryBackdropProps {
    /**
     * Data attribute for scope identification.
     */
    'data-scope': 'gallery';
    /**
     * Data attribute for part identification.
     */
    'data-part': 'backdrop';
}
/**
 * Pre-built props for the gallery content element.
 */
export interface UseGalleryContentProps {
    /**
     * Data attribute for scope identification.
     */
    'data-scope': 'gallery';
    /**
     * Data attribute for part identification.
     */
    'data-part': 'content';
}
/**
 * Pre-built props for a gallery action button element.
 */
export interface UseGalleryActionProps {
    /**
     * Data attribute for scope identification.
     */
    'data-scope': 'gallery';
    /**
     * Data attribute for part identification.
     */
    'data-part': string;
    /**
     * Data attribute for the action type.
     */
    'data-action': string;
    /**
     * Click handler to execute the action.
     */
    onClick: () => void;
}
/**
 * Pre-built props for the gallery fullscreen button element.
 */
export interface UseGalleryFullScreenProps extends UseGalleryActionProps {
    /**
     * Data attribute when the gallery is in fullscreen mode.
     */
    'data-fullscreen'?: '';
}
/**
 * Pre-built props for the gallery zoom-in button element.
 */
export interface UseGalleryZoomInProps extends UseGalleryActionProps {
    /**
     * Whether the button is disabled (when already zoomed in).
     */
    disabled?: boolean;
}
/**
 * Pre-built props for the gallery zoom-out button element.
 */
export interface UseGalleryZoomOutProps extends UseGalleryActionProps {
    /**
     * Whether the button is disabled (when not zoomed in).
     */
    disabled?: boolean;
}
/**
 * Pre-built props returned by the getItemProps getter.
 */
export interface UseGalleryGetItemProps {
    /**
     * Data attribute for scope identification.
     */
    'data-scope': 'gallery';
    /**
     * Data attribute for part identification.
     */
    'data-part': 'item';
    /**
     * Data attribute indicating whether the item is active.
     */
    'data-active'?: '';
}
/**
 * Defines the methods and properties exposed by useGallery.
 */
export interface UseGalleryExposes {
    /**
     * The state of the useGallery.
     */
    state: UseGalleryState;
    /**
     * The available actions for the gallery.
     */
    actions: useGalleryActions;
    /**
     * Ref to the content element.
     */
    contentRef: React.RefObject<HTMLElement | null>;
    /**
     * Ref to the thumbnail carousel element.
     */
    thumbnailRef: React.RefObject<CarouselRootInstance | null>;
    /**
     * Ref to the previous button element.
     */
    prevRef: React.RefObject<HTMLButtonElement | null>;
    /**
     * Ref to the next button element.
     */
    nextRef: React.RefObject<HTMLButtonElement | null>;
    /**
     * Registers an item and returns its assigned index plus an `unregister` callback to call on item unmount.
     * Pass null for first registration, or the existing index to re-register.
     * @param index - The existing index or null for first registration.
     * @returns The assigned index and an unregister callback that removes the item from the registry.
     */
    registerItem: (index: number | null) => {
        index: number;
        unregister: () => void;
    };
    /**
     * Navigates to the next item.
     * @returns void
     */
    handleNext: (originalEvent?: React.SyntheticEvent) => void;
    /**
     * Navigates to the previous item.
     * @returns void
     */
    handlePrev: (originalEvent?: React.SyntheticEvent) => void;
    /**
     * Selects a specific item by index.
     * @param index - The index of the item to select.
     * @returns void
     */
    selectItem: (index: number, originalEvent?: React.SyntheticEvent) => void;
    /**
     * Clears the pending action after it has been consumed by the active item.
     * @returns void
     */
    clearPendingAction: () => void;
    /**
     * Toggles the fullscreen state.
     * @returns void
     */
    toggleFullScreen: () => void;
    /**
     * Handles a click action dispatched from toolbar or action buttons.
     * @param action - The action to handle.
     * @returns void
     */
    handleClickAction: (action?: string) => void;
    /**
     * Reports the active item's transform state to the gallery.
     * @param itemState - The transform state of the active item.
     * @returns void
     */
    reportItemState: (itemState: {
        zoomed: boolean;
        rotated: boolean;
        flipped: boolean;
        scale: number;
        rotation: number;
        flip: {
            x: number;
            y: number;
        };
    }) => void;
    /**
     * Pre-built props for the root element.
     */
    rootProps: UseGalleryRootProps;
    /**
     * Pre-built props for the next button element.
     */
    nextProps: UseGalleryNextProps;
    /**
     * Pre-built props for the previous button element.
     */
    prevProps: UseGalleryPrevProps;
    /**
     * Pre-built props for the thumbnail carousel element.
     */
    thumbnailProps: UseGalleryThumbnailProps;
    /**
     * Pre-built props for the thumbnail content element.
     */
    thumbnailContentProps: UseGalleryThumbnailContentProps;
    /**
     * Returns pre-built props for a thumbnail item element.
     * @param index - The index of the thumbnail item.
     * @returns Pre-built thumbnail item props.
     */
    thumbnailItemProps: (index: number | undefined) => UseGalleryThumbnailItemProps;
    /**
     * Pre-built props for the toolbar element.
     */
    toolbarProps: UseGalleryToolbarProps;
    /**
     * Returns pre-built props for a toolbar item element.
     * @param action - The action string.
     * @returns Pre-built toolbar item props.
     */
    toolbarItemProps: (action: string | undefined) => UseGalleryToolbarItemProps;
    /**
     * Pre-built props for the header element.
     */
    headerProps: UseGalleryHeaderProps;
    /**
     * Pre-built props for the footer element.
     */
    footerProps: UseGalleryFooterProps;
    /**
     * Pre-built props for the backdrop element.
     */
    backdropProps: UseGalleryBackdropProps;
    /**
     * Pre-built props for the content element.
     */
    contentProps: UseGalleryContentProps;
    /**
     * Pre-built props for the download action button.
     */
    downloadProps: UseGalleryActionProps;
    /**
     * Pre-built props for the flip-x action button.
     */
    flipXProps: UseGalleryActionProps;
    /**
     * Pre-built props for the flip-y action button.
     */
    flipYProps: UseGalleryActionProps;
    /**
     * Pre-built props for the rotate-left action button.
     */
    rotateLeftProps: UseGalleryActionProps;
    /**
     * Pre-built props for the rotate-right action button.
     */
    rotateRightProps: UseGalleryActionProps;
    /**
     * Pre-built props for the fullscreen toggle button.
     */
    fullScreenProps: UseGalleryFullScreenProps;
    /**
     * Pre-built props for the zoom-in action button.
     */
    zoomInProps: UseGalleryZoomInProps;
    /**
     * Pre-built props for the zoom-out action button.
     */
    zoomOutProps: UseGalleryZoomOutProps;
    /**
     * Pre-built props for the zoom-toggle action button.
     */
    zoomToggleProps: UseGalleryActionProps;
    /**
     * Returns pre-built props for an item element at the given index.
     * @param index - The index of the item.
     * @returns Pre-built item props.
     */
    getItemProps: (index: number) => UseGalleryGetItemProps;
}
/**
 * Instance of useGallery headless.
 */
export type UseGalleryInstance = HeadlessInstance<UseGalleryProps, UseGalleryState, UseGalleryExposes>;
export {};
