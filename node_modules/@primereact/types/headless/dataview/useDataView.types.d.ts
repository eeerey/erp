/**
 *
 * The useDataView manages the state and functionality of a dataview component.
 *
 * [Live Demo](https://www.primereact.org/dataview/)
 *
 * @module useDataView
 * @group headless
 *
 */
import type { HeadlessInstance } from '@primereact/types/core';
import * as React from 'react';
/**
 * Defines the event object for layout change events.
 */
export interface UseDataViewLayoutChangeEvent {
    /**
     * Browser event that triggered the layout change.
     */
    originalEvent: React.SyntheticEvent | undefined;
    /**
     * The new layout value.
     */
    value: string;
}
/**
 * Defines valid properties in useDataView.
 */
export interface UseDataViewProps {
    /**
     * The controlled layout value.
     */
    layout?: string;
    /**
     * The default layout value for uncontrolled mode.
     */
    defaultLayout?: string;
    /**
     * Callback invoked when the layout changes.
     */
    onLayoutChange?: (event: UseDataViewLayoutChangeEvent) => void;
}
/**
 * Defines valid state in useDataView.
 */
export interface UseDataViewState {
    /**
     * The current layout value.
     */
    layout: string | undefined;
}
/**
 * Data attribute props for the DataView root element.
 */
export interface UseDataViewRootProps {
    /**
     * The current layout mode of the data view.
     */
    'data-layout'?: string;
}
/**
 * Data attribute props for the DataView content element.
 */
export interface UseDataViewContentProps {
    'data-scope': 'dataview';
    'data-part': 'content';
}
/**
 * Data attribute props for the DataView header element.
 */
export interface UseDataViewHeaderProps {
    'data-scope': 'dataview';
    'data-part': 'header';
}
/**
 * Data attribute props for the DataView footer element.
 */
export interface UseDataViewFooterProps {
    'data-scope': 'dataview';
    'data-part': 'footer';
}
/**
 * Data attribute props for the DataView empty element.
 */
export interface UseDataViewEmptyProps {
    'data-scope': 'dataview';
    'data-part': 'empty';
}
/**
 * Defines the methods and properties exposed by useDataView.
 */
export interface UseDataViewExposes {
    /**
     * Sorts the data based on the specified field and order.
     * @param value The data array to sort.
     * @param sortField The field name to sort by.
     * @param sortOrder The sort order (1 for ascending, -1 for descending).
     * @returns The sorted data array or null if no data is provided.
     */
    sort: (value: unknown[] | null, sortField: string, sortOrder: number) => unknown[] | null;
    /**
     * Pre-built data attribute props for the root element.
     * Includes data-scope, data-part, and data-layout.
     */
    rootProps: UseDataViewRootProps;
    /**
     * Pre-built data attribute props for the content element.
     */
    contentProps: UseDataViewContentProps;
    /**
     * Pre-built data attribute props for the header element.
     */
    headerProps: UseDataViewHeaderProps;
    /**
     * Pre-built data attribute props for the footer element.
     */
    footerProps: UseDataViewFooterProps;
    /**
     * Pre-built data attribute props for the empty element.
     */
    emptyProps: UseDataViewEmptyProps;
}
/**
 * Instance of useDataView headless.
 */
export type UseDataViewInstance = HeadlessInstance<UseDataViewProps, UseDataViewState, UseDataViewExposes>;
