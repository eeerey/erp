/**
 *
 * The useScrollArea manages the state and functionality of a scrollarea component.
 *
 * [Live Demo](https://www.primereact.org/scrollarea/)
 *
 * @module useScrollArea
 * @group headless
 *
 */
import type { HeadlessInstance } from '@primereact/types/core';
/**
 * Defines valid properties in useScrollArea.
 */
export interface UseScrollAreaProps {
    /**
     * Step factor to scroll the content while pressing the arrow keys.
     * @default 5
     */
    step?: number | undefined;
    /**
     * Controls scrollbar visibility behavior.
     * @default 'auto'
     */
    variant?: 'auto' | 'hover' | 'scroll' | 'always' | 'hidden';
    /**
     * When enabled, a fade mask is applied to the viewport edges to indicate overflow.
     * @default false
     */
    mask?: boolean;
    /**
     * Tab index for the viewport element to make it keyboard focusable.
     * @default 0
     */
    tabIndex?: number;
}
/**
 * Defines the hidden state of scrollbar axes.
 */
export interface UseScrollAreaHiddenState {
    /**
     * Whether the horizontal scrollbar is hidden (no horizontal overflow).
     */
    x: boolean;
    /**
     * Whether the vertical scrollbar is hidden (no vertical overflow).
     */
    y: boolean;
    /**
     * Whether the corner element is hidden (at least one axis has no overflow).
     */
    corner: boolean;
}
/**
 * Defines valid state in useScrollArea.
 */
export interface UseScrollAreaState {
    /**
     * Whether the pointer is hovering over the scroll area.
     */
    hovering: boolean;
    /**
     * Whether the content is actively scrolling horizontally.
     */
    scrollingX: boolean;
    /**
     * Whether the content is actively scrolling vertically.
     */
    scrollingY: boolean;
    /**
     * Whether the content is actively scrolling in any direction.
     */
    scrolling: boolean;
    /**
     * Whether touch input modality is active.
     */
    touchModality: boolean;
    /**
     * Whether the content overflows the viewport horizontally.
     */
    hasOverflowX: boolean;
    /**
     * Whether the content overflows the viewport vertically.
     */
    hasOverflowY: boolean;
    /**
     * Whether the viewport is scrolled past the left edge.
     */
    scrollXBefore: boolean;
    /**
     * Whether content remains to the right of the viewport.
     */
    scrollXAfter: boolean;
    /**
     * Whether the viewport is scrolled past the top edge.
     */
    scrollYBefore: boolean;
    /**
     * Whether content remains below the viewport.
     */
    scrollYAfter: boolean;
    /**
     * Whether the corner element is hidden.
     */
    cornerHidden: boolean;
}
/**
 * Pre-built props for the root element.
 */
export interface UseScrollAreaRootProps {
    /**
     * Component scope identifier.
     */
    'data-scope': 'scrollarea';
    /**
     * Component part identifier.
     */
    'data-part': 'root';
    /**
     * Ref to the root DOM element.
     */
    ref: React.RefObject<HTMLDivElement | null>;
    /**
     * ARIA role for the root element.
     */
    role: 'presentation';
    /**
     * Pointer enter event handler for hover detection.
     */
    onPointerEnter: (event: React.PointerEvent<HTMLDivElement>) => void;
    /**
     * Pointer leave event handler for hover detection.
     */
    onPointerLeave: () => void;
    /**
     * Pointer down event handler for touch modality detection.
     */
    onPointerDown: (event: React.PointerEvent<HTMLDivElement>) => void;
    /**
     * Current scrollbar visibility variant.
     */
    'data-variant'?: string;
    /**
     * Present when the mask prop is enabled.
     */
    'data-mask'?: '';
    /**
     * Present when the content is actively scrolling.
     */
    'data-scrolling'?: '';
    /**
     * Present when the content overflows horizontally.
     */
    'data-has-overflow-x'?: '';
    /**
     * Present when the content overflows vertically.
     */
    'data-has-overflow-y'?: '';
    /**
     * Present when the viewport is scrolled past the left edge.
     */
    'data-scroll-x-before'?: '';
    /**
     * Present when content remains to the right of the viewport.
     */
    'data-scroll-x-after'?: '';
    /**
     * Present when the viewport is scrolled past the top edge.
     */
    'data-scroll-y-before'?: '';
    /**
     * Present when content remains below the viewport.
     */
    'data-scroll-y-after'?: '';
    /**
     * Inline style with position and CSS custom properties for corner dimensions.
     */
    style: React.CSSProperties;
}
/**
 * Pre-built props for the viewport element.
 */
export interface UseScrollAreaViewportProps {
    /**
     * Component scope identifier.
     */
    'data-scope': 'scrollarea';
    /**
     * Component part identifier.
     */
    'data-part': 'viewport';
    /**
     * Ref to the viewport DOM element.
     */
    ref: React.RefObject<HTMLDivElement | null>;
    /**
     * Tab index for keyboard navigation. Undefined when there is no overflow.
     */
    tabIndex: number | undefined;
    /**
     * Scroll event handler that updates handle position and scroll state.
     */
    onScroll: () => void;
    /**
     * Inline style for native scrollbar suppression and overflow.
     */
    style: React.CSSProperties;
    /**
     * Present when the content is actively scrolling.
     */
    'data-scrolling'?: '';
    /**
     * Present when the content overflows horizontally.
     */
    'data-has-overflow-x'?: '';
    /**
     * Present when the content overflows vertically.
     */
    'data-has-overflow-y'?: '';
    /**
     * Present when the viewport is scrolled past the left edge.
     */
    'data-scroll-x-before'?: '';
    /**
     * Present when content remains to the right of the viewport.
     */
    'data-scroll-x-after'?: '';
    /**
     * Present when the viewport is scrolled past the top edge.
     */
    'data-scroll-y-before'?: '';
    /**
     * Present when content remains below the viewport.
     */
    'data-scroll-y-after'?: '';
}
/**
 * Pre-built props for the content element.
 */
export interface UseScrollAreaContentProps {
    /**
     * Component scope identifier.
     */
    'data-scope': 'scrollarea';
    /**
     * Component part identifier.
     */
    'data-part': 'content';
    /**
     * Inline style with min-width / min-height for overflowing content.
     */
    style: React.CSSProperties;
    /**
     * Present when the content is actively scrolling.
     */
    'data-scrolling'?: '';
    /**
     * Present when the content overflows horizontally.
     */
    'data-has-overflow-x'?: '';
    /**
     * Present when the content overflows vertically.
     */
    'data-has-overflow-y'?: '';
    /**
     * Present when the viewport is scrolled past the left edge.
     */
    'data-scroll-x-before'?: '';
    /**
     * Present when content remains to the right of the viewport.
     */
    'data-scroll-x-after'?: '';
    /**
     * Present when the viewport is scrolled past the top edge.
     */
    'data-scroll-y-before'?: '';
    /**
     * Present when content remains below the viewport.
     */
    'data-scroll-y-after'?: '';
}
/**
 * Pre-built props for a scrollbar element.
 */
export interface UseScrollAreaScrollbarProps {
    /**
     * Component scope identifier.
     */
    'data-scope': 'scrollarea';
    /**
     * Component part identifier based on orientation.
     */
    'data-part': 'scrollbar-y' | 'scrollbar-x';
    /**
     * Scrollbar orientation.
     */
    'data-orientation': 'vertical' | 'horizontal';
    /**
     * Ref to the scrollbar DOM element.
     */
    ref: React.RefObject<HTMLDivElement | null>;
    /**
     * Pointer down event handler for track click scrolling.
     */
    onPointerDown: (event: React.PointerEvent<HTMLDivElement>) => void;
    /**
     * Pointer move event handler for drag scrolling.
     */
    onPointerMove: (event: React.PointerEvent<HTMLDivElement>) => void;
    /**
     * Pointer up event handler that ends drag scrolling.
     */
    onPointerUp: (event: React.PointerEvent<HTMLDivElement>) => void;
    /**
     * Wheel event handler for scrolling via mouse wheel on the scrollbar.
     */
    onWheel: (event: React.WheelEvent<HTMLDivElement>) => void;
    /**
     * Inline style with absolute positioning and pointer/selection behavior.
     */
    style: React.CSSProperties;
    /**
     * Present when the pointer is hovering over the scroll area.
     */
    'data-hovering'?: '';
    /**
     * Present when the content is actively scrolling.
     */
    'data-scrolling'?: '';
    /**
     * Present when the content overflows horizontally.
     */
    'data-has-overflow-x'?: '';
    /**
     * Present when the content overflows vertically.
     */
    'data-has-overflow-y'?: '';
    /**
     * Present when the viewport is scrolled past the left edge.
     */
    'data-scroll-x-before'?: '';
    /**
     * Present when content remains to the right of the viewport.
     */
    'data-scroll-x-after'?: '';
    /**
     * Present when the viewport is scrolled past the top edge.
     */
    'data-scroll-y-before'?: '';
    /**
     * Present when content remains below the viewport.
     */
    'data-scroll-y-after'?: '';
}
/**
 * Pre-built props for a handle element.
 */
export interface UseScrollAreaHandleProps {
    /**
     * Component scope identifier.
     */
    'data-scope': 'scrollarea';
    /**
     * Component part identifier based on orientation.
     */
    'data-part': 'handle-y' | 'handle-x';
    /**
     * Handle orientation.
     */
    'data-orientation': 'vertical' | 'horizontal';
    /**
     * Ref to the handle DOM element.
     */
    ref: React.RefObject<HTMLDivElement | null>;
    /**
     * Pointer down event handler that initiates handle dragging.
     */
    onPointerDown: (event: React.PointerEvent<HTMLDivElement>) => void;
    /**
     * Inline style with size and transform driven by CSS custom properties.
     */
    style: React.CSSProperties;
}
/**
 * Pre-built props for the corner element.
 */
export interface UseScrollAreaCornerProps {
    /**
     * Component scope identifier.
     */
    'data-scope': 'scrollarea';
    /**
     * Component part identifier.
     */
    'data-part': 'corner';
    /**
     * Ref to the corner DOM element.
     */
    ref: React.RefObject<HTMLDivElement | null>;
    /**
     * Inline style with absolute positioning at the scrollbar intersection.
     */
    style: React.CSSProperties;
}
/**
 * Defines the methods and properties exposed by useScrollArea.
 */
export interface UseScrollAreaExposes {
    /**
     * The reactive state of the scroll area.
     */
    state: UseScrollAreaState;
    /**
     * Measured dimensions of the corner area where both scrollbars intersect.
     */
    cornerSize: {
        width: number;
        height: number;
    };
    /**
     * Boolean flags indicating whether each scrollbar and the corner should be rendered.
     */
    hiddenState: UseScrollAreaHiddenState;
    /**
     * Pre-built props for the root element.
     * Includes ref callback, ARIA role, pointer event handlers, data attributes, and corner CSS variables.
     */
    rootProps: UseScrollAreaRootProps;
    /**
     * Pre-built props for the viewport element.
     * Includes ref callback, tabIndex, scroll handler, and data attributes.
     */
    viewportProps: UseScrollAreaViewportProps;
    /**
     * Pre-built props for the content element.
     * Includes data attributes for scroll position and overflow state.
     */
    contentProps: UseScrollAreaContentProps;
    /**
     * Pre-built props for the vertical scrollbar element.
     */
    scrollbarYProps: UseScrollAreaScrollbarProps;
    /**
     * Pre-built props for the horizontal scrollbar element.
     */
    scrollbarXProps: UseScrollAreaScrollbarProps;
    /**
     * Pre-built props for the vertical handle element.
     */
    handleYProps: UseScrollAreaHandleProps;
    /**
     * Pre-built props for the horizontal handle element.
     */
    handleXProps: UseScrollAreaHandleProps;
    /**
     * Pre-built props for the corner element.
     * Includes ref callback and data attributes.
     */
    cornerProps: UseScrollAreaCornerProps;
    /**
     * Recalculates handle size and position based on current scroll state.
     */
    computeHandle: () => void;
    /**
     * Handles scroll events to update handle offset and scroll position state.
     */
    handleScroll: () => void;
    /**
     * Handles pointer down on the scrollbar track to jump-scroll to the clicked position.
     */
    handleScrollbarPointerDown: (event: React.PointerEvent<HTMLDivElement>, orientation: 'vertical' | 'horizontal') => void;
    /**
     * Handles pointer move during scrollbar drag interaction.
     */
    handleScrollbarPointerMove: (event: React.PointerEvent<HTMLDivElement>) => void;
    /**
     * Handles pointer up to end scrollbar drag interaction.
     */
    handleScrollbarPointerUp: (event: React.PointerEvent<HTMLDivElement>) => void;
    /**
     * Handles wheel events on the scrollbar element.
     */
    handleScrollbarWheel: (event: React.WheelEvent<HTMLDivElement>, orientation: 'vertical' | 'horizontal') => void;
    /**
     * Handles pointer down on the handle to initiate drag scrolling.
     */
    handleHandlePointerDown: (event: React.PointerEvent<HTMLDivElement>, orientation: 'vertical' | 'horizontal') => void;
    /**
     * Handles pointer enter on the root element for hover state tracking.
     */
    onRootPointerEnter: (event: React.PointerEvent<HTMLDivElement>) => void;
    /**
     * Handles pointer leave on the root element for hover state tracking.
     */
    onRootPointerLeave: () => void;
    /**
     * Handles pointer down on the root element for touch modality detection.
     */
    onRootPointerDown: (event: React.PointerEvent<HTMLDivElement>) => void;
}
/**
 * Instance of useScrollArea headless.
 */
export type UseScrollAreaInstance = HeadlessInstance<UseScrollAreaProps, UseScrollAreaState, UseScrollAreaExposes>;
