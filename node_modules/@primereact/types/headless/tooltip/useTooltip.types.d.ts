/**
 *
 * The useTooltip manages the state and functionality of a tooltip component.
 *
 * [Live Demo](https://www.primereact.org/tooltip/)
 *
 * @module useTooltip
 * @group headless
 *
 */
import type { HeadlessInstance } from '@primereact/types/core';
import type * as React from 'react';
import type { UseTooltipManagerExposes } from './useTooltipManager.types';
/**
 * The type of instant transition.
 * - 'delay': Opening during instant phase (adjacent tooltip transition)
 * - 'focus': Opening via focus
 * - 'dismiss': Closing via escape/press
 * - undefined: Normal hover transition
 */
export type InstantType = 'delay' | 'dismiss' | 'focus' | undefined;
export interface UseTooltipChangeEvent {
    /**
     * The open value of the tooltip.
     */
    value: boolean | undefined;
}
/**
 * Defines valid properties in useTooltip.
 */
export interface UseTooltipProps {
    /**
     * External anchor element for positioning the tooltip programmatically. When provided, it is
     * used instead of the element attached via `Tooltip.Trigger`.
     * @default undefined
     */
    anchor?: HTMLElement | null | undefined;
    /**
     * Controls the open state of the tooltip.
     */
    open?: boolean | undefined;
    /**
     * Whether the tooltip is open by default.
     * @default false
     */
    defaultOpen?: boolean | undefined;
    /**
     * Callback fired when the open state changes.
     * @default undefined
     */
    onOpenChange?: (event: UseTooltipChangeEvent) => void;
    /**
     * Callback fired after the leave (exit) transition completes.
     * @default undefined
     */
    onExitComplete?: () => void;
    /**
     * Whether the tooltip is disabled.
     * @default false
     */
    disabled?: boolean | undefined;
    /**
     * The delay in milliseconds before opening the tooltip on hover.
     * @default 600
     */
    openDelay?: number | undefined;
    /**
     * The delay in milliseconds before closing the tooltip.
     * @default 0
     */
    closeDelay?: number | undefined;
    /**
     * Whether the tooltip popup is interactive (hoverable).
     * When true, hovering over the popup keeps the tooltip open.
     * When false, the tooltip closes as soon as the pointer leaves the trigger.
     * @default true
     */
    interactive?: boolean | undefined;
    /**
     * The tooltip manager instance for group behavior.
     */
    tooltipManager?: UseTooltipManagerExposes | undefined;
}
/**
 * Defines valid state in useTooltip.
 */
export interface UseTooltipState {
    /**
     * Whether the tooltip is open.
     */
    open: boolean | undefined;
    /**
     * Whether the tooltip is currently rendered in the DOM (may still be playing exit animation
     * when `open` is false).
     */
    rendered: boolean;
    /**
     * The anchor element.
     */
    anchorElement: HTMLElement | null;
    /**
     * The positioner element.
     */
    positionerElement: HTMLDivElement | null;
    /**
     * The popup element.
     */
    popupElement: HTMLElement | null;
    /**
     * The arrow element.
     */
    arrowElement: HTMLDivElement | null;
    /**
     * The type of instant transition, if any.
     */
    instantType: InstantType;
}
/**
 * Pre-built props for the tooltip root element.
 */
export interface UseTooltipRootProps {
    /**
     * Instant transition type attribute.
     */
    'data-instant'?: string;
}
/**
 * Pre-built props for the tooltip trigger element.
 */
export interface UseTooltipTriggerProps {
    /**
     * Ref callback for the trigger element.
     */
    ref: (node: HTMLElement | null) => void;
    /**
     * Component scope identifier.
     */
    'data-scope': string;
    /**
     * Component part identifier.
     */
    'data-part': string;
    /**
     * Present when the tooltip is open.
     */
    'data-popup-open'?: string;
}
/**
 * Pre-built props for the tooltip positioner element.
 */
export interface UseTooltipPositionerProps {
    /**
     * Ref callback for the positioner element.
     */
    ref: (node: HTMLDivElement | null) => void;
    /**
     * Component scope identifier.
     */
    'data-scope': string;
    /**
     * Component part identifier.
     */
    'data-part': string;
    /**
     * Present when the tooltip is open.
     */
    'data-open'?: string;
}
/**
 * Pre-built props for the tooltip popup element.
 */
export interface UseTooltipPopupProps {
    /**
     * Ref callback for the popup element.
     */
    ref: (node: HTMLDivElement | null) => void;
    /**
     * Component scope identifier.
     */
    'data-scope': string;
    /**
     * Component part identifier.
     */
    'data-part': string;
    /**
     * Present when the tooltip popup is visible.
     */
    'data-open'?: string;
    /**
     * Present when an instant transition is active.
     */
    'data-instant'?: string;
    /**
     * The type of instant transition.
     */
    'data-instant-type': InstantType;
}
/**
 * Pre-built props for the tooltip arrow element.
 */
export interface UseTooltipArrowProps {
    /**
     * Ref callback for the arrow element.
     */
    ref: (node: HTMLDivElement | null) => void;
    /**
     * Component scope identifier.
     */
    'data-scope': string;
    /**
     * Component part identifier.
     */
    'data-part': string;
    /**
     * Present when the tooltip popup is visible.
     */
    'data-open'?: string;
    /**
     * Instant transition type attribute.
     */
    'data-instant'?: string;
}
/**
 * Defines the methods and properties exposed by useTooltip.
 */
export interface UseTooltipExposes {
    /**
     * Current state of the tooltip.
     */
    state: UseTooltipState;
    /**
     * Ref pointing to the popup element. Used by the motion system to apply enter/leave classes
     * synchronously before the first paint, avoiding a flash of the default (visible) state.
     */
    popupRef: React.RefObject<HTMLElement | null>;
    /**
     * Pre-built props for the root element.
     */
    rootProps: UseTooltipRootProps;
    /**
     * Pre-built props for the trigger element.
     */
    triggerProps: UseTooltipTriggerProps;
    /**
     * Pre-built props for the positioner element.
     */
    positionerProps: UseTooltipPositionerProps;
    /**
     * Pre-built props for the popup element.
     */
    popupProps: UseTooltipPopupProps;
    /**
     * Pre-built props for the arrow element.
     */
    arrowProps: UseTooltipArrowProps;
    /**
     * Sets the rendered state of the tooltip. Called by the motion system when the exit
     * animation finishes to unmount the popup.
     */
    setRendered: (rendered: boolean) => void;
    /**
     * Callback invoked after the enter animation completes.
     */
    onOpenComplete: () => void;
    /**
     * Callback invoked after the leave animation completes. Fires `onExitComplete`.
     */
    onCloseComplete: () => void;
}
/**
 * Instance of useTooltip headless.
 */
export type UseTooltipInstance = HeadlessInstance<UseTooltipProps, UseTooltipState, UseTooltipExposes>;
