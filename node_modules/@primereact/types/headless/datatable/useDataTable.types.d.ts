/**
 *
 * The useDataTable manages the state and functionality of a DataTable component.
 *
 * [Live Demo](https://www.primereact.org/datatable/)
 *
 * @module useDataTable
 * @group headless
 *
 */
import type { HeadlessInstance } from '@primereact/types/core';
/**
 * Sort order type for DataTable columns.
 * 1 = ascending, -1 = descending, 0 = unsorted.
 */
export type SortOrder = 1 | -1 | 0;
/**
 * Defines the metadata for a sorted column.
 */
export interface SortMeta {
    /**
     * The field name of the sorted column.
     */
    field: string;
    /**
     * The sort order of the column.
     */
    order: SortOrder;
}
/**
 * Event object for DataTable sort changes.
 */
export interface UseDataTableSortEvent {
    /**
     * The original browser event that triggered the sort.
     */
    originalEvent: React.SyntheticEvent;
    /**
     * The field name that was sorted.
     */
    field: string;
    /**
     * The new sort order.
     */
    order: SortOrder;
    /**
     * Multi-sort metadata when sorting by multiple columns.
     */
    multiSortMeta?: SortMeta[];
}
/**
 * Represents the selection keys as a record of string keys to boolean values.
 */
export type SelectionKeys = Record<string, boolean>;
/**
 * Defines the expanded keys map for row expansion.
 */
export type ExpandedKeys = Record<string, boolean>;
/**
 * Defines the event object for row expansion changes.
 */
export interface UseDataTableExpansionEvent {
    /**
     * Browser event that triggered the change.
     */
    originalEvent: React.SyntheticEvent;
    /**
     * The new expanded keys map.
     */
    value: ExpandedKeys;
}
/**
 * Defines the metadata for row grouping.
 */
export interface RowGroupMeta {
    /**
     * Whether this row is the start of a new group.
     */
    isGroupStart: boolean;
    /**
     * Whether this row is the end of a group.
     */
    isGroupEnd: boolean;
    /**
     * The value of the group field for this row.
     */
    groupValue: unknown;
    /**
     * The number of rows in this group.
     */
    groupCount: number;
    /**
     * The index of this group (0-based).
     */
    groupIndex: number;
}
/**
 * Metadata injected onto each flattened row when the DataTable runs in `treeMode`.
 * These `_tree*` fields are added by the tree-flattening step and are available on the
 * `item` passed to the body render-prop; intersect your row model with this to read them
 * type-safely, e.g. `({ item }: { item: MyRow & DataTableTreeRowMeta }) => ...`.
 */
export interface DataTableTreeRowMeta {
    /**
     * Depth of the row in the tree (0 for roots).
     */
    _treeLevel?: number;
    /**
     * Whether the node has children and can be expanded.
     */
    _treeHasChildren?: boolean;
    /**
     * Whether the node is a leaf (no children).
     */
    _treeLeaf?: boolean;
    /**
     * Key of the parent node, if any.
     */
    _treeParentKey?: string | number;
    /**
     * 1-based position of the node among its siblings (for `aria-posinset`).
     */
    _treePosInSet?: number;
    /**
     * Number of siblings in the node's set (for `aria-setsize`).
     */
    _treeSetSize?: number;
}
/**
 * Column resize mode.
 * 'fit' adjusts adjacent column width to maintain total table width.
 * 'expand' increases the total table width.
 */
export type ColumnResizeMode = 'fit' | 'expand';
/**
 * Event object for column resize end.
 */
export interface UseDataTableColumnResizeEvent {
    /**
     * The original browser event.
     */
    originalEvent: MouseEvent | TouchEvent;
    /**
     * The column index that was resized.
     */
    columnIndex: number;
    /**
     * The pixel delta of the resize.
     */
    delta: number;
}
/**
 * Edit mode for DataTable cells/rows.
 */
export type DataTableEditMode = 'cell' | 'row';
/**
 * Editing keys map for tracking which rows are being edited.
 */
export type EditingKeys = Record<string, boolean>;
/**
 * Event object for cell edit completion.
 */
export interface UseDataTableCellEditEvent {
    /**
     * The original browser event.
     */
    originalEvent: React.SyntheticEvent;
    /**
     * The row data being edited.
     */
    rowData: Record<string, unknown>;
    /**
     * The new row data after editing.
     */
    newRowData: Record<string, unknown>;
    /**
     * The field name of the cell.
     */
    field: string;
    /**
     * The original cell value.
     */
    value: unknown;
    /**
     * The new cell value.
     */
    newValue: unknown;
    /**
     * The row index.
     */
    rowIndex: number;
    /**
     * The type of action that completed the edit.
     */
    type: 'enter' | 'tab' | 'outside' | 'escape';
    /**
     * Call to prevent the edit from completing (keep cell in edit mode).
     */
    preventDefault: () => void;
}
/**
 * Event object for row edit operations.
 */
export interface UseDataTableRowEditEvent {
    /**
     * The original browser event.
     */
    originalEvent: React.SyntheticEvent;
    /**
     * The row data.
     */
    data: Record<string, unknown>;
    /**
     * The new row data (for save operations).
     */
    newData?: Record<string, unknown>;
    /**
     * The row index.
     */
    index: number;
}
/**
 * Event object for editing keys change.
 */
export interface UseDataTableEditingEvent {
    /**
     * The original browser event.
     */
    originalEvent: React.SyntheticEvent;
    /**
     * The new editing keys map.
     */
    value: EditingKeys;
}
/**
 * Filter match mode types supported by DataTable.
 */
export type FilterMatchMode = 'contains' | 'notContains' | 'startsWith' | 'endsWith' | 'equals' | 'notEquals' | 'in' | 'between' | 'lt' | 'lte' | 'gt' | 'gte' | 'dateIs' | 'dateIsNot' | 'dateBefore' | 'dateAfter' | 'custom';
/**
 * Defines filter constraint for a column.
 */
export interface FilterConstraint {
    value: unknown;
    matchMode: FilterMatchMode;
}
/**
 * Defines filter metadata for a column.
 */
export interface ColumnFilterMeta {
    value?: unknown;
    matchMode?: FilterMatchMode;
    constraints?: FilterConstraint[];
    operator?: 'and' | 'or';
}
/**
 * Defines the complete filter metadata map.
 */
export type DataTableFilterMeta = Record<string, ColumnFilterMeta | unknown>;
/**
 * Event fired when filters change.
 */
export interface UseDataTableFilterEvent {
    originalEvent: React.SyntheticEvent;
    filters: DataTableFilterMeta;
}
/**
 * Defines filter-related methods exposed by useDataTable.
 */
export interface UseDataTableFilterExposes {
    /**
     * Sets the filter for a specific field.
     */
    setFieldFilter: (event: React.SyntheticEvent, field: string, meta: ColumnFilterMeta) => void;
    /**
     * Clears the filter for a specific field.
     */
    clearFieldFilter: (event: React.SyntheticEvent, field: string) => void;
    /**
     * Clears all filters.
     */
    clearAllFilters: (event: React.SyntheticEvent) => void;
    /**
     * Adds a new constraint to a field's filter.
     */
    addConstraint: (event: React.SyntheticEvent, field: string, defaultMatchMode?: FilterMatchMode) => void;
    /**
     * Removes a constraint from a field's filter by index.
     */
    removeConstraint: (event: React.SyntheticEvent, field: string, index: number) => void;
    /**
     * Sets the operator (and/or) for a field's filter.
     */
    setOperator: (event: React.SyntheticEvent, field: string, operator: 'and' | 'or') => void;
    /**
     * Updates a specific constraint's value and optionally its matchMode.
     */
    setConstraintValue: (event: React.SyntheticEvent, field: string, index: number, value: unknown, matchMode?: FilterMatchMode) => void;
    /**
     * Changes the match mode of a specific constraint.
     */
    setConstraintMatchMode: (event: React.SyntheticEvent, field: string, index: number, matchMode: FilterMatchMode) => void;
    /**
     * Checks if a field has an active filter.
     */
    hasFilterValue: (field: string) => boolean;
    /**
     * Checks if any filter is active.
     */
    isFiltered: () => boolean;
    /**
     * Gets the complete filter metadata for a field.
     */
    getFieldFilterMeta: (field: string) => ColumnFilterMeta;
    /**
     * Returns base data-attrs for a `DataTable.Filter` root element.
     */
    getFilterRootProps: (field: string, display?: 'row' | 'menu') => Record<string, any>;
}
/**
 * Event payload for row click / double-click / context-menu callbacks.
 */
export interface UseDataTableRowMouseEvent {
    /**
     * Original browser mouse event.
     */
    originalEvent: React.MouseEvent;
    /**
     * Row data object.
     */
    data: Record<string, unknown>;
    /**
     * Zero-based row index within the current render.
     */
    index: number;
}
/**
 * Event object for row reorder.
 */
export interface UseDataTableRowReorderEvent {
    /**
     * The original browser event.
     */
    originalEvent: DragEvent;
    /**
     * The index of the dragged row.
     */
    dragIndex: number;
    /**
     * The index of the drop target row.
     */
    dropIndex: number;
    /**
     * The reordered data array.
     */
    value: Record<string, unknown>[];
}
/**
 * Defines row-reorder-related methods exposed by useDataTable.
 */
export interface UseDataTableRowReorderExposes {
    /**
     * Handles drag start on a row.
     */
    onDragStart: (event: React.DragEvent, rowIndex: number) => void;
    /**
     * Handles drag over on a row.
     */
    onDragOver: (event: React.DragEvent, rowIndex: number) => void;
    /**
     * Handles drag enter on a row (alias of `onDragOver`).
     */
    onDragEnter: (event: React.DragEvent, rowIndex: number) => void;
    /**
     * Handles drag leave on a row — clears drop-point markers.
     */
    onDragLeave: (event: React.DragEvent) => void;
    /**
     * Handles drop on a row.
     */
    onDrop: (event: React.DragEvent) => void;
    /**
     * Handles drag end.
     */
    onDragEnd: (event?: React.DragEvent) => void;
    /**
     * Returns props for the row reorder drag-handle element.
     */
    getRowReorderHandleProps: () => Record<string, any>;
}
/**
 * Options for CSV export.
 */
export interface DataTableExportOptions {
    selectionOnly?: boolean;
    fields?: string[];
    headers?: Record<string, string>;
    separator?: string;
    fileName?: string;
    /**
     * Custom export function. When provided, it is called instead of the default CSV download.
     * Receives the resolved data array and fields list.
     */
    exportFunction?: (options: {
        data: Record<string, unknown>[];
        fields: string[];
    }) => void;
}
/**
 * Defines export-related methods exposed by useDataTable.
 */
export interface UseDataTableExportExposes {
    /**
     * Exports data to CSV.
     */
    exportCSV: (options?: DataTableExportOptions) => void;
}
/**
 * Event fired when lazy loading is triggered.
 */
export interface UseDataTableLazyLoadEvent {
    originalEvent?: React.SyntheticEvent;
    first: number;
    rows: number;
    page: number;
    sortField?: string;
    sortOrder?: SortOrder;
    multiSortMeta?: SortMeta[];
    filters?: DataTableFilterMeta;
    globalFilter?: string | null;
}
/**
 * Event object for column reorder.
 */
export interface UseDataTableColumnReorderEvent {
    /**
     * The original browser event.
     */
    originalEvent: DragEvent;
    /**
     * The index of the column being dragged.
     */
    dragIndex: number;
    /**
     * The index of the column being dropped on.
     */
    dropIndex: number;
}
/**
 * Defines column-reorder-related methods exposed by useDataTable.
 */
export interface UseDataTableColumnReorderExposes {
    /**
     * Handles drag start on a column header.
     */
    onDragStart: (event: React.DragEvent, columnIndex: number) => void;
    /**
     * Handles drag over on a column header.
     */
    onDragOver: (event: React.DragEvent, columnIndex: number) => void;
    /**
     * Handles drag enter on a column header.
     */
    onDragEnter: (event: React.DragEvent, columnIndex: number) => void;
    /**
     * Handles drag leave on a column header.
     */
    onDragLeave: (event: React.DragEvent) => void;
    /**
     * Handles drop on a column header.
     */
    onDrop: (event: React.DragEvent, columnIndex: number) => void;
    /**
     * Handles drag end.
     */
    onDragEnd: (event: React.DragEvent) => void;
    /**
     * Registers a drop-target for a specific column index. The hook calls `notify` with `{ side }`
     * when this column becomes the active drop target and with `null` when it stops being active.
     * Returns an unregister function to call on unmount.
     */
    registerDropTarget: (columnIndex: number, notify: (state: {
        side: 'start' | 'end';
    } | null) => void) => () => void;
    /**
     * Returns props for the column reorder drag-handle element.
     */
    getColumnReorderHandleProps: () => Record<string, any>;
    /**
     * Returns props for the column reorder up-indicator element.
     */
    getColumnReorderIndicatorUpProps: () => Record<string, any>;
    /**
     * Returns props for the column reorder down-indicator element.
     */
    getColumnReorderIndicatorDownProps: () => Record<string, any>;
}
/**
 * Defines column-resize-related methods exposed by useDataTable.
 */
export interface UseDataTableColumnResizeExposes {
    /**
     * Handles mouse down on the column resize handle.
     * @param event The mouse event.
     * @param columnIndex The index of the column being resized.
     * @param headerCell The header cell DOM element.
     */
    onResizeStart: (event: React.PointerEvent, columnIndex: number, headerCell: HTMLElement) => void;
    /**
     * Registers a DOM element that the hook will use as the visual resize indicator.
     * Pass `null` to unregister (e.g. on component unmount).
     */
    registerIndicator: (element: HTMLElement | null) => void;
    /**
     * Returns props for the column resizer handle element. Pointer-down starts the drag.
     */
    getColumnResizerProps: (columnIndex?: number) => Record<string, any>;
}
/**
 * Defines editing-related methods exposed by useDataTable.
 */
export interface UseDataTableEditingExposes {
    /**
     * Checks if a row with the given key is being edited.
     */
    isEditing: (key: string | number) => boolean;
    /**
     * Starts editing a row.
     */
    startRowEdit: (event: React.SyntheticEvent, key: string | number, rowData?: Record<string, unknown>, rowIndex?: number) => void;
    /**
     * Saves a row edit.
     */
    saveRowEdit: (event: React.SyntheticEvent, key: string | number, rowData?: Record<string, unknown>, newData?: Record<string, unknown>, rowIndex?: number) => void;
    /**
     * Cancels a row edit.
     */
    cancelRowEdit: (event: React.SyntheticEvent, key: string | number, rowData?: Record<string, unknown>, rowIndex?: number) => void;
    /**
     * Starts editing a cell. `rowKey` should be passed when available so the editor stays on the same
     * logical row if its `rowIndex` shifts (e.g. tree expand/collapse).
     */
    startCellEdit: (event: React.SyntheticEvent, rowIndex: number, field: string, rowKey?: string | number) => void;
    /**
     * Completes a cell edit.
     */
    completeCellEdit: (event: UseDataTableCellEditEvent) => boolean;
    /**
     * Cancels a cell edit.
     */
    cancelCellEdit: (event: React.SyntheticEvent, rowIndex: number, field: string) => void;
    /**
     * Checks if a specific cell is being edited. When `rowKey` is provided the check prefers key
     * equality so the editor survives reshuffles of the flat row order (tree expand/collapse).
     */
    isCellEditing: (rowIndex: number, field: string, rowKey?: string | number) => boolean;
    /**
     * Returns base data-attrs for the cell editor wrapper element.
     */
    getCellEditorRootProps: (params: {
        rowIndex: number;
        field: string;
        rowKey?: string | number;
        isEditing: boolean;
    }) => Record<string, any>;
    /**
     * Returns base props for the row editor init button.
     */
    getRowEditorInitProps: () => Record<string, any>;
    /**
     * Returns base props for the row editor save button.
     */
    getRowEditorSaveProps: () => Record<string, any>;
    /**
     * Returns base props for the row editor cancel button.
     */
    getRowEditorCancelProps: () => Record<string, any>;
}
/**
 * Defines the expansion-related methods exposed by useDataTable.
 */
export interface UseDataTableExpansionExposes {
    /**
     * Checks if a row with the given key is expanded.
     * @param key The unique key of the row.
     * @returns Whether the row is expanded.
     */
    isExpanded: (key: string | number) => boolean;
    /**
     * Toggles the expansion state of a row.
     * @param event The browser event that triggered the toggle.
     * @param key The unique key of the row.
     */
    toggleExpansion: (event: React.SyntheticEvent, key: string | number) => void;
    /**
     * Returns props for the row toggle element. The shape distinguishes between a "spacer"
     * (rendered for tree-mode leaves) and a real button.
     */
    getRowToggleProps: (rowKey: string | number | undefined, item?: Record<string, unknown> | null) => {
        isSpacer: true;
        spacerProps: Record<string, any>;
    } | {
        isSpacer: false;
        rootProps: Record<string, any>;
        isExpanded: boolean;
    };
}
/**
 * Event object for DataTable selection changes.
 */
export interface UseDataTableSelectionEvent {
    /**
     * Browser event that triggered the selection change.
     */
    originalEvent: React.SyntheticEvent;
    /**
     * The new selection keys after the change.
     */
    value: SelectionKeys;
    /**
     * The type of selection action that occurred.
     */
    type: 'row' | 'checkbox' | 'radio' | 'all';
}
/**
 * Defines the selection-related methods exposed by useDataTable.
 */
export interface UseDataTableSelectionExposes {
    /**
     * Checks if a row with the given key is selected.
     * @param key The unique key of the row.
     * @returns Whether the row is selected.
     */
    isSelected: (key: string | number) => boolean;
    /**
     * Toggles the selection state of a row.
     * @param event The browser event that triggered the toggle.
     * @param key The unique key of the row.
     * @param mode The selection mode.
     */
    toggleSelection: (event: React.SyntheticEvent, key: string | number, mode: 'checkbox' | 'radio' | 'single') => void;
    /**
     * Toggles the selection of all rows.
     * @param event The browser event that triggered the toggle.
     */
    toggleAll: (event: React.SyntheticEvent) => void;
    /**
     * Checks if all rows are selected.
     * @returns Whether all rows are selected.
     */
    isAllSelected: () => boolean;
    /**
     * Handles row click selection with metaKey/shift support.
     * @param event The mouse event.
     * @param key The unique key of the row.
     */
    onRowClick: (event: React.MouseEvent, key: string | number) => void;
    /**
     * Checks if some but not all rows are selected.
     * @returns Whether some rows are selected.
     */
    isSomeSelected: () => boolean;
    /**
     * Checks if a tree node is partially selected (some descendants selected, but not all).
     * Only relevant in tree mode.
     * @param key The unique key of the node.
     * @returns Whether the node is partially selected.
     */
    isPartiallySelected: (key: string | number) => boolean;
}
/**
 * Defines sort-related methods exposed by useDataTable.
 */
export interface UseDataTableSortExposes {
    /**
     * Handles a sort event for the given field.
     * @param event The original browser event.
     * @param field The field name to sort by.
     */
    onSort: (event: React.SyntheticEvent, field: string, multiSort?: boolean) => void;
    /**
     * Returns whether the given field is currently sorted.
     * @param field The field name to check.
     * @returns Whether the field is sorted.
     */
    isSorted: (field: string) => boolean;
    /**
     * Returns the current sort order for the given field.
     * @param field The field name to check.
     * @returns The sort order (1, -1, or 0).
     */
    getSortOrder: (field: string) => SortOrder;
    /**
     * Returns the index of the given field in multi-sort metadata.
     * @param field The field name to check.
     * @returns The index of the field in multiSortMeta, or -1 if not found.
     */
    getSortIndex: (field: string) => number;
    /**
     * Returns props (data-attrs, role, aria, handlers) for a `DataTable.Sort` root element.
     */
    getSortProps: (field: string) => Record<string, any>;
}
/**
 * Event object for DataTable page changes.
 */
export interface UseDataTablePageEvent {
    /**
     * The original browser event that triggered the page change.
     */
    originalEvent: React.SyntheticEvent;
    /**
     * The new page number (0-indexed).
     */
    page: number;
    /**
     * The number of rows per page.
     */
    rows: number;
    /**
     * The index of the first row on the new page.
     */
    first: number;
    /**
     * The total number of pages.
     */
    pageCount: number;
}
/**
 * Defines pagination-related methods exposed by useDataTable.
 */
export interface UseDataTablePaginationExposes {
    /**
     * The current page number (0-indexed).
     */
    page: number;
    /**
     * The number of rows per page.
     */
    rows: number;
    /**
     * The index of the first row on the current page.
     */
    first: number;
    /**
     * The total number of pages.
     */
    pageCount: number;
    /**
     * Total number of records across all pages. In lazy mode this is the `totalRecords` prop
     * the caller supplies; otherwise it's the length of the (filtered, sorted) dataset.
     */
    totalRecords: number;
    /**
     * Handles a page change event.
     * @param event The original browser event.
     * @param page The new page number.
     */
    onPageChange: (event: React.SyntheticEvent, page: number) => void;
    /**
     * Handles a rows per page change event.
     * @param event The original browser event.
     * @param rows The new number of rows per page.
     */
    onRowsChange: (event: React.SyntheticEvent, rows: number) => void;
}
/**
 * Defines valid properties in useDataTable.
 */
export interface UseDataTableProps {
    /**
     * An array of objects to display.
     * @default null
     */
    data?: null | undefined | Record<string, unknown>[] | object[];
    /**
     * Default sort value for uncontrolled mode.
     * @default null
     */
    defaultValue?: null | undefined | string;
    /**
     * Sort field value for controlled mode.
     * @default null
     */
    value?: null | undefined | string;
    /**
     * A property to uniquely identify each row in the data.
     * @default null
     */
    dataKey?: null | undefined | string;
    /**
     * Whether the data is currently being loaded.
     * @default false
     */
    loading?: boolean | undefined;
    /**
     * Defines if data is loaded and interacted with in lazy manner.
     * @default false
     */
    lazy?: boolean | undefined;
    /**
     * Number of total records, used in lazy mode.
     * @default 0
     */
    totalRecords?: number | undefined;
    /**
     * When enabled, the table can be scrolled horizontally and/or vertically.
     * @default false
     */
    scrollable?: boolean | undefined;
    /**
     * Height of the scroll viewport in fixed pixels or the "flex" keyword for a dynamic size.
     * @default null
     */
    scrollHeight?: null | undefined | string;
    /**
     * Property name to sort by in single-sort mode.
     * @default undefined
     */
    sortField?: string | undefined;
    /**
     * Sort order for single-sort mode.
     * @default undefined
     */
    sortOrder?: SortOrder | undefined;
    /**
     * Default sort field for uncontrolled single-sort mode.
     * @default undefined
     */
    defaultSortField?: string | undefined;
    /**
     * Default sort order for uncontrolled single-sort mode.
     * @default undefined
     */
    defaultSortOrder?: SortOrder | undefined;
    /**
     * An array of SortMeta objects for multi-column sorting (controlled).
     * @default undefined
     */
    multiSortMeta?: SortMeta[] | undefined;
    /**
     * Initial multi-column sort metadata for uncontrolled mode.
     * @default undefined
     */
    defaultMultiSortMeta?: SortMeta[] | undefined;
    /**
     * Callback invoked when the sort state changes.
     * @param event The sort event object.
     */
    onSortChange?: (event: UseDataTableSortEvent) => void;
    /**
     * When true, clicking a sorted column header a third time removes the sort.
     * @default false
     */
    removableSort?: boolean | undefined;
    /**
     * Determines where null values are placed during sort.
     * 1 = nulls last (default), -1 = nulls first.
     * @default 1
     */
    nullSortOrder?: number;
    /**
     * Defines the selection mode. 'single' allows one row, 'multiple' allows many.
     * @default null
     */
    selectionMode?: 'single' | 'multiple' | null;
    /**
     * When true, row click selection requires Ctrl/Cmd key. Without it, click selects only that row.
     * Shift+Click extends range. Only applies when selectionMode is set.
     * @default false
     */
    metaKeySelection?: boolean;
    /**
     * The selection keys for controlled mode.
     * @default undefined
     */
    selectionKeys?: SelectionKeys;
    /**
     * The default selection keys for uncontrolled mode.
     * @default undefined
     */
    defaultSelectionKeys?: SelectionKeys;
    /**
     * The selection keys for controlled mode.
     * @default undefined
     * @deprecated Use `selectionKeys` instead.
     */
    selectedKeys?: SelectionKeys;
    /**
     * The default selection keys for uncontrolled mode.
     * @default undefined
     * @deprecated Use `defaultSelectionKeys` instead.
     */
    defaultSelectedKeys?: SelectionKeys;
    /**
     * Callback invoked when the selection changes.
     * @default undefined
     */
    onSelectionChange?: (event: UseDataTableSelectionEvent) => void;
    /**
     * Current page number for controlled pagination (0-indexed).
     * @default undefined
     */
    page?: number | undefined;
    /**
     * Default page number for uncontrolled pagination (0-indexed).
     * @default 0
     */
    defaultPage?: number | undefined;
    /**
     * Number of rows to display per page for controlled mode.
     * @default undefined
     */
    rows?: number | undefined;
    /**
     * Default number of rows to display per page for uncontrolled mode.
     * @default 10
     */
    defaultRows?: number | undefined;
    /**
     * Array of integer values to display inside the rows per page dropdown.
     * @default undefined
     */
    rowsPerPageOptions?: number[] | undefined;
    /**
     * Whether pagination is enabled.
     * @default false
     */
    paginator?: boolean | undefined;
    /**
     * Callback invoked when the page or rows per page changes.
     * @param event The page change event object.
     */
    onPageChange?: (event: UseDataTablePageEvent) => void;
    /**
     * The expanded keys map for controlled row expansion.
     * @default undefined
     */
    expandedKeys?: ExpandedKeys;
    /**
     * The default expanded keys map for uncontrolled row expansion.
     * @default undefined
     */
    defaultExpandedKeys?: ExpandedKeys;
    /**
     * Callback invoked when the expanded keys change.
     * @param event The expansion event object.
     */
    onExpandedChange?: (event: UseDataTableExpansionEvent) => void;
    /**
     * The field name to group rows by.
     * @default undefined
     */
    groupField?: string | undefined;
    /**
     * When enabled, columns can be reordered using drag and drop.
     * @default false
     */
    reorderableColumns?: boolean | undefined;
    /**
     * Callback invoked when columns are reordered.
     */
    onColumnReorder?: (event: UseDataTableColumnReorderEvent) => void;
    /**
     * When enabled, columns can be resized using drag and drop.
     * @default false
     */
    resizableColumns?: boolean | undefined;
    /**
     * Defines whether the overall table width should change on column resize.
     * 'fit' mode keeps total width the same. 'expand' mode increases total width.
     * @default 'fit'
     */
    columnResizeMode?: ColumnResizeMode | undefined;
    /**
     * Callback invoked when a column resize ends.
     */
    onColumnResizeEnd?: (event: UseDataTableColumnResizeEvent) => void;
    /**
     * Defines the editing mode.
     * @default undefined
     */
    editMode?: DataTableEditMode | undefined;
    /**
     * The editing keys map for controlled row editing.
     * @default undefined
     */
    editingKeys?: EditingKeys;
    /**
     * The default editing keys for uncontrolled row editing.
     * @default undefined
     */
    defaultEditingKeys?: EditingKeys;
    /**
     * Callback invoked when the editing keys change.
     */
    onEditingKeysChange?: (event: UseDataTableEditingEvent) => void;
    /**
     * Callback invoked when a row edit is initiated.
     */
    onRowEditInit?: (event: UseDataTableRowEditEvent) => void;
    /**
     * Callback invoked when a row edit is saved.
     */
    onRowEditSave?: (event: UseDataTableRowEditEvent) => void;
    /**
     * Callback invoked when a row edit is cancelled.
     */
    onRowEditCancel?: (event: UseDataTableRowEditEvent) => void;
    /**
     * Callback invoked when a cell edit is initiated.
     */
    onCellEditInit?: (event: {
        originalEvent: React.SyntheticEvent;
        field: string;
        rowIndex: number;
    }) => void;
    /**
     * Callback invoked when a cell edit completes.
     */
    onCellEditComplete?: (event: UseDataTableCellEditEvent) => void;
    /**
     * Callback invoked when a cell edit is cancelled.
     */
    onCellEditCancel?: (event: UseDataTableCellEditEvent) => void;
    /**
     * Filter metadata for controlled mode.
     * @default undefined
     */
    filters?: DataTableFilterMeta;
    /**
     * Default filter metadata for uncontrolled mode.
     * @default undefined
     */
    defaultFilters?: DataTableFilterMeta;
    /**
     * Global filter value applied across globalFilterFields.
     * @default null
     */
    globalFilter?: string | null;
    /**
     * An array of field names to search with globalFilter.
     * @default undefined
     */
    globalFilterFields?: string[];
    /**
     * Delay in ms before applying filter (for debounce).
     * @default 0
     */
    filterDelay?: number;
    /**
     * Callback invoked when filters change.
     */
    onFilter?: (event: UseDataTableFilterEvent) => void;
    /**
     * When enabled, rows can be reordered using drag and drop.
     * @default false
     */
    reorderableRows?: boolean;
    /**
     * Callback invoked when rows are reordered.
     */
    onRowReorder?: (event: UseDataTableRowReorderEvent) => void;
    /**
     * A function that returns a className for a given row.
     * @param data The row data object.
     * @param options Additional row info.
     * @returns A className string or undefined.
     */
    rowClassName?: (data: Record<string, unknown>, options: {
        index: number;
        props: UseDataTableProps;
    }) => string | undefined;
    /**
     * Callback invoked when lazy loading is triggered (page, sort, filter change in lazy mode).
     */
    onLazyLoad?: (event: UseDataTableLazyLoadEvent) => void;
    /**
     * Callback fired when a row is clicked. Does NOT fire when the click originates on interactive content
     * (buttons, inputs, checkboxes, radios) — those bubble their own semantics.
     */
    onRowClick?: (event: UseDataTableRowMouseEvent) => void;
    /**
     * Callback fired on row double-click.
     */
    onRowDoubleClick?: (event: UseDataTableRowMouseEvent) => void;
    /**
     * Callback fired on row right-click (contextmenu). Does not prevent the default menu — call
     * `event.originalEvent.preventDefault()` in the handler if you want to.
     */
    onRowContextMenu?: (event: UseDataTableRowMouseEvent) => void;
    /**
     * When enabled, rows highlight on mouse hover via `data-row-hover` attribute on root.
     * @default false
     */
    rowHover?: boolean;
    /**
     * When enabled, selected rows are highlighted via `data-highlight-on-select` attribute on root.
     * @default true
     */
    highlightOnSelect?: boolean;
    /**
     * When enabled, data is treated as a tree structure with `key`, `data`, and `children` fields.
     * Tree nodes are flattened internally and sort, filter, pagination operate on the tree structure.
     * Pagination applies to root-level nodes only.
     * @default false
     */
    treeMode?: boolean;
    /**
     * Table size variant. Adjusts cell padding and font size via CSS.
     * @default undefined
     */
    size?: 'small' | 'large' | undefined;
    /**
     * When enabled, rows are rendered with alternating background colors.
     * @default false
     */
    stripedRows?: boolean;
    /**
     * When enabled, borders are displayed between cells.
     * @default false
     */
    showGridlines?: boolean;
}
/**
 * Defines valid state in useDataTable.
 */
export interface UseDataTableState {
    /**
     * The processed data array after applying sort, filter, and pagination.
     */
    processedData: Record<string, unknown>[];
    /**
     * Whether the data is currently being loaded.
     */
    loading: boolean;
    /**
     * The current sort field.
     */
    sortField?: string;
    /**
     * The current sort order.
     */
    sortOrder?: SortOrder;
    /**
     * The current multi-sort metadata.
     */
    multiSortMeta?: SortMeta[];
    /**
     * The current selection keys.
     */
    selectionKeys: SelectionKeys;
    /**
     * The current page number (0-indexed).
     */
    page: number;
    /**
     * The number of rows per page.
     */
    rows: number;
    /**
     * The index of the first row on the current page.
     */
    first: number;
    /**
     * The total number of pages.
     */
    pageCount: number;
    /**
     * Total record count. In lazy mode this is the `totalRecords` prop; otherwise the length of
     * the filtered/sorted dataset.
     */
    totalRecords: number;
    /**
     * The paginated data array for the current page (non-lazy mode only).
     */
    paginatedData: Record<string, unknown>[];
    /**
     * The current expanded keys map.
     */
    expandedKeys: ExpandedKeys;
    /**
     * The computed group metadata for each row, keyed by row index.
     * Only populated when groupField is set.
     */
    groupMetaMap?: Map<number, RowGroupMeta>;
    /**
     * The current editing keys map.
     */
    editingKeys: EditingKeys;
    /**
     * The currently editing cell, if any.
     */
    editingCell?: {
        rowIndex: number;
        field: string;
        rowKey?: string | number;
    } | null;
    /**
     * The current filter metadata.
     */
    filters: DataTableFilterMeta;
    /**
     * The current global filter value.
     */
    globalFilter: string | null;
}
/**
 * Data attribute props for the DataTable root element.
 */
export interface UseDataTableRootProps {
    /**
     * Component scope identifier.
     */
    'data-scope': string;
    /**
     * Component part identifier.
     */
    'data-part': string;
    /**
     * Present when the datatable is loading.
     */
    'data-loading'?: string;
}
/**
 * Pre-built props for the table container element.
 */
export interface UseDataTableTableContainerProps {
    /**
     * Component scope identifier.
     */
    'data-scope': string;
    /**
     * Component part identifier.
     */
    'data-part': string;
    /**
     * Inline styles for scroll support.
     */
    style?: React.CSSProperties;
}
/**
 * Pre-built props for the table element.
 */
export interface UseDataTableTableProps {
    /**
     * Component scope identifier.
     */
    'data-scope': string;
    /**
     * Component part identifier.
     */
    'data-part': string;
    /**
     * ARIA role for the table.
     */
    role: string;
}
/**
 * Defines the methods and properties exposed by useDataTable.
 */
export interface UseDataTableExposes {
    /**
     * The state of the useDataTable.
     */
    state: UseDataTableState;
    /**
     * Pre-built data attribute props for the root element.
     */
    rootProps: UseDataTableRootProps;
    /**
     * Pre-built props for the table container element (scroll wrapper).
     */
    tableContainerProps: UseDataTableTableContainerProps;
    /**
     * Pre-built props for the table element.
     */
    tableProps: UseDataTableTableProps;
    /**
     * Returns a unique key for the given row using the dataKey property.
     * @param item The data object for the row.
     * @param index The index of the row.
     * @returns The unique key for the row.
     */
    getRowKey: (item: Record<string, unknown>, index: number) => string | number;
    /**
     * Sort-related methods and utilities.
     */
    sort: UseDataTableSortExposes;
    /**
     * Selection-related methods and utilities.
     */
    selection: UseDataTableSelectionExposes;
    /**
     * Pagination-related state and methods.
     */
    pagination: UseDataTablePaginationExposes;
    /**
     * Expansion-related methods and utilities.
     */
    expansion: UseDataTableExpansionExposes;
    /**
     * Column reorder-related methods and utilities.
     */
    columnReorder: UseDataTableColumnReorderExposes;
    /**
     * Column resize-related methods and utilities.
     */
    columnResize: UseDataTableColumnResizeExposes;
    /**
     * Editing-related methods and utilities.
     */
    editing: UseDataTableEditingExposes;
    /**
     * Filter-related methods and utilities.
     */
    filter: UseDataTableFilterExposes;
    /**
     * Row reorder-related methods and utilities.
     */
    rowReorder: UseDataTableRowReorderExposes;
    /**
     * Export-related methods and utilities.
     */
    exportData: UseDataTableExportExposes;
    /**
     * Keyboard navigation methods.
     */
    keyboard: {
        getRowTabIndex: (index: number) => number;
        onRowKeyDown: (event: React.KeyboardEvent<HTMLTableRowElement>, index: number) => void;
        focusRow: (tbody: HTMLElement | null, index: number) => void;
    };
}
/**
 * Instance of useDataTable headless.
 */
export type UseDataTableInstance = HeadlessInstance<UseDataTableProps, UseDataTableState, UseDataTableExposes>;
