/**
 *
 * The useOrganizationChart manages the state and functionality of a org chart component.
 *
 * [Live Demo](https://www.primereact.org/organizationchart/)
 *
 * @module useOrganizationChart
 * @group headless
 *
 */
import type { HeadlessInstance } from '@primereact/types/core';
import * as React from 'react';
/**
 * Defines the tree node data.
 */
export interface TreeNode {
    /**
     * Unique identifier of the node. (required)
     */
    key: string;
    /**
     * Label of the node.
     */
    label?: React.ReactNode;
    /**
     * Children of the node.
     */
    children?: TreeNode[];
    /**
     * Optional properties.
     */
    [key: string]: unknown;
}
/**
 * Collapsed state map. A key present with `true` is collapsed; absent keys are expanded (the
 * org chart is expanded by default).
 */
export type OrganizationChartCollapsedKeys = Record<string, boolean>;
/**
 * Selection state map.
 * - single / multiple mode: `Record<string, boolean>`
 * - checkbox mode: `Record<string, { checked: boolean; partialChecked: boolean }>`
 */
export type OrganizationChartSelectionKeys = Record<string, boolean | {
    checked: boolean;
    partialChecked: boolean;
}>;
/**
 * Event fired when the collapsed state changes.
 */
export interface UseOrganizationChartCollapsedChangeEvent {
    /**
     * The original event that triggered the change.
     */
    originalEvent: React.SyntheticEvent;
    /**
     * The new collapsed keys.
     */
    value: OrganizationChartCollapsedKeys;
}
/**
 * Event fired when the selection state changes.
 */
export interface UseOrganizationChartSelectionChangeEvent {
    /**
     * The original event that triggered the change.
     */
    originalEvent: React.SyntheticEvent;
    /**
     * The new selection keys.
     */
    value: OrganizationChartSelectionKeys;
}
/**
 * Defines valid properties in useOrganizationChart.
 */
export interface UseOrganizationChartProps {
    /**
     * The data of the org chart.
     */
    value?: TreeNode[];
    /**
     * The gap between nodes, as a single value or `[x, y]`.
     * @default [40, 56]
     */
    gap?: number | number[];
    /**
     * The selection mode. When omitted, nodes are not selectable. `checkbox` cascades selection to
     * descendants and reflects a partial state on ancestors.
     */
    selectionMode?: 'single' | 'multiple' | 'checkbox';
    /**
     * Controlled collapsed keys. A key with `true` is collapsed; absent keys are expanded.
     */
    collapsedKeys?: OrganizationChartCollapsedKeys;
    /**
     * Initial collapsed keys for uncontrolled mode.
     */
    defaultCollapsedKeys?: OrganizationChartCollapsedKeys;
    /**
     * Fires when the collapsed state changes.
     */
    onCollapsedChange?: (event: UseOrganizationChartCollapsedChangeEvent) => void;
    /**
     * Controlled selection keys.
     */
    selectionKeys?: OrganizationChartSelectionKeys;
    /**
     * Initial selection keys for uncontrolled mode.
     */
    defaultSelectionKeys?: OrganizationChartSelectionKeys;
    /**
     * Fires when the selection state changes.
     */
    onSelectionChange?: (event: UseOrganizationChartSelectionChangeEvent) => void;
}
/**
 * Defines valid state in useOrganizationChart.
 */
export interface UseOrganizationChartState {
    /**
     * The current collapsed keys.
     */
    collapsedKeys: OrganizationChartCollapsedKeys;
    /**
     * The current selection keys.
     */
    selectionKeys: OrganizationChartSelectionKeys;
}
/**
 * Pre-built props for a tree (connector wrapper) element.
 */
export interface UseOrganizationChartTreeProps {
    'data-scope': 'organizationchart';
    'data-part': 'tree';
    role: 'treeitem';
    'data-level': number;
    'data-collapsed'?: '';
    'data-selected'?: '';
    'data-expandable'?: '';
    'aria-level': number;
    'aria-expanded'?: boolean;
    'aria-selected'?: boolean;
}
/**
 * Pre-built props for a node element.
 */
export interface UseOrganizationChartNodeProps {
    'data-scope': 'organizationchart';
    'data-part': 'node';
    tabIndex: number;
    'data-selectable'?: '';
    'data-collapsible'?: '';
    'data-collapsed'?: '';
    'data-selected'?: '';
    onClick: (event: React.MouseEvent<HTMLDivElement>) => void;
    onKeyDown: (event: React.KeyboardEvent<HTMLDivElement>) => void;
}
/**
 * Defines the methods and properties exposed by useOrganizationChart.
 */
export interface UseOrganizationChartExposes {
    /**
     * The state of the useOrganizationChart.
     */
    state: UseOrganizationChartState;
    /**
     * Returns the org chart nodes (the original `value`).
     */
    getNodes: () => TreeNode[];
    /**
     * Looks up a node by its key.
     */
    findNodeByKey: (key: string) => TreeNode | undefined;
    /**
     * Returns the 1-based depth of a node by its key.
     */
    getNodeLevel: (key: string) => number;
    /**
     * Toggle the selection state of a node (respecting `selectionMode`).
     */
    toggleNodeSelect: (key?: string, event?: React.SyntheticEvent) => void;
    /**
     * Toggle the collapsed state of a node.
     */
    toggleNodeCollapse: (key?: string, event?: React.SyntheticEvent) => void;
    /**
     * Whether a node can be expanded/collapsed (has children).
     */
    isExpandable: (node?: TreeNode) => boolean;
    /**
     * Whether nodes are selectable (a `selectionMode` is set).
     */
    isSelectable: () => boolean;
    /**
     * Whether a node is collapsed.
     */
    isCollapsed: (node?: TreeNode) => boolean;
    /**
     * Whether a node is selected.
     */
    isSelected: (node?: TreeNode) => boolean;
    /**
     * Whether a node is partially selected (checkbox mode).
     */
    isPartiallySelected: (node?: TreeNode) => boolean;
    /**
     * Returns pre-built props for the root element.
     */
    rootProps: {
        'data-scope': 'organizationchart';
        'data-part': 'root';
        role: 'tree';
        'aria-multiselectable': boolean;
    };
    /**
     * Returns pre-built props for a tree (connector wrapper) element.
     */
    getTreeProps: (node: TreeNode | undefined, level: number) => UseOrganizationChartTreeProps;
    /**
     * Returns pre-built props for a node element.
     */
    getNodeProps: (node: TreeNode | undefined, handleClick: (event: React.MouseEvent<HTMLDivElement>) => void) => UseOrganizationChartNodeProps;
    /**
     * Returns pre-built props for a collapse toggle button element.
     */
    getCollapseButtonProps: (node: TreeNode | undefined, handleClick: (event: React.MouseEvent<HTMLButtonElement>) => void) => {
        'data-scope': 'organizationchart';
        'data-part': 'toggle';
        onClick: (event: React.MouseEvent<HTMLButtonElement>) => void;
        onKeyDown: (event: React.KeyboardEvent<HTMLButtonElement>) => void;
    };
    /**
     * Returns pre-built props for the toggle indicator (state-matched icon slot) element.
     */
    getToggleIndicatorProps: () => {
        'data-scope': 'organizationchart';
        'data-part': 'toggle-indicator';
        'aria-hidden': true;
    };
    /**
     * Pre-built props for the children subtree element.
     */
    subtreeProps: {
        'data-scope': 'organizationchart';
        'data-part': 'subtree';
        role: 'group';
    };
    /**
     * CSS custom properties for the configured gap.
     */
    orgChartStyle: React.CSSProperties;
    /**
     * Handles key down on a node.
     */
    handleNodeKeyDown: (event: React.KeyboardEvent<HTMLDivElement>, key?: string) => void;
    /**
     * Handles key down on a collapse button.
     */
    handleCollapseKeyDown: (event: React.KeyboardEvent<HTMLButtonElement>, key?: string) => void;
}
/**
 * Instance of useOrganizationChart.
 */
export type UseOrganizationChartInstance = HeadlessInstance<UseOrganizationChartProps, UseOrganizationChartState, UseOrganizationChartExposes>;
