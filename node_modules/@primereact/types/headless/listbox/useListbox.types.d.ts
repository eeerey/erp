/**
 *
 * The useListbox manages the state and functionality of a listbox component.
 *
 * [Live Demo](https://www.primereact.org/listbox/)
 *
 * @module useListbox
 * @group headless
 *
 */
import type { HeadlessInstance } from '@primereact/types/core';
import * as React from 'react';
/**
 * Event fired when the listbox's value changes.
 */
export interface UseListboxValueChangeEvent {
    /**
     * The original event that triggered the change.
     */
    originalEvent: React.SyntheticEvent;
    /**
     * The value state of the listbox. Type depends on optionValue configuration.
     */
    value: unknown;
}
/**
 * Props for the useListbox hook.
 * @template T The type of the option data.
 */
export interface UseListboxProps<T = unknown> {
    /**
     * The current value of the listbox. For single selection, this is a single value. For multiple selection, this is an array of values.
     */
    value?: unknown;
    /**
     * The default value of the listbox when used in uncontrolled mode.
     */
    defaultValue?: unknown;
    /**
     * An array of options to display in the listbox.
     */
    options?: T[];
    /**
     * Unique key for each option.
     */
    optionKey?: string;
    /**
     * Label field for each option.
     */
    optionLabel?: string;
    /**
     * Value field for each option.
     */
    optionValue?: string;
    /**
     * Field to check if an option is disabled.
     */
    optionDisabled?: string;
    /**
     * Label field for option groups.
     */
    optionGroupLabel?: string;
    /**
     * Field that contains the children options in a group.
     */
    optionGroupChildren?: string;
    /**
     * When present, it specifies that the listbox should be disabled.
     * @defaultValue false
     */
    disabled?: boolean;
    /**
     * When present, it specifies that the component should have invalid state style.
     * @defaultValue false
     */
    invalid?: boolean;
    /**
     * The locale to use for localization. Used for accessibility labels.
     */
    locale?: string;
    /**
     * When present, allows selecting multiple options.
     * @defaultValue false
     */
    multiple?: boolean;
    /**
     * When enabled, requires holding the meta key (Ctrl/Cmd) to select/deselect items in multiple selection mode.
     * @defaultValue false
     */
    metaKeySelection?: boolean;
    /**
     * When enabled, the focused option is automatically highlighted.
     * @defaultValue true
     */
    autoOptionFocus?: boolean;
    /**
     * When enabled, the focused option is automatically selected.
     * @defaultValue false
     */
    selectOnFocus?: boolean;
    /**
     * When enabled, the focused option changes on hover.
     * @defaultValue true
     */
    focusOnHover?: boolean;
    /**
     * Index of the element in tabbing order.
     * @defaultValue 0
     */
    tabIndex?: number;
    /**
     * Callback to invoke when the value changes.
     * @param {UseListboxValueChangeEvent} event - Custom change event.
     */
    onValueChange?: (event: UseListboxValueChangeEvent) => void;
}
/**
 * Defines valid state in useListbox.
 */
export interface UseListboxState {
    /**
     * The current value(s) selected in the listbox. Type depends on optionValue configuration.
     */
    value?: unknown;
    /**
     * Whether the listbox is currently focused.
     */
    focused?: boolean;
    /**
     * The index of the currently focused option. -1 if no option is focused.
     */
    focusedOptionIndex?: number;
}
/**
 * Props for the Listbox list element.
 * Includes role, tabIndex, ARIA, and keyboard/focus event handlers.
 */
export interface UseListboxListElementProps {
    /**
     * Unique ID for the list element.
     */
    id: string;
    /**
     * Component scope identifier.
     */
    'data-scope': string;
    /**
     * Component part identifier.
     */
    'data-part': string;
    /**
     * Ref for the list element.
     */
    ref: React.RefObject<HTMLUListElement | null>;
    /**
     * Tab index for the list element.
     */
    tabIndex: number;
    /**
     * ARIA role for the list element.
     */
    role: 'listbox';
    /**
     * ID of the currently focused option for screen readers.
     */
    'aria-activedescendant': string | undefined;
    /**
     * Focus event handler for the list.
     */
    onFocus: () => void;
    /**
     * Blur event handler for the list.
     */
    onBlur: () => void;
    /**
     * Keyboard event handler for the list.
     */
    onKeyDown: (event: React.KeyboardEvent) => void;
}
/**
 * Props for the Listbox filter element.
 * Includes role, ARIA, and keyboard/focus event handlers.
 */
export interface UseListboxFilterElementProps {
    /**
     * Component scope identifier.
     */
    'data-scope': string;
    /**
     * Component part identifier.
     */
    'data-part': string;
    /**
     * ARIA role for the filter element.
     */
    role: 'searchbox';
    /**
     * Autocomplete attribute for the filter input.
     */
    autoComplete: 'off';
    /**
     * Whether the filter is disabled.
     */
    disabled: boolean | undefined;
    /**
     * Index of the element in tabbing order.
     */
    tabIndex: number | undefined;
    /**
     * ID of the list element owned by this filter.
     */
    'aria-owns': string | undefined;
    /**
     * ID of the currently focused option for screen readers.
     */
    'aria-activedescendant': string | undefined;
    /**
     * Blur event handler for the filter.
     */
    onBlur: () => void;
    /**
     * Change event handler for the filter.
     */
    onChange: () => void;
    /**
     * Keyboard event handler for the filter.
     */
    onKeyDown: (event: React.KeyboardEvent) => void;
}
/**
 * Props for the Listbox root element.
 * Includes id, data attributes, and blur event handler.
 */
export interface UseListboxRootElementProps {
    /**
     * Unique ID for the root element.
     */
    id: string | undefined;
    /**
     * Component scope identifier.
     */
    'data-scope': string;
    /**
     * Component part identifier.
     */
    'data-part': string;
    /**
     * Blur event handler for focus-out tracking.
     */
    onBlur: (event: React.FocusEvent) => void;
}
/**
 * Props for a Listbox option element.
 * Includes id, role, ARIA attributes, data attributes, and mouse/touch event handlers.
 */
export interface UseListboxOptionElementProps {
    /**
     * Component scope identifier.
     */
    'data-scope': string;
    /**
     * Component part identifier.
     */
    'data-part': string;
    /**
     * Unique ID for the option element.
     */
    id: string;
    /**
     * ARIA role for the option.
     */
    role: 'option';
    /**
     * Whether the option is currently selected.
     */
    'aria-selected': boolean | undefined;
    /**
     * Whether the option is disabled.
     */
    'aria-disabled': boolean;
    /**
     * Total number of selectable options for ARIA.
     */
    'aria-setsize': number;
    /**
     * Position of this option in the set for ARIA.
     */
    'aria-posinset': number;
    /**
     * Present when the option is selected.
     */
    'data-selected'?: string;
    /**
     * Present when the option is not selected.
     */
    'data-unselected'?: string;
    /**
     * Present when the option is disabled.
     */
    'data-disabled'?: string;
    /**
     * Present when the option is focused.
     */
    'data-focused'?: string;
    /**
     * Click event handler for option selection.
     */
    onClick: (event: React.MouseEvent) => void;
    /**
     * Mouse down event handler for focus change.
     */
    onMouseDown: (event: React.MouseEvent) => void;
    /**
     * Mouse move event handler for hover focus.
     */
    onMouseMove: (event: React.MouseEvent) => void;
    /**
     * Touch end event handler.
     */
    onTouchEnd: () => void;
}
/**
 * Props for a hidden focusable sentinel element used by the listbox for focus management.
 * Includes role, ARIA, data attributes, and focus event handler.
 */
export interface UseListboxHiddenFocusableProps {
    /**
     * ARIA role for the hidden element.
     */
    role: 'presentation';
    /**
     * Ref for the hidden focusable sentinel element.
     */
    ref: React.RefObject<HTMLElement | null>;
    /**
     * Hides the element from assistive technologies.
     */
    'aria-hidden': true;
    /**
     * Data attribute marking the element as a focus guard sentinel.
     */
    'data-focus-guard': '';
    /**
     * Focus event handler for the hidden element.
     */
    onFocus: (event: React.FocusEvent) => void;
}
/**
 * Defines the methods and properties exposed by useListbox.
 * @template T The type of the option data.
 */
export interface UseListboxExposes<T = unknown> {
    /**
     * The state of the useListbox.
     */
    state: UseListboxState;
    /**
     * Reference to the list element of the listbox.
     */
    listRef: React.RefObject<HTMLUListElement | null>;
    /**
     * Reference to the first hidden focusable element for accessibility.
     */
    firstHiddenElementRef: React.RefObject<HTMLElement | null>;
    /**
     * Reference to the last hidden focusable element for accessibility.
     */
    lastHiddenElementRef: React.RefObject<HTMLElement | null>;
    /**
     * Gets the list of options for the listbox.
     * @returns {T[]} The list of options.
     */
    getOptions: () => T[];
    /**
     * Gets the unique ID for an option at a given index.
     * @param {number} index - The index of the option.
     * @returns {string} The option ID.
     */
    getOptionId: (index: number) => string;
    /**
     * Gets the label for a given option.
     * @param {T} option - The option object.
     * @returns {string | null} The option label.
     */
    getOptionLabel: (option: T) => string | null;
    /**
     * Gets the value for a given option. Return type depends on optionValue configuration.
     * @param {T} option - The option object.
     * @returns {unknown} The option value.
     */
    getOptionValue: (option: T) => unknown;
    /**
     * Checks if a given option is disabled.
     * @param {T} option - The option object.
     * @returns {boolean} True if the option is disabled.
     */
    isOptionDisabled: (option: T) => boolean;
    /**
     * Checks if a given option is a group header.
     * @param {T} option - The option object.
     * @returns {boolean} True if the option is a group.
     */
    isOptionGroup: (option: T) => boolean;
    /**
     * Gets the label for a given option group.
     * @param {T} optionGroup - The option group object.
     * @returns {string | null} The group label.
     */
    getOptionGroupLabel: (optionGroup: T) => string | null;
    /**
     * Gets the children options for a given option group.
     * @param {T} optionGroup - The option group object.
     * @returns {T[] | null} The group children.
     */
    getOptionGroupChildren: (optionGroup: T) => T[] | null;
    /**
     * Gets the ID of the currently focused option.
     * @returns {string | null} The focused option ID.
     */
    getFocusedOptionId: () => string | undefined;
    /**
     * Pre-built props for the first hidden focusable sentinel element.
     * Includes role, ARIA, data attributes, and focus event handler.
     */
    firstHiddenProps: UseListboxHiddenFocusableProps;
    /**
     * Pre-built props for the last hidden focusable sentinel element.
     * Includes role, ARIA, data attributes, and focus event handler.
     */
    lastHiddenProps: UseListboxHiddenFocusableProps;
    /**
     * Callback when an option is selected.
     * @param {React.MouseEvent | React.KeyboardEvent} event - The event.
     * @param {T} option - The selected option.
     * @param {number} [index] - The index of the option.
     */
    onOptionSelect: (event: React.MouseEvent | React.KeyboardEvent, option: T, index?: number) => void;
    /**
     * Callback when touch ends on an option.
     */
    onOptionTouchEnd: () => void;
    /**
     * Callback when the clear button is clicked.
     * @param {React.MouseEvent} event - The mouse event.
     */
    onClearClick: (event: React.MouseEvent) => void;
    /**
     * Checks if an option matches the search value.
     * @param {T} option - The option to check.
     * @returns {boolean} True if the option matches.
     */
    isOptionMatched: (option: T) => boolean;
    /**
     * Checks if an option is valid (not disabled and not a group).
     * @param {T} option - The option to check.
     * @returns {boolean} True if the option is valid.
     */
    isValidOption: (option: T) => boolean;
    /**
     * Checks if an option is valid for selection.
     * @param {T} option - The option to check.
     * @returns {boolean} True if the option is valid for selection.
     */
    isValidSelectedOption: (option: T) => boolean;
    /**
     * Checks if an option is currently selected.
     * @param {T} option - The option to check.
     * @returns {boolean} True if the option is selected.
     */
    isSelected: (option: T) => boolean;
    /**
     * Changes the focused option index.
     * @param {React.MouseEvent | React.KeyboardEvent} event - The event.
     * @param {number} [index] - The new focused index.
     */
    changeFocusedOptionIndex: (event: React.MouseEvent | React.KeyboardEvent, index?: number) => void;
    /**
     * Finds the index of the selected option.
     * @returns {number} The index of the selected option, or -1 if not found.
     */
    findSelectedOptionIndex: () => number;
    /**
     * Finds the index of the first focused option.
     * @returns {number} The index of the first focused option.
     */
    findFirstFocusedOptionIndex: () => number;
    /**
     * Scrolls the option at the given index into view.
     * @param {number} [index] - The index of the option.
     */
    scrollInView: (index?: number) => void;
    /**
     * Updates the listbox model with a new value.
     * @param {React.SyntheticEvent} event - The event.
     * @param {unknown} value - The new value.
     */
    updateModel: (event: React.SyntheticEvent, value: unknown) => void;
    /**
     * Automatically updates the model based on option selection.
     */
    autoUpdateModel: () => void;
    /**
     * Property name used for equality comparison.
     */
    equalityKey: string | undefined;
    /**
     * Checks if the listbox has a value.
     * @returns {boolean} True if there is a value.
     */
    hasValue: () => boolean;
    /**
     * Checks if two values are equal using the equality key.
     * @param {unknown} value1 - The first value.
     * @param {unknown} value2 - The second value.
     * @returns {boolean} True if the values are equal.
     */
    isEquals: (value1: unknown, value2: unknown) => boolean;
    /**
     * Pre-built props for the root element.
     * Includes id, data attributes, and blur event handler.
     */
    rootProps: UseListboxRootElementProps;
    /**
     * Pre-built props for the list element.
     * Includes role, tabIndex, ARIA attributes, and keyboard/focus event handlers.
     */
    listProps: UseListboxListElementProps;
    /**
     * Pre-built props for the filter element.
     * Includes role, ARIA attributes, and keyboard/focus event handlers.
     */
    filterProps: UseListboxFilterElementProps;
}
/**
 * Instance of useListbox headless.
 * @template T The type of the option data.
 */
export type UseListboxInstance<T = unknown> = HeadlessInstance<UseListboxProps<T>, UseListboxState, UseListboxExposes<T>>;
