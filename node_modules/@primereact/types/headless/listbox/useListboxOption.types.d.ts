/**
 *
 * The useListboxOption manages the state and functionality of a Listbox option.
 *
 * [Live Demo](https://www.primereact.org/listbox/)
 *
 * @module useListboxOption
 * @group headless
 *
 */
import type { HeadlessInstance } from '@primereact/types/core';
import type { UseListboxOptionElementProps } from './useListbox.types';
import type { UseListboxInstance } from './useListbox.types';
/**
 * Defines valid properties in useListboxOption.
 */
export interface UseListboxOptionProps {
    /**
     * The option data object.
     */
    option?: unknown;
    /**
     * The index of the option in the list.
     */
    index?: number;
    /**
     * Whether the option is a group header.
     * @default false
     */
    group?: boolean;
    /**
     * Whether the option is disabled.
     * @default false
     */
    disabled?: boolean;
    /**
     * The parent listbox instance providing state and methods.
     */
    context?: UseListboxInstance | undefined | null;
}
/**
 * Defines valid state in useListboxOption.
 */
export interface UseListboxOptionState {
    /**
     * Whether the option is currently selected.
     */
    selected: boolean;
    /**
     * Whether the option is currently focused.
     */
    focused: boolean;
    /**
     * Whether the option is disabled.
     */
    disabled: boolean;
    /**
     * Whether the option is a group header.
     */
    group: boolean;
}
/**
 * Defines the methods and properties exposed by useListboxOption.
 */
export interface UseListboxOptionExposes {
    /**
     * Pre-built props for a non-group option element.
     * Includes id, role, ARIA attributes, data attributes, and mouse/touch event handlers.
     */
    optionProps: UseListboxOptionElementProps;
    /**
     * Pre-built props for a group option element.
     * Includes data-scope and data-part attributes.
     */
    groupProps: {
        'data-scope': string;
        'data-part': string;
    };
    /**
     * Pre-built props for the option indicator element.
     * Includes data-scope, data-part, and data-selected/data-unselected attributes.
     */
    optionIndicatorProps: {
        'data-scope': string;
        'data-part': string;
        'data-selected'?: string;
        'data-unselected'?: string;
    };
}
/**
 * Instance of useListboxOption headless.
 */
export type UseListboxOptionInstance = HeadlessInstance<UseListboxOptionProps, UseListboxOptionState, UseListboxOptionExposes>;
