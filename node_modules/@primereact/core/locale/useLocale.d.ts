import type { LocaleValue } from '@primereact/types/core';
/**
 * Returns the active locale as `{ lang, messages, t }`. Reads from the LocaleContext so it
 * re-renders on language changes; falls back to the singleton when no provider is present.
 */
export declare function useLocale(): LocaleValue;
