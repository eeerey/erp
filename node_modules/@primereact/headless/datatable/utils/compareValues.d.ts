import type { SortOrder } from '@primereact/types/headless/datatable';
/**
 * Compares two values for sorting.
 * Delegates to `@primeuix/utils` `sort` with a numeric-aware locale comparator.
 * @param nullSortOrder Determines where null/empty values are placed. 1 = nulls last (default), -1 = nulls first.
 */
export declare const compareValues: (valueA: unknown, valueB: unknown, order: SortOrder, nullSortOrder?: number) => number;
/**
 * Sorts data by a single field.
 * @param nullSortOrder Determines where null/empty values are placed. 1 = nulls last (default), -1 = nulls first.
 */
export declare const sortSingle: (data: Record<string, unknown>[], field: string, order: SortOrder, nullSortOrder?: number) => Record<string, unknown>[];
/**
 * Sorts data by multiple fields.
 * @param nullSortOrder Determines where null/empty values are placed. 1 = nulls last (default), -1 = nulls first.
 */
export declare const sortMultiple: (data: Record<string, unknown>[], multiSortMeta: {
    field: string;
    order: SortOrder;
}[], nullSortOrder?: number) => Record<string, unknown>[];
